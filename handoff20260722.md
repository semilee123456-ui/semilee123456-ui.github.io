# 참택스 작업 내역 (2026-07-22, 자동 진행분)

브랜치: `claude/history-review-lyyqx9`
최신 커밋: `226a181` (기존 `e3b061d` 위에 추가됨, main과 동일한 지점에서 시작)

## ⚠️ GitHub push 실패

이번 세션은 GitHub push 권한이 막혀 있습니다:
```
Permission to semilee123456-ui/semilee123456-ui.github.io.git denied to semilee123456-ui.
```
(참고: 이전 세션의 인수인계 문서에 언급된 "GitHub App authorized but not installed"와 같은
계열의 문제로 보입니다. git push, GitHub API(push_files, create_pull_request) 양쪽 다 403으로
막혀있는 것까지 확인했습니다.) 그래서 아래 작업은 로컬 커밋으로만 존재하고,
`chamtax-repo.bundle`에 담아드립니다.

## 반영 방법

```bash
cd semilee123456-ui.github.io   # 로컬 클론
git fetch /path/to/chamtax-repo.bundle claude/history-review-lyyqx9:claude/history-review-lyyqx9
git push -u origin claude/history-review-lyyqx9
```
main과의 공통 조상이 바로 직전 커밋이라 fast-forward push가 될 겁니다. 별도 PR 없이 그냥
main으로 merge(또는 fast-forward)해도 됩니다.

## 이번 세션에서 자동으로 처리한 작업 (커밋 순서대로)

1. **국가지도 실제 지리 데이터로 재디자인** (`232057a`) — 기존엔 국경선이 부정확한 장식용
   대륙 모양이라 "어느 나라인지 안 와닿는다"는 피드백이 있었음. `johan/world.geo.json`
   (Natural Earth 기반)에서 미국 + 아시아 20개국의 실제 국경선을 가져와 등장방형 투영으로
   그려 넣음. 나라를 탭하면 핀뿐 아니라 실제 국경선 자체도 하이라이트되도록 개선.
   라이트/다크모드, 모바일 확인 완료.
2. **개인정보처리방침·면책조항·문의하기 화면 스크린샷 감사** (`232057a`) — 다크모드·고대비·
   큰글씨·10개 언어(아랍어·우르두어 RTL 포함) × 3화면 = 120개 조합 자동 검사, 이슈 0건.
3. **확률 페이지에 재미 애니메이션 추가** (`232057a`) — "역대 가장 많이 나온 번호" 섹션이
   텍스트로만 나열되어 있던 걸, 실제 당첨공 모양(draw-ball)으로 시각화하고 아코디언을 펼치면
   공이 하나씩 통통 튀며 나타나는 애니메이션 추가.
4. **전체 회귀 테스트 재실행** — `home_audit.js`/`faq_audit.js`/`audit_odds_compare.js` 모두
   `ISSUES: 0`, 22개 언어 전체에 대해 콘솔 에러·가로 스크롤 없음 확인.
5. **전체 사이트 버그·언어 혼용 감사** (신규 작업, 코드 변경 없음 — 감사만 진행, 이슈 없었음):
   - 메인 앱(SPA) 22개 언어 × 7개 화면(홈/비교/확률/FAQ/개인정보/면책/문의) + 인터랙티브
     위젯(번개추첨, 내번호비교, 잭팟계산기 드로어) 전수 스캔 → 한국어/영어 누출 0건
   - 42개 SEO 랜딩페이지(아랍어·베트남어·태국어 등) 전수 확인 → 언어 혼용처럼 보였던 부분은
     전부 의도된 것("한국어로 보기" 링크, "세무사"·"종합소득세" 등 한국 고유 용어 원어 병기).
     실제 버그 없음
   - 깨진 링크·이미지·콘솔 에러 43개 페이지 전체 0건, sitemap.xml/robots.txt 정상
6. **저장소 루트 레거시 파일 정리** (`981e915`) — `i18n/` + `i18n-source/` + `scripts/` 구조로
   리팩터되기 전에 남아있던 루트 레벨 중복 파일 26개 삭제/정리:
   - `ar.json`~`zh.json`(22개 언어 파일), `translations.json`, `build-i18n.js` — 전부
     실제 소스(`i18n-source/translations.json`, `i18n/*.json`, `scripts/build-i18n.js`)와
     내용이 어긋난 옛 스냅샷이고 코드 어디서도 참조되지 않는 죽은 파일이었음
   - 인코딩이 깨진 파일명의 옛 인수인계 문서 중복 2개 삭제 (`handoff-2026-07-17.md`와
     바이트 단위로 완전히 동일)
   - 인코딩 깨진 "코드 가이드북" 문서 1개는 내용이 고유해서 삭제 대신 파일명만 정상 UTF-8로
     복구 (`참택스_코드_가이드북.md`)
   - `manifest.json`(PWA 매니페스트, 실제 참조됨)은 그대로 유지

모든 작업 후 회귀 테스트 3종 재확인 완료, `ISSUES: 0`.

7. **공유카드 기본값 문제 수정** (`226a181`) — 계산기에 아무 금액도 입력하지 않은 기본
   상태(placeholder만 있고 실제 입력값은 없음)에서 "결과 공유하기"를 누르면, 계산해본 적도
   없는 기본값(100M USD)이 마치 실제 결과인 것처럼 "793억원" 카드로 나가던 문제.
   `isAmountManuallyEdited`(사용자가 직접 입력했거나 슬라이더/퀵필 버튼을 조작한 적 있는지
   이미 추적하던 플래그)를 확인해서, 실제로 조작한 적이 있을 때만 결과 카드를 공유하고 그
   전에는 특정 금액 없이 사이트 소개 문구(하나·태그라인)만 공유하도록 수정. 5개 언어
   (한국어/영어/중국어/아랍어/일본어)로 정상 동작 확인, Web Share API·클립보드 폴백 둘 다 확인.

## 처리하지 않은 항목 (다음에 필요하면 이어서)

- **파워볼 잭팟 금액 데이터 보강**: usamega.com에서 더 오래된 구간을 복사해주셔야 진행 가능
  (사용자 자료 제공 필요)

## 회귀 테스트 실행법 (참고)
```bash
python3 -m http.server 9000 &
NODE_PATH=/opt/node22/lib/node_modules node tests/home_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/faq_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/audit_odds_compare.js
```

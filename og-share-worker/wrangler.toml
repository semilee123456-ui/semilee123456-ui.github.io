name = "chamtax-og-share"
main = "src/index.js"
compatibility_date = "2026-07-31"
compatibility_flags = ["nodejs_compat"]

# 배포하면 기본적으로 https://chamtax-og-share.<your-cloudflare-subdomain>.workers.dev 에서
# 바로 동작합니다 — 커스텀 도메인/DNS 설정 없이도 됩니다. 나중에 원하면 아래 주석을 풀고
# 커스텀 서브도메인(예: share.chamtax.com)을 연결할 수 있습니다(그 경우 해당 도메인의 DNS가
# Cloudflare에 있어야 함). 지금 당장은 필요 없습니다.

# [[routes]]
# pattern = "share.chamtax.com/*"
# custom_domain = true

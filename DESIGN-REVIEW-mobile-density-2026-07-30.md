# 모바일 화면 정보 밀도 점검 — 디자인팀 인수인계 (2026-07-30)

## 요청 배경
"휴대폰으로 보면 글씨도 많고 복잡하고 한눈에 안 들어온다"는 피드백으로 홈 화면
(`index.html`의 `#view-home`, 첫 화면)을 코드 기준으로 점검함. 실제 리디자인은
디자인팀 판단이 필요한 작업이라 이 세션에서 직접 뜯어고치지 않고, **점검 결과와
구체적 실행 아이디어만 정리**함(토큰 절약 목적).

## 먼저 확인한 것 — "복붙 중복 텍스트"는 이미 대부분 정리된 상태
과거 세션들이 실제 중복 마크업 2건(연금 안내 버튼 중복, 화면에 보이지도 않던
`#home-final-basis` 죽은 문단)을 이미 찾아서 삭제함(2026-07-30 이전 세션, `HANDOFF.md`
"구현 4" 참고). 이번에 다시 훑어봤을 때 **완전히 똑같은 문장이 같은 화면에 두 번
찍혀있는 사례는 못 찾았음** — 지금 남은 "복잡해 보임" 문제는 글자가 중복돼서가
아니라, **켜져 있는 정보량 자체가 많고 서로 다른 자리에서 같은 사실을 조금씩 다른
말로 반복 설명**하기 때문. 아래 표는 그 지점들.

## 정보 밀도 핫스팟 (파일 기준)

| 위치 (`index.html` 라인) | 문제 | 제안 |
|---|---|---|
| 510행 `#introPersonaAccordion` (기본 펼침) | 첫 화면 최상단에 "나는 어떤 경우일까요?" 카드가 **항상 펼쳐진 채로** 뜸 — 안에 4개 행, 그 중 2개는 20~26개 옵션 `<select>` + "이동" 버튼까지 포함. 계산기 보러 온 사람이 스크롤 전에 이 텍스트 벽부터 만남 | 이미 열어두기로 확정한 이유(재외국민/외국인 방문자가 자기 경우를 못 찾고 이탈하는 것 방지, 2026-07-25 결정)가 있어서 접기만 하면 안 됨. **대안**: (1) localStorage로 "한 번 본 사람"은 다음 방문부터 접힌 상태로 시작 (2) 4개 행을 지금처럼 다 펼쳐두지 말고 "다른 경우 더보기" 형태로 1차는 2줄만 보여주고 나머지 접기 |
| 593~651행 `.result-hero` | 결과 카드 하나에 카테고리 태그·금액·세전/세후 비교·시각화 바·마일스톤 배지·통화별 안내문 최대 4종(조건부)·기준 요약·환율 라인·연금 아코디언·공유/저장 버튼까지 최대 11개 요소가 순서대로 쌓임 | 전부 필요한 정보이긴 하나 시각적 위계가 평평함(다 같은 굵기로 쌓여있음) — "핵심 숫자 1~2개 + 나머지는 보조 정보"로 크기·색 대비를 나눠서 스캔 우선순위를 주는 게 디자인 쪽 작업으로 적합 |
| 654~827행 `.input-card` | 안내문(`krwHintFull`) → 잭팟 바로입력 버튼 2개 → 국가/통화 select 2개 → 탭 3개 → 탭별 슬라이더 → "세금 계산 기준" 국가 토글(4개+더보기 접이식) → 기준 안내문(`basisHint`)까지 한 카드 안에 다 들어있음 | 입력 카드 하나가 사실상 "금액 입력 + 통화 설정 + 국가 설정 + 세금 기준 설정" 4가지 다른 작업을 다 떠안고 있음 — 카드를 용도별로 쪼개거나(예: 통화/국가 설정을 별도 접이식으로), 처음엔 "금액 입력"만 보이고 나머지는 필요할 때 펼치는 구조가 스캔하기엔 더 나을 수 있음 |
| 655행 `input.krwHintFull`, 767행 `input.announcedHint`, 901~906행 `flowExplainTitle/List/Note` | "일시불은 발표 금액의 약 45~60%(또는 58%)"라는 같은 사실이 표현만 바꿔 3곳(일시불 탭 힌트, 연금액 탭 힌트, "결과 더 자세히 보기"의 설명 박스)에 각각 등장 | 세 곳이 서로 다른 탭/아코디언이라 동시에 보이진 않지만, 같은 숫자를 세 벌 따로 유지·번역해야 해서 유지보수 부담도 있음 — 하나의 "일시불 vs 연금 차이" 공용 설명으로 합치고 나머지는 그 설명으로 연결하는 링크만 두는 방향 추천 |
| 621~642행 연금 아코디언(항상 노출, 접힘) vs 897~936행 "결과 더 자세히 보기"(접힘) | 둘 다 접힌 상태지만 나란히 있어서, 펼치면 둘 다 "일시불/연금 차이·세율 설명"류 정보를 담고 있음(전자는 연금 수령액 표, 후자는 세율 항목·용어 설명) — 사용자 입장에서 "더 자세히"가 두 군데로 나뉜 느낌 | 하나의 "더 자세히 보기"로 합치고 내부를 탭이나 섹션으로 나누는 편이 "더 볼 곳이 어디였지?"하는 혼란을 줄임 |
| 940~959행 `.explore-section`, 961~997행 잭팟 더보기, 999~1022행 재미로 보기, 1024행 광고, 1026~1067행 신뢰 섹션 | 계산 완료 후에도 스크롤할 콘텐츠 블록이 5개 더 있음(둘러보기 카드 3장, 잭팟 접이식, 재미 콘텐츠 접이식, 광고, 신뢰 배지) | 계산기 자체 목적(세후 실수령액 확인)은 이미 끝난 지점이라 개별 블록 문제는 아니지만, 전체 페이지 길이가 상당히 김 — 우선순위 낮은 블록(재미로 보기, 신뢰 배지)을 홈이 아니라 별도 탭/링크로 빼는 것도 검토할 만함 |

## 실행 우선순위 제안 (디자인팀 판단용, 순서는 참고용)
1. **Quick win**: 재방문자에게는 `#introPersonaAccordion` 기본 접힘(최초 1회만 펼침) — 코드 변경 작으면서 첫 화면 체감 길이를 가장 크게 줄임
2. **중간**: 45~60%/58% 설명 3곳을 하나의 공용 설명 + 링크로 통합
3. **구조 변경(디자인 필요)**: `.input-card` 안 4가지 설정(금액/통화/국가/기준)을 단계별 또는 섹션별로 시각적으로 분리
4. **선택**: 계산 완료 후 하단 콘텐츠 블록(재미로 보기·신뢰 배지 등) 노출 방식 재검토

## 참고 스크린샷
첨부한 `mobile_home_initial.png`(390px 폭, 홈 화면 최초 로드 상태, 전체 스크롤 캡처)에서
위 표의 각 구간을 실제 세로 길이로 확인 가능.

## 참고 (반영 전 확인할 것)
- 이 문서는 코드 변경 없이 순수 점검만 담음(요청대로 토큰 절약).
- `input.krwHintFull`/`input.announcedHint`/`home.flowExplainNote` 등은 26개 언어
  전부 번역되어 있는 키라, 문구를 합치거나 지울 때는 `i18n-source/translations.json`도
  같이 정리해야 함(`node scripts/build-i18n.js` 재실행 필수) — `HANDOFF.md`의
  "다국어(i18n) 작업 방법" 섹션 참고.
- `#introPersonaAccordion` 기본 펼침 결정 이유(2026-07-25)는 index.html 505~509행
  주석에 남아있음 — 접는 방향으로 바꾸기 전에 그 배경(재외국민/외국인 방문자 이탈 방지)을
  대체할 방법이 있는지 먼저 확인할 것.

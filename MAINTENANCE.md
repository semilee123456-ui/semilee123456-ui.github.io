# 참택스(ChamTax) — 연례 점검 체크리스트

이 파일은 자주 안 건드려서 잊어버리기 쉬운, 시간이 지나면서 저절로 낡는 항목들을 모아둔
체크리스트예요. 매년 1월에 (아래 "실행 방법" 참고로 자동 리마인드됨) 훑어보고, 발견된
문제는 고치거나 최소한 "점검 이력"에 남겨서 다음 해에 또 처음부터 확인하지 않게 할 것.

petscan-ai(사용자의 다른 프로젝트)의 동명 파일을 벤치마킹해 2026-08-22에 만듦 — 그
프로젝트가 "자주 안 건드려서 낡는 항목"을 매년 자동으로 상기시키는 방식이 참택스에도
그대로 적용될 수 있다고 판단함.

## 매년 1월에 확인할 것

1. **도메인 갱신 상태** — `chamtax.com`(`CNAME` 파일, GitHub Pages 커스텀 도메인) 등록이
   자동 갱신되는지, 만료일이 임박하지 않았는지 레지스트라 콘솔에서 확인. 세션이 대신
   로그인할 수 없으니 사용자에게 화면 확인을 요청할 것. 도메인이 만료되면 사이트 전체가
   다운되는 가장 치명적인 항목이라 최우선.
2. **미국 연방/50개 주 세율·과세표준 변경** — 미 연방 원천징수율(24%/37%)이나 물가연동
   구간, 메릴랜드·애리조나 등 비거주자 예외 조항, 각 주 복권세 관련 법 개정이 있었는지
   가볍게 확인(전수 재검증이 아니라 "올해 바뀐 게 있는지" 뉴스/공식 발표 확인 수준).
   `script.js`의 `TAX_MODEL`·`STATE_TAX` 관련 상수가 실제와 어긋나면 회귀 테스트
   (`drift_consistency_test.js`)로는 못 잡음 — 법령 자체가 바뀐 건 사람이 확인해야 함.
3. **42개국 세율·조세조약 변경** — `data/country-lottery-tax-rates.json`의 `confidence:
   estimate/approximate` 국가부터 우선순위로, 그 해 세법 개정 뉴스가 있었는지 확인(41개국
   전수 재조사 아님). 발견되면 `mcp-server/tax-data.js`·`scripts/build-lottery-tax-data-hub.js`의
   `COUNTRY_META`까지 같이 고치고 `tests/mcp_sync_check.js` 재실행 — 2026-08-21 세션에서
   이 3곳이 동시에 낡아있었던 전례 있음(HANDOFF-ARCHIVE.md 참고).
4. **환율 폴백값(`EXCHANGE_RATE_*`, `script.js`)** — 실시간 API 실패 시에만 쓰는 안전망이라
   방치돼도 화면 오류는 없지만, 2026-08-22 점검 때 최대 36일 낡아있던 걸 처음 발견함. 1년에
   한 번은 Frankfurter(`https://api.frankfurter.app/latest?from=USD&to=...`)와
   open.er-api.com으로 32개 통화 전부 재조회해서 갱신.
5. **`CPI_BASE_YEAR`(script.js)** — 해당 연도 미국 CPI 연평균이 확정되면 갱신(전년도 10월
   결측 등 예외 상황이 있으면 HANDOFF.md "알려진 미해결 항목"의 기존 판단 참고).
6. **검색엔진 웹마스터 도구 소유 확인**(GSC·네이버·Bing·Daum·Yandex) — 소유 확인이
   만료됐거나 재인증이 필요한 곳이 있는지 각 콘솔에서 확인. 확인 파일들(`google*.html`,
   `naver*.html`, `yandex_*.html`, `robots.txt`의 `#DaumWebMasterTool:...`)이 실제로
   최신 계정과 일치하는지도 같이 확인.
7. **`robots.txt`의 AI 크롤러 허용 목록** — 지난 1년 사이 새로 등장한 AI 크롤러(신규 LLM
   봇 User-Agent)가 있으면 허용 목록에 추가할지 검토.
8. **`sitemap.xml` 정합성** — 실제 파일과 대조해서 빠진 페이지·죽은 링크가 없는지,
   `lastmod`이 극단적으로 오래된 항목 중 실제로 콘텐츠가 바뀌었는데 갱신 안 된 게 없는지
   확인. `tests/broken_link_audit.js`로 기계적 검증은 가능하나, "내용이 바뀌었는데
   lastmod만 안 올라간" 건 못 잡음.
9. **AdSense/GA4 정책 준수** — 구글 정책이 바뀐 게 있는지, 특히 쿠키·개인정보 관련 요구
   사항이 늘었다면 `view-privacy`(개인정보처리방침)가 최신 상태인지 재확인. 애드센스
   재심사 진행 상황도 같이 확인(2026-08-22 기준 "아직 멀었음" 상태).
10. **CDN 고정 버전**(Pretendard 폰트 `cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9`)
    — 신버전으로 갱신할 가치가 있는지 가볍게만 확인. 급한 항목 아님, 문제없으면 그냥
    넘어갈 것.
11. **`llms.txt`/`llms-full.txt`/`feed.xml`/`feed.json`** — 그 해 새로 생긴 랜딩페이지나
    큰 changelog 항목이 반영 안 됐으면 `node scripts/generate-feed.js` 재실행, `llms-full.txt`는
    페이지 내용이 크게 바뀐 경우에만 `node scripts/generate-llms-full.js` 재실행(사소한
    문구 수정 정도로는 재실행 불필요).

## 실행 방법

이 체크리스트는 매년 1월 1일 01:00 UTC에 새 세션을 띄우는 Routine("참택스 연례 점검
(매년 1월 1일)", `trig_01Qp5GWvpfhcdGCRvNqajSuG`, 2026-08-22 생성)으로 자동 리마인드되도록
예약돼 있음(실제 점검·수정은 그 세션이 이 파일을 보고 진행). 그 세션은 GitHub MCP 도구
없이 뜰 수 있으니, PR을 못 올리면 브랜치에 커밋·push까지만 하고 사용자에게 직접 병합을
요청하도록 지시돼 있음. 세션이 대신 로그인할 수 없는 항목(1, 6)은 사용자에게 텍스트로
안내하는 방식으로 진행할 것.

## 점검 이력

(아직 실행 이력 없음 — 첫 점검 후 날짜 + 발견 사항 + 조치 요약을 여기 추가할 것. 문제
없이 통과한 항목은 "이상 없음"이라고만 짧게 적을 것.)

# 참택스(ChamTax) 프로젝트 — 인수인계 문서 (통합본)

이 파일 하나가 **유일한 정식 인수인계 문서**입니다. `handoff-*.md`로 흩어져있던 예전 문서
6개(2026-07-17 ~ 2026-07-23)를 이 파일 하나로 합쳤습니다. **새 세션을 시작하면 이 파일 전체를
Claude에게 읽게 하세요.**

**앞으로도 새 `handoff-...-partN.md` 파일을 만들지 말고, 이 파일 안의 "작업 이력" 섹션 맨 아래에
새 세션 항목을 이어서 추가할 것.** "지금 상태" 관련 섹션(프로젝트 개요·아키텍처·회귀
테스트·알려진 미해결 항목)은 바뀐 내용이 있으면 그때그때 덮어써서 항상 최신 상태를 유지할 것 —
날짜별로 쌓아두지 말고.

---

## 📌 새 세션 시작 시 지켜야 할 것

1. **항상 서브에이전트(Agent tool)로 시작할 것.** 메인 세션에서 탐색·점검·수정을 전부 직접
   하지 말고, 실질적인 작업은 서브에이전트에게 맡겨서 메인 세션의 컨텍스트를 아낄 것.
2. **이 저장소는 공개 저장소임(`semilee123456-ui/semilee123456-ui.github.io`).** 커밋되는
   모든 글(커밋 메시지, 코드 주석, 이 인수인계 문서 포함)은 GitHub에 업로드되면 누구나 볼 수
   있다고 가정하고 작성할 것.
3. **인수인계 문서(이 파일)에 쓰려는 내용 중 공개 저장소에 올리기 부적절한 게 있으면**(민감
   정보, 사용자 개인 신상, 내부용으로만 남기고 싶은 메모 등) — **이 파일에 넣지 말고, 대신
   사용자에게 채팅으로 직접 말할 것.** 조용히 빼놓기만 하지 말고, "이런 내용이 있는데
   공개 파일엔 못 적어서 여기 따로 말씀드려요"라고 알려줄 것.

---

## ⚠️ GitHub push가 항상 막혀있음 (구조적 제약, 매 세션 재확인 불필요)

이 세션(Claude Code)은 **GitHub에 직접 push할 권한이 없습니다** — `git push` 시도하면 항상
`403`(Anthropic GitHub App이 Authorize만 되고 Install은 안 된 상태로 이미 진단 완료된 알려진
이슈). GitHub API(`push_files`, `create_pull_request`)도 마찬가지로 막혀있음을 확인했습니다.
재진단하지 말고 아래 방식을 그대로 따르세요:

1. Claude가 로컬에서 파일을 고치고 로컬 git에는 커밋함
2. `SendUserFile`로 바뀐 파일(들)을 사용자에게 직접 전달 — 여러 개면 zip 하나로 묶어서 전달
3. **사용자가 GitHub 웹 UI에서 "파일 업로드"로 직접 덮어씀**

커밋 서명 관련 훅 경고("Unverified")는 매번 뜨는데, `git config`는 이미 올바르게
(`noreply@anthropic.com` / `Claude`) 설정되어 있고 — 단지 이 브랜치가 아직 GitHub에
push되지 않아서 GitHub이 서명을 검증할 대상 자체가 없는 것뿐입니다. 해결 불가능/불필요한
표시상의 문제이니 매번 재진단하지 마세요.

**파일명 하이픈 버그**: GitHub 웹 업로드 시 파일명의 하이픈(`-`)이 사라지는 문제가 이 계정에서
반복 확인됨. 새 파일을 만들 때는 하이픈 대신 언더스코어(`_`)를 기본으로 쓸 것. 이미 라이브에
있는 기존 파일명(하이픈 포함)은 검색엔진 인덱싱 때문에 바꾸지 말 것.

---

## 프로젝트 개요

- **이름**: 참택스 (ChamTax)
- **내용**: 미국 복권(파워볼·메가밀리언즈) 당첨금의 세후 실수령액 계산기. 세금 계산 기준을
  한국(kr)/미국(us)/중국(cn)/인도(in) 4개국 중 고를 수 있음(미국은 주별 세율까지).
- **호스팅**: GitHub Pages, 저장소 `semilee123456-ui/semilee123456-ui.github.io`
- **빌드 도구 없음** — 순수 HTML/CSS/JS (i18n JSON 빌드용 Node 스크립트 하나만 예외)
- **대상**: 한국에 거주하는 40~60대가 주 타겟 — 접근성(최소 폰트 크기, 명암비) 기준이
  명시적으로 있음. 한국 거주 외국인(재외국민이 아닌, 한국에 사는 외국인)도 별도 페르소나로
  지원함 — FAQ 등에서 두 페르소나를 혼동하지 않도록 주의(아래 "대상 배지" 참고).
- **22개 언어 지원**: ko(기본, HTML에 직접 한글로 박혀있음), en/zh/vi/th/ru(이상 5개는
  `pickLang`의 위치 인자), 나머지 16개(km/ne/id/my/si/uz/mn/kk/ky/ur/bn/lo/ja/ar/hi/fr/tl —
  `pickLang`의 `more` 객체 인자로 처리, 정확히는 17개).

### 파일/폴더 구조
```
index.html          메인 SPA (홈/비교/확률체감/FAQ/개인정보/면책/문의 화면이 전부 .view로 존재)
styles.css           전체 스타일 (:root 디자인 토큰, 다크모드는 :root[data-theme="dark"])
script.js            전체 로직 (세금 계산 + 렌더링 + i18n — 매우 큰 파일, 1MB+)
odds-data.js         확률체감 탭 전용 대용량 데이터(역대 당첨번호·잭팟 아카이브), 그 탭 열 때만 지연 로드
manifest.json        PWA 매니페스트
sitemap.xml          SEO 사이트맵

i18n/                언어별 번역 JSON 22개 — 빌드 산출물, 직접 고치지 말 것
i18n-source/translations.json   번역 원본(source of truth) — 여기를 수정
scripts/build-i18n.js           translations.json → i18n/*.json 생성 스크립트

tests/               회귀 테스트 스크립트 9개 (아래 "회귀 테스트" 섹션 참고)

*.html (그 외 40여 개)   언어별/국가별 SEO 랜딩 페이지 — 메인 계산기와 별개, 각자 <style> 안에
                         자체 :root 색상/폰트 변수를 중복 정의(반복적 버그 원인이었음 — 새
                         디자인 토큰을 바꿀 때 이 페이지들도 같이 확인할 것)
```

### 다국어(i18n) 작업 방법
1. `i18n-source/translations.json`에서 해당 키의 번역을 수정 (**`i18n/*.json`을 직접 고치지
   말 것** — 다음 빌드 때 덮어써짐). 큰 JSON 파일 전체를 `json.dump()`로 재직렬화하면 기존의
   수작업 포맷팅이 깨져서 거대한 diff가 남으므로, 바뀌는 부분만 타겟팅한 문자열 치환으로 수정할 것.
2. `node scripts/build-i18n.js` 실행 → `i18n/*.json` 22개 재생성
3. HTML 정적 텍스트: `data-i18n`/`data-i18n-html`/`data-i18n-placeholder`/`data-i18n-aria-label`
   속성 + `resolveI18n(key)`가 `i18n/{lang}.json`을 fetch해서 치환. **주의**: plain `data-i18n`은
   언어 전환마다 `el.textContent`로 통째로 덮어써지므로, 원문 안에 `<span>` 같은 마크업을 넣어도
   다음 전환 때 사라짐 — 마크업이 필요하면 `data-i18n-html`을 쓸 것(`tests/i18n_attr_lint.js`가
   이 오사용을 사전 경고함).
4. JS 동적 문자열: `pickLang(ko, en, zh, vi, th, ru, more)` — 6개는 위치 인자, 나머지 17개는
   `more` 객체. **`more`를 빠뜨리면 그 17개 언어에서 영어로 조용히 폴백됨** — 과거 세션에서
   발견된 버그의 상당수가 이 실수였음.

---

## 코드 아키텍처 핵심 패턴

- **`sharedCountry`**: 'kr'/'us'/'cn'/'in'/... — 사용자가 고른 "세금 계산 기준". 홈 화면과
  국가비교 화면이 이 값을 공유함. `data-basis="kr"` 같은 속성으로 FAQ 항목을 세금 기준별로도
  필터링함(`filterFaq()`).
- **`currentLang`**: 'ko'/'en'/... — 표시 언어. localStorage에 저장 안 되고 새로고침하면
  'ko'로 리셋됨(`?lang=` URL 파라미터로 시작은 가능, 적용 후 URL에서 자동 제거).
- **FAQ 대상 배지**: `data-basis`가 "어느 나라 세법을 적용받는지"만 구분하고 "당신이
  누구인지"(재외국민 vs 한국 거주 외국인)는 구분 못 함 — 특정 페르소나 전용 질문에는
  `faq-audience-badge` 클래스로 별도 배지를 붙임(예: "🌏 해외 거주 한국인 전용").
- **다크모드**: `:root[data-theme="dark"]`, `localStorage.getItem('theme')`,
  `toggleTheme()`/`syncThemeToggleIcon()`(script.js). `--teal`/`--status-red`/`--gold`/`--navy`는
  "다크모드에서 글자색으로 쓰일 때" 기준으로 튜닝되어 있어서, 이 색을 **배경**으로 쓰고 흰
  글자를 얹는 배지/버튼에는 하드코딩된 `color:#fff` 대신 `color:var(--on-accent)`를 쓸 것
  (라이트=흰색, 다크=잉크색으로 자동 전환됨).
- **접근성 폰트 floor**: `--fs-small:16px`가 명시적 최소값(시니어 가독성 연구 근거 주석 있음).
  랜딩페이지들은 자체 CSS 변수를 따로 가지고 있어 메인 앱과 별도로 확인 필요.
- **`COUNTRY_TAX_PROFILES`**: 국가별 세금 프로필. 새 국가 추가 시 여기 + `calcTakeHome()` 세율
  로직 + 국가 지도 좌표(`COUNTRY_MAP_COORDS`/SVG path, flagCode로 매칭) 3곳을 같이 갱신.
  국가 지도(`#countryMapWrap`)는 **"국가별 비교" 탭(view-compare)에만 있음**(홈 탭 아님 —
  테스트 짤 때 실수하기 쉬움, 아래 "테스트 짤 때 흔한 함정" 참고).
- **`formatWon(n)`**: 원화 금액을 언어별로 다르게 표시(억/조 vs million/billion). 비슷한 요구가
  생기면 이 함수의 분기 패턴을 참고.
- **비파괴적 non-breaking space 수정 패턴**: 한국어는 "단어(괄호)"처럼 공백 없이 붙여 쓰는
  경우가 많은데, 좁은 화면에서 이 지점이 그대로 줄바꿈되면 문장이 잘린 것처럼 보임. 고치는
  방법 2가지:
  - 원래 공백이 있어야 자연스러운 자리(예: 라벨과 괄호 사이)는 일반 공백을 NBSP(U+00A0,
    파이썬에서 `' '`로 명시적으로 써야 함 — 터미널에 직접 타이핑하면 전송 과정에서 일반
    공백으로 뭉개지는 경우가 있었음)로 바꿔서 그 지점에서 줄바꿈이 안 일어나게 함.
  - 원래도 공백이 없어야 자연스러운 자리(예: "주(State)에서"의 ")"와 "에서" 사이)는 NBSP로도
    막히지 않음(CJK 스크립트는 거의 모든 글자 경계에서 줄바꿈이 허용되기 때문) —
    `<span style="white-space:nowrap">...</span>`로 통째로 감싸야 함. 이 경우 `.textContent`가
    아니라 `.innerHTML`로 대입해야 마크업이 살아남음.

---

## 테스트 짤 때 흔한 함정 (Playwright로 감사 스크립트 짤 때 참고)

1. **`getComputedStyle(el).display === 'none'`만으로는 조상 요소의 `display:none`을 못 잡음.**
   `display`는 상속되는 속성이 아니라서, 부모가 `display:none`이어도 자식 요소 자신의 계산된
   `display` 값은 그대로 `block` 등으로 나옴. **실제 렌더링 여부는
   `el.getClientRects().length === 0`로 확인할 것** — 조상 체인 어디서 `display:none`이 걸려도
   정확히 잡힘.
2. **어느 탭(view)에 있는지 먼저 확인할 것.** 예: 국가 지도는 `view-compare`에 있지 홈
   (`view-home`)에 없음. 엉뚱한 탭에서 요소를 찾으면 애초에 화면에 없어서
   `getBoundingClientRect()`가 전부 0을 반환하고, "이슈 없음"이 사실은 "아무것도 안 본" 거짓
   통과가 됨.
3. **0건이 나왔다고 끝내지 말고, 일부러 버그를 재현시켜서 탐지되는지 네거티브 컨트롤로
   확인할 것.** (코드를 임시로 한 줄 주석 처리 → 테스트 재실행 → 실패하는지 확인 → 원복)
4. **NBSP(U+00A0) 같은 non-ASCII 문자를 Python/JS 소스에 직접 타이핑하지 말 것.** 도구 호출을
   거치는 과정에서 일반 공백으로 뭉개지는 게 간헐적으로 관찰됨 — `' '`처럼 ASCII escape
   시퀀스로 명시할 것(파이썬은 소스 코드 안에서 이 시퀀스를 안정적으로 해석함).
5. **파일에 쓰는 문자열 치환은 `expected_count`를 지정해서 검증할 것.** 같은 문구가 눈에
   보이는 UI 텍스트뿐 아니라 `<script type="application/ld+json">` SEO 구조화 데이터 블록에도
   중복되어 있는 경우가 많아서(FAQ 스키마 등), 무심코 전체 치환하면 JSON-LD를 깨뜨림. 개수가
   안 맞으면 더 구체적인 앞뒤 문맥으로 앵커를 좁힐 것.

---

## 회귀 테스트 (tests/, 9개)

```bash
python3 -m http.server 9000 &   # 저장소 루트에서
NODE_PATH=/opt/node22/lib/node_modules node tests/home_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/faq_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/audit_odds_compare.js
NODE_PATH=/opt/node22/lib/node_modules node tests/wrap_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/lang_leak_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/console_error_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/map_scroll_audit.js
node tests/i18n_coverage_audit.js   # 브라우저 불필요
node tests/i18n_attr_lint.js        # 브라우저 불필요
node tests/broken_link_audit.js     # 브라우저 불필요
```
Playwright 크로미움 경로: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`
(이미 설치되어 있음, `playwright install` 하면 안 됨). 전부 마지막 줄에 `ISSUES: 0`(또는
`TOTAL ISSUES: 0`)이어야 정상.

| 파일 | 확인하는 것 |
|---|---|
| `home_audit.js` | 여러 뷰포트에서 텍스트가 카드 밖으로 넘치는지 |
| `faq_audit.js` | FAQ 언어·기준 조합 레이아웃 |
| `audit_odds_compare.js` | 확률체감/비교 탭 |
| `wrap_audit.js` | 단어+괄호/%가 공백 없이 붙어있다 그 지점에서 줄바꿈되는 버그 |
| `i18n_coverage_audit.js` | `data-i18n*` 키가 번역 파일에 없거나 일부 언어만 채워진 경우 |
| `i18n_attr_lint.js` | plain `data-i18n`인데 `data-i18n-html`로 바꿔야 할 후보 사전경고 |
| `broken_link_audit.js` | 로컬 href/src 참조 중 실제로 없는 파일 |
| `lang_leak_audit.js` | 다른 언어 선택했는데 한글 3자 이상 연속으로 남아있는 경우(언어 혼용) |
| `console_error_audit.js` | 탭×언어 로드 시 JS 콘솔 에러 |
| `map_scroll_audit.js` | 국가 카드 탭 시 지도가 화면 밖에 남아있는 회귀 |

**예약 자동화(CCR Routine)와의 관계**: 매주 월요일 오후 6시(KST)에 도는
`"참택스 주간 버그·오버플로우 점검"` 루틴(예약 자동화 목록에서 이름으로 찾을 것)이 위 9개 스크립트를
그대로 실행하도록 연결되어 있음(2026-07-23 연결). 새 테스트 스크립트를 추가하면 이 루틴
프롬프트에도 `update_trigger`로 같이 반영할 것 — 안 그러면 세션에서 수동으로만 돌리는 테스트로
남고 주간 자동 점검엔 빠짐. 이 프로젝트의 다른 예약 루틴(잭팟 갱신 체크 2개, 세법 변경 분기별
체크, 월간 세율·환율 재검증)은 각자 다른 목적이니 헷갈리지 말 것 — "루틴에 넣어줘" 같은
모호한 지시가 오면 tests/ 폴더 얘기인지 실제 예약 자동화 얘기인지 먼저 확인할 것(과거에 이걸
헷갈려서 사용자가 스크린샷으로 재확인해야 했던 적 있음).

---

## 알려진 미해결 항목

- **로또 번호 백필 갭**: 메가밀리언즈는 2008년 1월 이전(~2001년대), 파워볼은 1992년 8월~2025년
  5월 구간이 비어있음. 사용자가 usamega.com 데이터를 채팅에 붙여준 적 있지만, 과거 세션에서
  손으로 옮겨 적다가 실제로 다른 연도/게임 데이터를 섞어 넣는 실수를 낸 적이 있어서(발견 즉시
  중단·삭제함) 재확인 없이는 다시 시도하지 않기로 함. **GPT에게 검증을 물어본 결과도 신뢰
  불가로 확인됨**(유명한 연도는 맞았지만 덜 알려진 연도는 완전히 지어낸 걸로 확인) — GPT
  데이터는 검증용으로도 쓰지 말 것. 가장 안전한 방법: 사용자가 `.txt` 파일로 업로드해주면
  스크립트로 기계적으로 파싱(사람이 옮겨 적는 실수 자체를 없앰). 이어서 할 때는 반드시
  요일(메가밀리언즈 화/금, 파워볼 월/수/토)·날짜 간격 자동 검증 스크립트를 만들어서 통과한
  것만 반영할 것.
- **`i18n_attr_lint.js`가 찾은 9개 후보** — plain `data-i18n`인데 한국어 원문에 괄호
  붙여쓰기 패턴이 있어 잠재적으로 줄바꿈 버그가 날 수 있는 곳: `compare.panelDesc`,
  `odds.life1`, `faq.checklistFooter`, `faq.q17`, `faq.ticketMeta`, `privacy.b4`, `privacy.h5`,
  `disclaimer.b4`, `disclaimer.b5`. 지금 테스트한 폭(320~414px)에서는 실제로 안 끊겨서
  급하진 않음.
- **`odds.howtoText`** 등 일부 콘텐츠가 6개 핵심 언어(ko/en/zh/vi/th/ru)만 지원 — 확장하려면
  나머지 17개 언어 번역 추가 필요.
- **랜딩페이지 aria-label**(`LANGUAGE_CONTENT_PAGES`) 약 23개 항목이 영어/한국어 폴백 상태 —
  화면에 보이는 링크 텍스트는 현지화되어 있지만 스크린리더용 aria-label만 미완성. 완전
  현지화하려면 약 400개 문자열 추가 필요, 비용 대비 효과 낮다고 판단해 보류 중.
- **미국 주(State) 이름은 모든 언어에서 영어 그대로** — 의도적 결정(고유명사 번역이 오히려
  혼란을 줄 수 있다고 판단).
- **국가 지도는 실제 국경선 기반**(Natural Earth)이지만 정밀 지도는 아님 — 참고용 시각화
  목적, 정확한 지리 정보로 오인되지 않게 하는 게 의도.

---

## 작업 이력 (날짜순, 세션마다 맨 아래에 새 항목 추가)

### 2026-07-17
- 새 페이지 2개(해외 거주 한국인용 한국어/중국어판) 업로드 누락 발견·정리, 파일명 하이픈→
  언더스코어 변경 정책 확립.
- 탭 제목 언어 고정 버그, `?lang=` URL 파라미터 미제거 버그 수정.
- "해외 거주 한국인" 배너가 언어 무관하게 영어 페이지로만 연결되던 버그 수정.
- FAQ 국민연금 답변에 외국인 캐비엇 추가, "이 돈이면 뭘 살 수 있나" 코너가 언어가 아니라
  세금 기준을 따라가도록 수정.

### 2026-07-21
- 다크모드 수동 토글 전환, 랜딩페이지 37개 다크모드 지원 추가.
- **명암비 버그 발견**: `--teal`/`--navy`/`--status-red`/`--gold`를 배경으로 쓰고 흰 글자를
  얹는 버튼/배지가 다크모드에서 명암비 1.2~2.9:1까지 떨어져 있던 것 — `--on-accent` 시맨틱
  변수 도입해서 13곳 수정.
- 랜딩페이지 42개 최소 폰트 크기 미달 발견·수정(16px 미만 → 16px).
- 필리핀어(Tagalog) 번역이 554개 키 중 72개만 있던 것 발견·전체 채움, pickLang `more` 인자
  누락 51곳 발견·수정.
- 비교 화면 제목이 21개 언어에서 옛 문구("Korea vs. US")로 잘못 남아있던 오역 발견·재번역.
- 확률체감 상금표 분모가 한국어 "만" 단위로 하드코딩되어 모든 언어에 노출되던 언어혼용 버그 수정.
- 번개 번호뽑기 게임화 연출, 국가 지도(장식용 단순 도형 버전) 최초 추가.

### 2026-07-22 (자동 진행분)
- 국가 지도를 실제 지리 데이터(Natural Earth 기반 국경선)로 재디자인.
- 개인정보/면책/문의 화면 120개 조합(다크모드×고대비×큰글씨×10언어×3화면) 감사, 이슈 0.
- 확률 페이지 "역대 최다 번호" 섹션에 당첨공 애니메이션 추가.
- 전체 사이트 22개 언어 × 7화면 언어 혼용 감사(이슈 없음), 저장소 루트 레거시 파일 26개 정리.
- 공유카드가 미입력 기본값(100M USD)을 마치 실제 결과인 것처럼 공유하던 버그 수정.

### 2026-07-22 (후속 세션, `new-session-g8qu5q`)
- 역대 잭팟 확인 기록 + 날짜별 당첨번호 조회 통합.
- 확률 비교 막대그래프 애니메이션이 CSS transition 리셋 트릭 타이밍 문제로 재생 안 되던 버그 →
  `Element.animate()`로 교체.
- 확률체감 탭 전용 대용량 데이터를 `odds-data.js`로 분리, 지연 로드로 초기 로딩 ~10% 절감.
- 메가밀리언즈 과거 데이터 442건(2013-11-08~2018-01-30) 반영, 아카이브 총 1,274건
  (2013-11-08~2026-01-20)으로 확장.

### 2026-07-22 (세 번째 세션, `work-summary-axxlbb` — 현재 작업 브랜치)
- 홈 실수령/세금 비율 막대 최초 진입 애니메이션.
- FAQ에 FTC 서류 체크리스트 + 종합소득세 신고 D-day, 신고 전체 흐름 3단계 요약 추가(22개
  언어 전체 번역).
- FAQ에 "🌏 해외 거주 한국인 전용" 대상 배지 도입 — `data-basis`가 세금 기준만 구분하고
  독자 페르소나(재외국민 vs 한국 거주 외국인)는 구분 못 하던 문제.
- 홈 화면 계산기에 연금(annuity) 30년 지급표 아코디언 추가.
- 잭팟 퀵필 버튼 탭 시 모바일 숫자 키패드가 뜨던 문제(`input.focus()` 제거) 수정.
- **⚠️ 로또 번호 백필 중 신뢰도 문제 발견**: 손으로 옮겨 적다가 다른 연도/게임 데이터를
  섞어 넣는 실수를 스스로 발견·중단·삭제. 이후 요일·날짜간격 자동 검증 스크립트를 만들어
  통과한 것만 반영하는 방식으로 전환. GPT 데이터도 실험적으로 대조해봤는데 덜 알려진
  연도는 완전히 지어낸 것으로 확인되어 이후 사용 중단(자세한 내용은 위 "알려진 미해결
  항목" 참고).
- 메가밀리언즈 아카이브를 2008-08-15~2026-07-21로, 파워볼을 최초 추첨일(1992-04-22)부터
  일부 확장.
- 언어 혼용 재검사 서브에이전트를 두 번 시도했으나 세션 인터럽트로 최종 결과 미수신 —
  2026-07-23 세션에서 `lang_leak_audit.js`로 사실상 대체·재확인됨(0건).

### 2026-07-23 (네 번째 세션)
- 사용자 스크린샷으로 지적된 버그 2건 수정: 날짜 조회 입력칸이 아이폰 사파리에서 테두리
  밖으로 삐져나오던 문제(`-webkit-min-logical-width:0`), "주(State)에서"/"원천징수(30%)가"가
  좁은 화면에서 그 지점 그대로 끊기던 문제.
- 같은 버그 클래스를 홈/비교/확률체감/FAQ 전체에서 스캔 → 28건 추가 발견·수정(그 중 8개는
  plain `data-i18n`이라 `data-i18n-html`로 전환 필요). 21개 외국어 재검사 결과 이 버그는
  한국어에만 있음을 확인(다른 언어는 괄호 앞에 공백을 넣는 게 표준이라 구조적으로 발생 안 함).
- 회귀 테스트 6개 신규 작성(`i18n_coverage_audit.js`/`i18n_attr_lint.js`/
  `broken_link_audit.js`/`lang_leak_audit.js`/`console_error_audit.js`/`map_scroll_audit.js`).
- **"루틴에 넣어달라"는 요청을 처음엔 `tests/` 폴더로 잘못 이해함** — 사용자가 실제 예약
  자동화(CCR Routine) 목록 스크린샷을 보여줘서, "참택스 주간 버그·오버플로우 점검" 루틴
  프롬프트가 위 9개 테스트 스크립트를 실제로 실행하도록 `update_trigger`로 연결(원래는
  Playwright 점검 로직을 매번 자연어로 새로 설명해서 fresh 세션이 재구현하는 방식이었음).
- 테스트 스크립트 자체에서 발견한 함정 2건(조상 요소 `display:none` 미탐지,
  `map_scroll_audit.js`가 엉뚱한 탭을 보고 있었던 것) — 둘 다 위 "테스트 짤 때 흔한 함정"
  섹션에 일반화해서 기록.
- 흩어져있던 인수인계 문서 6개를 이 파일(`HANDOFF.md`) 하나로 통합.

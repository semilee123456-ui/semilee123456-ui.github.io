# 참택스(ChamTax) 프로젝트 — 인수인계 문서 (통합본)

> ## ⚠️⚠️⚠️ 제일 먼저 읽을 것 — 이 저장소는 항상 퍼블릭(공개) 저장소입니다 ⚠️⚠️⚠️
> `semilee123456-ui/semilee123456-ui.github.io`는 누구나 인터넷에서 볼 수 있는 공개 저장소입니다.
> **커밋 메시지, 코드 주석, 이 인수인계 문서를 포함해 이 저장소에 올라가는 모든 글은 전 세계
> 누구나 읽을 수 있다고 가정하고 쓸 것.** 민감 정보·개인 신상·내부용으로만 남기고 싶은 메모
> 등 공개하면 안 되는 내용은 **절대 이 파일이나 커밋에 적지 말고, 대신 사용자에게 채팅으로
> 직접 말할 것** — 조용히 빼놓기만 하지 말고 "이런 내용이 있는데 공개 파일엔 못 적어서 여기
> 따로 말씀드려요"라고 알려줄 것. 자세한 내용은 아래 "새 세션 시작 시 지켜야 할 것" 2~3번 참고.

> ## ⚠️ 이 저장소는 `petscan-ai`와 완전히 무관한 별개 프로젝트입니다
> 사용자 GitHub 계정에는 이 저장소(`semilee123456-ui.github.io`, 참택스/ChamTax) 말고도
> `petscan-ai`(다른 팀이 만드는 특수동물 AI 스캐너 서비스)·`us-lottery-tax-data`·
> `best-personal-finance-tools` 저장소가 있음(2026-08-06 확인). **`petscan-ai`는 이 세션이
> 다루는 프로젝트가 아니고, 이 저장소의 코드/커밋/인수인계와 아무 관련이 없음** — 저장소를
> 헷갈려서 `petscan-ai` 관련 작업을 여기 커밋하거나, 반대로 참택스 작업 맥락을 `petscan-ai`로
> 착각해서 진행하지 말 것. 작업 시작 전 저장소 이름(`semilee123456-ui.github.io`)이 맞는지
> 항상 확인할 것.

이 파일 하나가 **유일한 정식 인수인계 문서**입니다. `handoff-*.md`로 흩어져있던 예전 문서들
(2026-07-17 ~ 2026-07-23)을 이 파일 하나로 합쳤습니다. **새 세션을 시작하면 이 파일 전체를
Claude에게 읽게 하세요.**

**앞으로도 새 `handoff-...-partN.md` 파일을 만들지 말고, 이 파일 안의 "작업 이력" 섹션 맨 아래에
새 세션 항목을 이어서 추가할 것.** 다만 이 섹션이 계속 쌓이기만 하면 파일이 다시 비대해지므로
(2026-07-28에 2,300줄+ 상태에서 한 번 정리한 전례 있음, 아래 "작업 이력" 섹션 참고), **"작업
이력" 섹션에 남아있는 날짜별 세션 항목이 3~4개를 넘어가면, 가장 오래된 항목부터
`HANDOFF-ARCHIVE.md`(저장소 루트, 이 파일과 형제 파일)로 그대로(요약·의역 없이 원문 그대로)
옮기고 이 파일에는 최근 3개 세션 + "이보다 오래된 세션 기록은 `HANDOFF-ARCHIVE.md` 참고" 한
줄 포인터만 남길 것.** 옮기는 작업 자체도 "작업 이력"에 쌓아두지 말고 담담하게 그때그때
수행할 것(별도 세션 항목으로 기록할 필요 없음). "지금 상태" 관련 섹션(프로젝트 개요·아키텍처·
회귀 테스트·알려진 미해결 항목)은 바뀐 내용이 있으면 그때그때 덮어써서 항상 최신 상태를 유지할
것 — 날짜별로 쌓아두지 말고.

---

## 📌 새 세션 시작 시 지켜야 할 것

1. **항상 서브에이전트(Agent tool)로 시작할 것.** 메인 세션에서 탐색·점검·수정을 전부 직접
   하지 말고, 실질적인 작업은 서브에이전트에게 맡겨서 메인 세션의 컨텍스트를 아낄 것.
2. **이 저장소는 공개 저장소임(`semilee123456-ui/semilee123456-ui.github.io`).** 커밋되는
   모든 글(커밋 메시지, 코드 주석, 이 인수인계 문서 포함)은 GitHub에 업로드되면 누구나 볼 수
   있다고 가정하고 작성할 것.
3. **인수인계 문서(이 파일)에 쓰려는 내용 중 공개 저장소에 올리기 부적절한 게 있으면**(민감
   정보, 사용자 개인 신상, 내부용으로만 남기고 싶은 메모 등) — **이 파일에 넣지 말고, 대신
   사용자에게 채팅으로 직접 말할 것.** 조용히 빼놓기만 하지 말고, "이런 내용이 있는데
   공개 파일엔 못 적어서 여기 따로 말씀드려요"라고 알려줄 것.
4. **사이트 파일(HTML/CSS/JS)을 실제로 고쳤으면, 매번 Artifact 도구로 바뀐 페이지(또는 관련
   화면)를 렌더링해서 오른쪽 프리뷰 패널에 띄울 것.** 2026-07-24(열한 번째 세션)에 사용자가
   명시적으로 요청한 습관 — "커밋했다"는 텍스트 보고만으로 끝내지 말고, 매번 눈으로 바로 결과를
   확인할 수 있게 할 것. 로컬 HTML 파일을 그대로 Artifact로 publish하면 되고(외부 리소스 fetch가
   막혀있는 CSP 환경이라 `styles.css`/`script.js` 같은 외부 파일 링크는 인라인으로 합쳐 넣어야
   실제로 렌더링됨 — 원본 파일 자체를 고치지 말고 미리보기용 임시 파일을 스크래치패드에 따로
   만들 것), 텍스트 변경처럼 스크린샷/HTML 렌더링이 의미 없는 작업(예: HANDOFF.md 문서 수정,
   ads.txt 값 변경)까지 억지로 프리뷰를 만들 필요는 없음 — 사용자가 눈으로 확인할 시각적 결과물이
   있는 변경(레이아웃, 신규 UI 요소, 문구 노출 방식 등)에 한해 적용. **2026-07-28 세션에서 이
   습관을 지키지 못함**(Playwright 텍스트 검증만 하고 Artifact 프리뷰는 한 번도 안 띄움) —
   다음 세션은 이 항목을 다시 챙길 것.
5. **세션을 시작하자마자(코드를 고치기 전에) `git fetch origin main && git diff --stat
   origin/main HEAD`로 로컬과 실제 배포본 사이에 차이가 있는지부터 확인할 것.** 2026-07-24
   (일곱 번째 세션)와 2026-07-27(열세 번째 세션) 두 번 다, 이전 세션이 "다 전달했다"고
   기록해놓고 실제로는 사용자가 일부 파일(zip 여러 개 중 하나, 또는 특정 파일)을 GitHub에
   안 올렸거나 못 올린 채로 세션이 끝난 뒤, 다음 세션(또는 같은 세션 후반부)에서 사용자가
   "진짜 반영된 거 맞아?"라고 물어봐서야 뒤늦게 발견됨(연금 슬라이더 HTML이 몇 세션째 안
   올라가 있었던 사례 등). **이전 세션의 이 문서 기록을 "실제로 배포됨"과 동일시하지 말고,
   git으로 직접 검증할 것** — diff가 있으면 그 파일들부터 사용자에게 다시 전달.
6. **이 저장소는 여러 세션이 동시에 작업할 수 있어서, `origin/main`이 세션 진행 중에도
   계속 앞으로 나아갈 수 있음.** 위 5번 항목(세션 시작 시 1회 확인)만으로는 부족함 —
   **매 작업 배치를 시작하기 전이나 PR을 머지하기 직전에도 다시 `git fetch origin main`으로
   확인**하고, `origin/main`이 앞서 있으면 `git merge origin/main`으로 합친 뒤(이 프로젝트는
   지금까지 대부분 자동 병합됐음 — 같은 파일이라도 서로 다른 부분을 고쳤을 땐 충돌 안 남)
   문법 검사(`node --check script.js`)와 핵심 회귀 테스트를 한 번 더 돌리고 나서 다음
   작업을 진행할 것. **로컬에서 오래 들고 있던 브랜치를 그대로 강제 push하면 안 됨** —
   `origin/main`에 이미 있는 독립적인 변경사항(다른 세션이 추가한 기능 등)을 덮어써서
   되돌려버릴 위험이 있음(2026-07-28에 `index.html`의 금액 슬라이더 기능을 옛 zip 업로드
   워크플로 중 이렇게 잃어버릴 뻔했다가 병합으로 막은 전례가 있음 — 지금은 zip 워크플로
   자체가 없어졌지만, "오래된 로컬본을 그대로 덮어쓰지 말 것"이라는 원칙은 여전히 유효함).
7. **`script.js`나 `styles.css` 내용을 고쳤으면, `index.html`이 그 파일을 부르는 캐시버스팅
   버전 쿼리(`?v=YYYYMMDD-N`)도 반드시 같이 올릴 것.** 2026-08-03 세션에서 이걸 빠뜨려서,
   병합 직후 사용자가 확인한 스크린샷에 수정 사항이 전혀 반영 안 된 것처럼 보인 사고가
   있었음 — GitHub Pages 원본에는 이미 새 파일이 있었지만 Cloudflare가 예전 버전 URL의
   응답을 최대 4시간(`cache-control: max-age=14400`)까지 그대로 캐싱하고 있었음(자세한
   진단 경위는 아래 "작업 이력"의 2026-08-03 해당 항목 참고). 버전을 안 올리면 실제 배포가
   아무리 잘 됐어도 방문자는 계속 예전 코드를 받게 됨 — 파일 내용만 고치고 이 단계를
   빠뜨리는 실수를 반복하지 말 것. **2026-08-04부터 index.html이 실제로 부르는 파일이
   `script.js`/`styles.css`가 아니라 `script.min.js`/`styles.min.css`로 바뀌었으니(바로
   아래 8번 항목 참고), 버전 쿼리도 그 두 파일 기준으로 올릴 것.**
8. **⚠️ 2026-08-04부터: `script.js`/`styles.css`를 고쳤으면 커밋 전에 반드시
   `node scripts/build-min.js`를 실행해서 `script.min.js`/`styles.min.css`를 재생성하고
   원본과 함께 커밋할 것 — index.html은 이제 원본이 아니라 이 두 minify된 파일을 불러옴
   (`terser`/`clean-css`로 압축, gzip 대비 script.js 30%·styles.css 66% 추가 절감 확인,
   자세한 배경은 아래 "작업 이력"의 2026-08-04 "사이트 가볍게" 항목 참고). `npm install`이
   아직 안 되어있으면 먼저 한 번 실행(`package.json`의 devDependencies, `node_modules`는
   `.gitignore`에 이미 있음). **이 단계를 로컬에서 빠뜨려도 `.github/workflows/
   minify-assets.yml`이 main에 push될 때 자동으로 재생성·커밋해주는 안전망이 2026-08-04에
   추가됨** — 다만 그 사이(같은 세션에서 Artifact 미리보기로 즉시 확인할 때 등)엔 `.min`
   파일이 구버전으로 남아있으니, 직접 결과를 바로 보려면 여전히 로컬에서 이 스크립트를
   돌릴 것. `i18n-source/translations.json`→`i18n/*.json`(3번 항목 아래 지금까지 계속
   강조된 패턴)과 같은 종류의 실수이니 자동화가 있다고 방심하지 말 것. 원본 파일
   (`script.js`/`styles.css`) 맨 위에도 이 안내가 적혀있음.
9. **⚠️ 토큰을 최대한 아껴 쓸 것 — 2026-08-07 세션이 하루 만에 sitemap.html 26개 언어 번역·
   헤더 재구성 등을 처리하며 PR을 10개 넘게 만드는 등 컨텍스트를 크게 씀, 사용자가 직접
   "토큰 아껴 써달라"고 요청함.** 이 저장소 루트의 `CLAUDE.md`(세션 자체 컨텍스트 절약용
   메모, 이 인수인계 문서와는 별개)를 항상 먼저 읽고 그 지침을 따를 것 — 특히 대량 JSON/번역
   결과를 터미널에 그대로 찍지 말고 파일로 저장, 스크린샷은 폭·언어 조합마다 다 찍지 말고
   대표 몇 개만, 반복되는 로직은 스크립트로 저장해 재사용. **추가로 이번 세션에서 얻은 교훈**:
   (a) "배포 확인만 기록"하는 것처럼 코드 변경이 전혀 없는 순수 문서(HANDOFF.md) 커밋은 매번
   별도 PR로 만들지 말고, 다음 실질적인 코드 변경 커밋이 생길 때 같이 묶어서 PR 하나로
   올릴 것(사용자가 매번 "배포됐는지 확인해줘"라고 명시적으로 요청하는 경우는 예외 — 그때는
   그 요청에 맞춰 바로 확인·기록하되, PR은 여러 개로 쪼개지 말고 다음 배치에 합칠지 그때
   판단할 것). (b) 26개 언어 번역처럼 사람이 다시 볼 필요가 있는 대량 텍스트는 채팅에 전부
   나열하지 말고 파일 하나로 만들어 경로만 안내(제미나이 검수 요청 파일들처럼) — 채팅에
   그대로 쓰면 이후 모든 턴마다 그 내용이 컨텍스트에 계속 남아 누적됨.

---

## Git push·자동 Routine 현재 상태 (요약 — 아래는 전부 지나간 일의 압축본)

- **인터랙티브 세션의 `git push`는 정상 동작함**(2026-07-31부터, Claude GitHub App을 계정
  전체 read/write 권한으로 설치 완료). 그 전까지 있었던 "push 권한이 없어 zip으로 파일을
  사용자에게 전달 → 사용자가 GitHub 웹 UI로 직접 업로드" 우회 워크플로(2026-07-23~07-31)는
  완전히 폐기됨 — 지금은 `git add`→`commit`→`push -u origin <브랜치>`까지 세션이 직접 끝냄.
  이 시기에 실제로 있었던 사고 두 가지만 참고로 남김: (1) 이 인수인계 문서 자체가 커밋 안 된
  채 옛 `handoff-*.md` 조각 파일들만 저장소에 남아있었던 적이 있었음(지금은 이 파일 하나로
  통합되어 정상 커밋됨, 재발 걱정 안 해도 됨). (2) GitHub 웹 "Upload files"로 하이픈(`-`)
  든 파일(예: `odds-data.js`)을 올리면 하이픈이 사라진 별도 파일(`oddsdata.js`)로 잘못
  커밋되는 버그가 있었음(2026-07-23 실제 사고, `odds-data.js` 구버전이 몇 주 방치됨) — 지금은
  웹 업로드를 안 쓰므로 해당 없지만, 혹시 웹 UI로 파일을 올릴 일이 있으면 업로드 후 하이픈
  없는 파일이 새로 안 생겼는지 확인할 것. 새 파일명은 관성적으로 언더스코어(`_`)를 기본으로 씀.
- **예약 실행되는 자동 Routine(트리거)들은 인터랙티브 세션과 별개의 클라우드 환경에서 돌고,
  그 환경은 `git push`가 항상 403으로 막혀있어 `mcp__github__push_files` API로 반영함** —
  각 Routine 프롬프트 자체에 이미 이렇게 명시돼 있음. **2026-08-03에 여기 문서화됐던
  "15개 개별 루틴" 표는 이미 낡았고, 그 이후로도 최소 한 번 더 재구성됨** — `list_triggers`로
  확인한 실제 구성(2026-08-20 기준)은 파워볼/메가밀리언즈 잭팟 갱신 체크 각 1개, 월간
  세율·환율 재검증, 분기별 세법 변경 체크, 주간 버그·오버플로우 점검 등 5개 내외로 예전보다
  단순화돼 있음. **trigger ID·이름·개수·주기는 세션마다 바뀔 수 있으니 이 문서에 표로
  박아두지 말고, 필요할 때마다 `list_triggers`로 실제 상태를 확인할 것.** 공통 원칙은 계속
  유지됨: 명백하고 안전한 문제만 직접 고쳐서 반영까지 끝내고, 애매하면 코드를 건드리지 말고
  이 문서에 발견 사항만 기록.

---

## 프로젝트 개요

- **이름**: 참택스 (ChamTax)
- **내용**: 미국 복권(파워볼·메가밀리언즈) 당첨금의 세후 실수령액 계산기. 세금 계산 기준은
  `COUNTRY_TAX_PROFILES`(script.js) 배열 기준 **42개국**(2026-08-20 확인 — 21개국에서 계속
  늘어나는 중이라 이 숫자도 곧 낡음, 문서 서술 말고 배열의 실제 `code:` 항목 개수를 셀 것).
  미국은 주별 세율까지 지원. 목록에 없는 나라 방문자를 위한 `기타 국가` 옵션도 있음
  (`country === 'other'` 분기, `calcTakeHome()`) — 확정적인 미국 IRS 비거주자 원천징수 30%만
  계산하고 자국 추가세는 "❓ 확인 필요"로 표시(0원 단정 안 함). 이 옵션은 `COUNTRY_TAX_PROFILES`
  배열 안에는 없어서(국가별 비교 탭·지도·잭팟 랭킹처럼 "진짜 나라 비교"가 전제인 기능엔 안
  넣었음) 국가 수를 셀 때 포함시키지 말 것. 홈 화면 세금 기준 토글 + 국가별 비교 탭 지도
  (`#countryMapWrap`) 아래 안내 버튼 + 비교 탭 카드 그리드/잭팟 히스토리 국가별 금액 목록,
  이 세 곳에 "🌐 기타 국가" 카드/칩이 **정렬·그룹핑 로직 밖에서 항상 맨 끝에 고정**으로
  노출됨(순위 경쟁 대상이 아니라서). 지도 자체(핀·국경선)는 실제 국가만 지원 — "기타 국가"는
  좌표가 없어 지도엔 안 올라감.
- 홈 화면 금액 입력 탭(일시불/연금액/희망액) 3개 + 국가별 비교 탭(`#view-compare`)의
  `#amountInput`까지 총 4곳이 공용 통화 선택기(`sharedInputCurrency`)를 공유함 — 현재 32개
  통화 지원(`CURRENCY_RATE_CONFIG`, script.js), 기본값 **KRW**. 선택기 DOM은 두 개
  (`#homeCurrencySelect`/`#compareCurrencySelect`, 서로 다른 화면이라 하나의 `<select>`를
  공유할 수 없음)지만 가리키는 전역 상태는 하나뿐이라 어느 쪽에서 바꾸든 나머지 전부 동시에
  바뀜. 자세한 설계 근거는 아래 "코드 아키텍처 핵심 패턴"의 `sharedInputCurrency` 항목 참고.
- **호스팅**: GitHub Pages, 저장소 `semilee123456-ui/semilee123456-ui.github.io`
- **커스텀 도메인**: `chamtax.com` (Cloudflare Registrar에서 2026-07-23 구매, 원가 등록비
  ~$10.46/yr, 마크업 없음). Cloudflare 네임서버 + A레코드(GitHub Pages IP 4개, "DNS only"/회색
  구름) + `www` CNAME → `semilee123456-ui.github.io`. 저장소 Settings→Pages→Custom domain에
  연결 완료, "DNS check successful" + "Enforce HTTPS" 모두 초록 체크 확인함(2026-07-23).
  기존 `semilee123456-ui.github.io` 주소도 계속 작동하며 새 도메인으로 자동 리다이렉트됨.
- **빌드 도구 없음** — 순수 HTML/CSS/JS (i18n JSON 빌드용 Node 스크립트 하나만 예외)
- **대상**: 한국에 거주하는 40~60대가 주 타겟 — 접근성(최소 폰트 크기, 명암비) 기준이
  명시적으로 있음. 한국 거주 외국인(재외국민이 아닌, 한국에 사는 외국인)도 별도 페르소나로
  지원함 — FAQ 등에서 두 페르소나를 혼동하지 않도록 주의(아래 "대상 배지" 참고).
- **36개 언어 지원**(2026-08-20 확인 — ko(기본, HTML에 직접 한글로 박혀있음) + en/zh/vi/th/ru
  5개(`pickLang`의 위치 인자) + `ADDITIONAL_LANGS`(script.js) 나머지 30개(`pickLang`의 `more`
  객체 인자로 처리). **이 숫자는 계속 늘어나는 중이라 문서 서술을 믿지 말고
  `ADDITIONAL_LANGS.length + 6`으로 실제 개수를 셀 것** — 지난 세션들에서 이 요약 줄이 실제
  언어 수보다 뒤처진 채로 며칠~몇 주씩 방치된 전례가 반복됨). `i18n-source/translations.json`
  (정적 텍스트) + `script.js`의 `pickLang()` 동적 문자열(`more` 객체) 양쪽 다 전체 언어가
  채워져야 완전한 번역 — 새 언어를 추가하면 `tests/i18n_coverage_audit.js`로 누락 여부를
  확인할 것(문서 서술보다 이 테스트 결과가 진실).

### 파일/폴더 구조
```
index.html          메인 SPA (홈/비교/확률체감/FAQ/개인정보/면책/문의 화면이 전부 .view로 존재)
styles.css           전체 스타일 (:root 디자인 토큰, 다크모드는 :root[data-theme="dark"])
script.js            전체 로직 (세금 계산 + 렌더링 + i18n — 매우 큰 파일, 1MB+)
odds-data.js         확률체감 탭 전용 대용량 데이터(역대 당첨번호·잭팟 아카이브), 그 탭 열 때만 지연 로드
manifest.json        PWA 매니페스트
sw.js                PWA 서비스 워커(2026-07-24 신규, index.html에서만 등록) — 네트워크 우선,
                     앱 셸만 캐싱, 자세한 설계 이유는 파일 상단 주석 + 아래 "작업 이력" 참고
sitemap.xml          SEO 사이트맵 — 새 페이지 만들 때마다 같이 갱신할 것(안 하면 검색엔진 색인이
                     늦어짐), `tests/broken_link_audit.js`로 누락·깨진 링크 확인 가능
robots.txt           크롤링 허용 + sitemap 위치 명시, 정상

i18n/                언어별 번역 JSON(ko 제외 나머지 언어 수만큼, 2026-08-20 기준 35개) —
                     빌드 산출물, 직접 고치지 말 것
i18n-source/translations.json   번역 원본(source of truth) — 여기를 수정
scripts/build-i18n.js           translations.json → i18n/*.json 생성 스크립트

tests/               회귀 테스트 스크립트(2026-08-20 기준 16개, 아래 "회귀 테스트" 섹션 참고 —
                     계속 늘어나는 중이라 정확한 개수는 `ls tests/*.js | wc -l`로 확인할 것)

*.html (그 외, 2026-08-20 기준 143개)   언어별/국가별 SEO 랜딩 페이지 — 메인 계산기와 별개,
                     각자 자체 <style> 안에 자체 :root 색상/폰트 변수를 중복 정의(반복적 버그
                     원인이었음 — 새 디자인 토큰을 바꿀 때 이 페이지들도 같이 확인할 것). 국가
                     수(42개국)를 따라 계속 늘어나는 중이라 정확한 개수는 `ls *.html | wc -l`로
                     확인할 것. title/description/hreflang/JSON-LD는 여러 차례 SEO 감사를
                     통과했음(자세한 경위는 필요할 때 "작업 이력"/`HANDOFF-ARCHIVE.md` 검색).
```

### 다국어(i18n) 작업 방법
1. `i18n-source/translations.json`에서 해당 키의 번역을 수정 (**`i18n/*.json`을 직접 고치지
   말 것** — 다음 빌드 때 덮어써짐). 큰 JSON 파일 전체를 `json.dump()`로 재직렬화하면 기존의
   수작업 포맷팅이 깨져서 거대한 diff가 남으므로, 바뀌는 부분만 타겟팅한 문자열 치환으로 수정할 것.
2. `node scripts/build-i18n.js` 실행 → `i18n/*.json`(ko 제외 언어 수만큼, 계속 늘어나는 중) 재생성
3. HTML 정적 텍스트: `data-i18n`/`data-i18n-html`/`data-i18n-placeholder`/`data-i18n-aria-label`
   속성 + `resolveI18n(key)`가 `i18n/{lang}.json`을 fetch해서 치환. **주의**: plain `data-i18n`은
   언어 전환마다 `el.textContent`로 통째로 덮어써지므로, 원문 안에 `<span>` 같은 마크업을 넣어도
   다음 전환 때 사라짐 — 마크업이 필요하면 `data-i18n-html`을 쓸 것(`tests/i18n_attr_lint.js`가
   이 오사용을 사전 경고함).
4. JS 동적 문자열: `pickLang(ko, en, zh, vi, th, ru, more)` — 6개는 위치 인자, 나머지는
   `more` 객체(`ADDITIONAL_LANGS`, 2026-08-20 기준 30개). **`more`를 빠뜨리면 그 언어들에서
   영어로 조용히 폴백됨** — 과거 세션에서 발견된 버그의 상당수가 이 실수였음.
5. **새 `data-i18n*` 키를 추가했으면 반드시 `tests/i18n_coverage_audit.js`를 돌려서 전체 언어에
   번역이 채워졌는지 확인할 것.** 2026-07-23에 FTC 체크리스트 관련 키 10개가 번역 누락 상태로
   몇 세션째 라이브에 떠 있던 걸 이 테스트를 처음 만들면서 발견함(외국어 페이지에서 그 부분만
   한국어 원문이 그대로 노출되고 있었음) — 새 기능 추가 시 "번역 다 했다"고 적어놓고 실제로는
   빠뜨리는 실수가 반복되니, 서술이 아니라 이 테스트 통과 여부로 확인할 것.

---

## 코드 아키텍처 핵심 패턴

- **`script.min.js`/`styles.min.css`(2026-08-04 신규)**: `index.html`이 실제로 로드하는 건
  `script.js`/`styles.css`가 아니라 이 압축본 두 파일임(`i18n/*.json`이 `i18n-source/
  translations.json`의 생성 파일인 것과 동일한 패턴). **`script.js`/`styles.css`를 고치면
  `node scripts/build-min.js`로 압축본을 다시 만들어야 화면에 반영됨** — 로컬에서 깜빡해도
  `.github/workflows/minify-assets.yml`이 main에 push될 때(`script.js`/`styles.css` 변경
  감지) 자동으로 재생성·커밋해주므로 라이브에는 결국 반영되지만, 같은 세션에서 Artifact
  미리보기 등으로 즉시 확인하려면 직접 돌려야 함. `sw.js`의 `APP_SHELL_PATHS`도 `.min` 파일
  경로를 캐싱하므로, 이 두 파일의 경로 자체를 바꿀 일이 있으면 `sw.js`도 같이 갱신할 것
  (놓치면 서비스 워커 오프라인 폴백이 구버전 또는 존재하지 않는 파일을 참조하게 됨). gzip
  대비 추가 절감 효과: script.js 30%(573KB→401KB), styles.css 66%(62KB→21KB) — 자세한 배경은
  아래 "작업 이력"의 2026-08-04 해당 항목 참고.
- **`sharedCountry`**: 'kr'/'us'/'cn'/'in'/... — 사용자가 고른 "세금 계산 기준". 홈 화면과
  국가비교 화면이 이 값을 공유함. `data-basis="kr"` 같은 속성으로 FAQ 항목을 세금 기준별로도
  필터링함(`filterFaq()`).
- **`LANG_TO_COUNTRY`는 더 이상 존재하지 않음(2026-08-01 도입 → 2026-08-03 제거)**: 한때
  언어 전환 시 `sharedCountry`(세금 계산 기준)를 자동으로 맞춰주는 기능이 있었으나, 브라우저
  언어가 실제 거주국과 무관한 경우가 흔해서(예: 한국 거주자가 영어 Windows 사용) 사용자가
  "홈페이지가 한국에서 외국중심으로 됐다"고 지적해 제거함(자세한 경위는 아래 "작업 이력"의
  2026-08-03 해당 항목 참고). **지금은 `sharedCountry`가 언어와 무관하게 항상 'kr' 기본값을
  유지하고, 사용자가 직접 고르거나(`#homeCountryToggle`, `?country=` URL 파라미터 등) 명시적
  신호가 있을 때만 바뀜.** `LANG_TO_CURRENCY`(통화 자동감지)는 표시 단위만 바꿀 뿐 세금 계산
  자체에는 영향 없어 리스크가 낮다고 판단해 계속 유지 중 — "언어 기반 자동 전환을 다시
  추가해달라"는 요청이 오면, 화면 표시(언어/통화)와 세금 계산 기준(국가)을 같은 리스크로
  묶지 말 것.
- **`currentLang`**: 'ko'/'en'/... — 표시 언어. localStorage에 저장 안 되고 새로고침하면
  'ko'로 리셋됨(`?lang=` URL 파라미터로 시작은 가능, 적용 후 URL에서 자동 제거).
- **`sharedInputCurrency`**(2026-07-28 신규, 같은 날 후속 세션에서 국가별 비교 탭까지 확장):
  'KRW'/'USD'/'CNY'/... — 홈 화면 금액 입력 탭(일시불/연금액/희망액) 3개 **+ 국가별 비교 탭
  (`#view-compare`)의 `#amountInput` 1개**, 총 4개 입력칸이 공유하는 "표시 통화". 선택기
  DOM은 두 개(`#homeCurrencySelect`는 홈 3탭 위, `#compareCurrencySelect`는 비교 탭
  `#amountInput` 위 — 서로 다른 화면(view)에 있어서 하나의 `<select>`를 공유할 수 없음, 대신
  `setSharedInputCurrency()`가 호출될 때마다 존재하는 쪽 selector의 `.value`를 서로 맞춰줌)지만
  가리키는 전역 상태(`sharedInputCurrency`)는 하나뿐이라 어느 쪽에서 바꾸든 나머지 전부 동시에
  바뀜 — `sharedAmountUsd`/`sharedCountry`가 홈↔비교 탭 사이에 이미 공유되는 것과 같은 원칙을
  그대로 확장한 것(비교 탭을 별도 상태로 분리하지 않기로 한 게 이 후속 세션의 핵심 판단, 아래
  작업 이력 참고). 기본값 **KRW**(과거엔 일시불/연금액이 항상 "Million USD"였음 — 이 기본값
  변경이 최초 세션의 핵심 판단, 아래 작업 이력 참고). 내부 계산·슬라이더 값(`sharedAmountUsd`,
  `homeAmountSlider`/`compareAmountSlider`의 `data-usd-min/max`·로그스케일 위치)은 **항상 USD
  기준으로 그대로 유지**되고, `sharedInputCurrency`는 순수히 "입력칸에 타이핑/표시되는 단위"만
  바꾸는 표시 레이어임 — `usdMillionsToInputUnits()`/`inputUnitsToUsdMillions()`가 이 변환을
  전담(KRW는 "억원" 단위, 그 외는 "Million <코드>" 단위). 통화 전환 시 재표시는 슬라이더의
  반올림된 위치가 아니라 `sharedAmountUsd`/`sharedAnnouncedUsdMillions`(정확한 원본)에서 다시
  계산해서, KRW↔다른 통화를 여러 번 왔다갔다 해도 드리프트가 안 생기게 함. 통화별
  symbol/flagEmoji/locale/실시간환율 메타데이터는 `CURRENCY_DISPLAY_META`(script.js,
  `CURRENCY_RATE_CONFIG` 근처)에 모아둠. **`#sideByCountryGrid`(국가별 비교 카드) — 순위(정렬·
  동일금액 그룹핑)는 여전히 항상 `result.final`(KRW-억 단위 실값) 기준으로만 정해지고, 표시
  통화가 바뀌어도 절대 안 바뀜(의도된 설계, `updateSideBySide()` 참고). 다만 화면에 보이는 금액
  텍스트 자체는 2026-07-28(서른 번째 세션)부터 표시 통화(`sharedInputCurrency`)를 따름** — 예전엔
  카드가 항상 `formatWon()`(KRW 고정)이었는데, 같은 세션이 이미 검증된 `formatEokKrwInDisplayCurrency()`
  (희망액 탭 미리보기가 쓰던 헬퍼, KRW면 `formatWon()` 그대로·그 외 통화는 `formatCompactCurrencyAmount()`로
  압축 표기)를 그대로 재사용해서 실제 통화 변환이 되도록 고침("정렬은 KRW 실값, 표시만 통화별"이라는
  원칙은 입력칸 레이어와 동일). 이전에 있었던 "카드는 항상 원화 기준"이라는 안내 캡션
  (`#compareGridCurrencyNote`)은 이제 사실이 아니게 되어 제거함.
  **2026-08-01: 홈 탭 히어로(`#home-final-amt`, 결과 화면에서 제일 큰 숫자)는 이 통화 전환에서
  빠져있던 걸 사용자가 폰 스크린샷으로 직접 지적해서 발견함** — `formatWon()`이 `'₩ ' + ...`로
  통화 기호를 하드코딩하고 있어서, 비교 탭 카드는 이미 통화별로 바뀌는데 정작 홈 화면 결과는
  베트남어+VND로 봐도 항상 원화로만 보이는 상태였음. `animateValueChange()`(카운트업 애니메이션)와
  `updateHomeCalc()`의 `home-final-amt`/`tax-impact-before`/`tax-impact-after`/`tax-impact-diff`를
  전부 `formatEokKrwInDisplayCurrency()`로 통일해서 해결(KRW는 완전히 동일하게 동작, 회귀 없음).
  사용자가 "전부 다 변경해줘"라고 명시적으로 요청해서, 잭팟 히스토리(`jh-amt`)/물가반영
  랭킹·최장 이월 기록(`jh-rank-amt`)/확률체감 탭 잭팟 계산기(`jc-jackpot`/`jc-cash`/`jc-final`,
  주의: 이 위젯은 `calcTakeHome(..., 'kr')`로 항상 한국 기준 계산이 고정이지만, **표시 통화는
  이제 이 계산 기준과 별개로 사용자 선택을 따름** — "한국 기준으로 계산한 값을 원하는 통화로
  보여준다"는 개념)/연금 30년 스케줄(`annuity-schedule-net`)/재미로 보기(`fun-summary`)/미국
  주별 비교표(`us-state-row-amt`)까지 `formatWon()` 하드코딩 총 23곳을 전부 같은 헬퍼로 전환함.
  **의도적으로 그대로 둔 곳(회귀 아님)**: `#home-krw-amt`(입력칸 밑 "≈ X억원" 참고 환산 줄)와
  `#compare-krw-amt`(비교 탭의 같은 역할 줄)는 표시 통화가 이미 KRW가 아닐 때만 보이는 "원화
  환산 참고용" 힌트라 의도적으로 KRW 고정 유지 — 히어로(주역)는 통화별, 이 힌트(보조)는 항상
  원화라는 원칙. **부수적으로 발견한 문구 버그**: "실제로는 달러로 받고 원화는 한국 기준
  비교용"이라던 `home-usd-note`/`trustLine` 안내 문구가, 히어로가 더 이상 원화 고정이 아니게
  되면서 "원화(KRW)"를 콕 집어 말하는 게 화면에 실제 보이는 숫자와 안 맞게 됨 — "화면 금액은
  참고용 환산"이라는 통화 무관 표현으로 26개 언어 전부 일반화(`faq.a19ru`처럼 `translations.json`
  키가 아니라 `script.js` 안의 `pickLang()` 인라인 문자열이라 두 곳 다 직접 찾아 고쳐야 했음).
- **FAQ 대상 배지**: `data-basis`가 "어느 나라 세법을 적용받는지"만 구분하고 "당신이
  누구인지"(재외국민 vs 한국 거주 외국인)는 구분 못 함 — 특정 페르소나 전용 질문에는
  `faq-audience-badge` 클래스로 별도 배지를 붙임(예: "🌏 해외 거주 한국인 전용").
- **다크모드**: `:root[data-theme="dark"]`, `localStorage.getItem('theme')`,
  `toggleTheme()`/`syncThemeToggleIcon()`(script.js). `--teal`/`--status-red`/`--gold`/`--navy`는
  "다크모드에서 글자색으로 쓰일 때" 기준으로 튜닝되어 있어서, 이 색을 **배경**으로 쓰고 흰
  글자를 얹는 배지/버튼에는 하드코딩된 `color:#fff` 대신 `color:var(--on-accent)`를 쓸 것
  (라이트=흰색, 다크=잉크색으로 자동 전환됨).
- **접근성 폰트 floor**: `--fs-small:16px`가 명시적 최소값(시니어 가독성 연구 근거 주석 있음).
  랜딩페이지들은 자체 CSS 변수를 따로 가지고 있어 메인 앱과 별도로 확인 필요.
- **`COUNTRY_TAX_PROFILES`**: 국가별 세금 프로필. 새 국가 추가 시 여기 + `calcTakeHome()` 세율
  로직 + 국가 지도 좌표(`COUNTRY_MAP_COORDS`/SVG path, flagCode로 매칭) 3곳을 같이 갱신.
  국가 지도(`#countryMapWrap`)는 **"국가별 비교" 탭(view-compare)에만 있음**(홈 탭 아님 —
  테스트 짤 때 실수하기 쉬움, 아래 "테스트 짤 때 흔한 함정" 참고). **`detailPage`/`detailLabel`
  필드(선택 사항)**: 해당 국가 전용 상세 랜딩페이지(`{country}-resident-us-lottery-tax.html`)로
  가는 링크를 국가별 비교 카드에 자동으로 붙여줌(`updateSideBySide()`가 `rows.length === 1`일
  때만, 즉 다른 나라와 실수령액이 같아서 그룹으로 묶이지 않았을 때만 노출 — 세율이 미국
  원천징수 30%보다 낮은 나라는 자주 그룹으로 묶여서 이 링크가 안 보일 수 있음, 정상 동작).
  2026-08-01 이전엔 `cn`/`kr` 2개국만 이 필드가 있었는데(26개 "한국에 사는 OO 국적자" 랜딩
  페이지와는 완전히 다른 카테고리 — 이건 "그 나라에 실제 거주하는 사람"용, 자국의 진짜 세법을
  다룸), 2026-08-01 세션이 나머지 18개국(us 제외 — 영어 방문자는 메인 계산기가 이미 완전히
  커버해서 언어 격차가 없다고 판단해 스킵)에 같은 필드를 채우고 대응 페이지를 새로 만듦(아래
  "작업 이력" 참고). 새 국가를 이 방식으로 추가할 땐 렌더링 코드를 안 건드려도 됨(이미 범용
  메커니즘) — `COUNTRY_TAX_PROFILES` 항목에 두 필드만 추가하고 페이지 파일만 만들면 됨.
- **`formatWon(n)`**: 원화 금액을 언어별로 다르게 표시(억/조 vs million/billion). 비슷한 요구가
  생기면 이 함수의 분기 패턴을 참고.
- **`highlightCountryOnMap()`**(script.js): 국가별 비교 탭에서 카드를 탭하면 지도가 스크롤 밖에
  있을 때만 `scrollIntoView({block:'nearest'})`로 보이게 함. 이 스크롤 로직을 건드릴 일이 있으면
  `tests/map_scroll_audit.js`로 회귀 확인할 것.
- **비파괴적 non-breaking space 수정 패턴**: 한국어는 "단어(괄호)"처럼 공백 없이 붙여 쓰는
  경우가 많은데, 좁은 화면에서 이 지점이 그대로 줄바꿈되면 문장이 잘린 것처럼 보임. 고치는
  방법 2가지:
  - 원래 공백이 있어야 자연스러운 자리(예: 라벨과 괄호 사이)는 일반 공백을 NBSP(U+00A0,
    파이썬에서 `'\xa0'`로 명시적으로 써야 함 — 터미널에 직접 타이핑하면 전송 과정에서 일반
    공백으로 뭉개지는 경우가 있었음)로 바꿔서 그 지점에서 줄바꿈이 안 일어나게 함.
  - 원래도 공백이 없어야 자연스러운 자리(예: "주(State)에서"의 ")"와 "에서" 사이)는 NBSP로도
    막히지 않음(CJK 스크립트는 거의 모든 글자 경계에서 줄바꿈이 허용되기 때문) —
    `<span style="white-space:nowrap">...</span>`로 통째로 감싸야 함. 이 경우 `.textContent`가
    아니라 `.innerHTML`로 대입해야 마크업이 살아남음.
  - **주의(2026-07-23 발견)**: `display:flex; justify-content:space-between`인 컨테이너
    (예: `.calc-detail-summary`) 안에 이 nowrap `<span>`을 넣으면, 원래 텍스트가 "텍스트노드 +
    span + 텍스트노드" 3개의 flex item으로 쪼개져서 space-between이 그 사이 간격을 화면
    전체로 벌려버리는 별개의 버그가 생김(줄바꿈 문제가 아니라 flex 레이아웃 문제). 이런
    컨테이너에서는 nowrap span을 추가할 때 전체 텍스트를 감싸는 바깥 `<span>` 하나를 더 씌워서
    flex item 개수를 1개로 유지할 것.

---

## 테스트 짤 때 흔한 함정 (Playwright로 감사 스크립트 짤 때 참고)

1. **`getComputedStyle(el).display === 'none'`만으로는 조상 요소의 `display:none`을 못 잡음.**
   `display`는 상속되는 속성이 아니라서, 부모가 `display:none`이어도 자식 요소 자신의 계산된
   `display` 값은 그대로 `block` 등으로 나옴. **실제 렌더링 여부는
   `el.getClientRects().length === 0`로 확인할 것** — 조상 체인 어디서 `display:none`이 걸려도
   정확히 잡힘.
2. **어느 탭(view)에 있는지 먼저 확인할 것.** 예: 국가 지도는 `view-compare`에 있지 홈
   (`view-home`)에 없음. 엉뚱한 탭에서 요소를 찾으면 애초에 화면에 없어서
   `getBoundingClientRect()`가 전부 0을 반환하고, "이슈 없음"이 사실은 "아무것도 안 본" 거짓
   통과가 됨.
3. **0건이 나왔다고 끝내지 말고, 일부러 버그를 재현시켜서 탐지되는지 네거티브 컨트롤로
   확인할 것.** (코드를 임시로 한 줄 주석 처리 → 테스트 재실행 → 실패하는지 확인 → 원복). 이
   원칙 자체로 2026-07-23에 진짜 버그(FTC 번역 누락)를 하나 찾아냈음 — 테스트를 새로 만드는
   행위 자체가 버그 탐지로 이어진 사례.
4. **NBSP(U+00A0) 같은 non-ASCII 문자를 Python/JS 소스에 직접 타이핑하지 말 것.** 도구 호출을
   거치는 과정에서 일반 공백으로 뭉개지는 게 간헐적으로 관찰됨 — `'\xa0'`처럼 ASCII escape
   시퀀스로 명시할 것(파이썬은 소스 코드 안에서 이 시퀀스를 안정적으로 해석함).
5. **파일에 쓰는 문자열 치환은 `expected_count`를 지정해서 검증할 것.** 같은 문구가 눈에
   보이는 UI 텍스트뿐 아니라 `<script type="application/ld+json">` SEO 구조화 데이터 블록에도
   중복되어 있는 경우가 많아서(FAQ 스키마 등), 무심코 전체 치환하면 JSON-LD를 깨뜨림. 개수가
   안 맞으면 더 구체적인 앞뒤 문맥으로 앵커를 좁힐 것.
6. **Playwright 브라우저 컨텍스트의 기본 언어는 'ko'가 아닐 수 있음.** locale 설정 없이
   그냥 `page.goto()`만 하면 사이트가 영어로 뜨는 경우가 있었음 — 한국어 기준으로 테스트하려면
   `newPage({ locale: 'ko-KR' })` + `?lang=ko` 둘 다 명시할 것.
7. **이 실행 환경은 외부 인터넷이 막혀있어서(폰트 CDN·광고·환율 API 등) "Failed to load
   resource" 콘솔 에러가 항상 남.** `console_error_audit.js`는 이런 외부 리소스 실패를
   필터링하고 로컬(127.0.0.1) 리소스 실패나 진짜 JS 예외만 잡도록 만들어져 있음 — 비슷한
   테스트를 새로 만들 때 이 필터링을 빠뜨리면 매번 40개 안팎의 가짜 이슈가 뜬다.

---

## 회귀 테스트 (tests/, 2026-08-20 기준 16개 — 계속 늘어나는 중, `ls tests/*.js | wc -l`로 확인할 것)

```bash
python3 -m http.server 9000 &   # 저장소 루트에서
NODE_PATH=/opt/node22/lib/node_modules node tests/home_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/faq_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/audit_odds_compare.js
NODE_PATH=/opt/node22/lib/node_modules node tests/wrap_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/lang_leak_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/console_error_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/map_scroll_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/nav_slider_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/link_navigation_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/full_overflow_sweep.js   # 느림(전체 언어×폭×화면), 타임아웃 넉넉히
node tests/i18n_coverage_audit.js         # 브라우저 불필요
node tests/i18n_attr_lint.js              # 브라우저 불필요
node tests/broken_link_audit.js           # 브라우저 불필요
node tests/draw_archive_integrity_check.js   # 브라우저 불필요, 로또 아카이브 날짜순·중복·형식 검증
node tests/fact_consistency_audit.js         # 브라우저 불필요
node tests/drift_consistency_test.js         # 브라우저 불필요, 아래 참고
```
Playwright 크로미움 경로: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`
(이미 설치되어 있음, `playwright install` 하면 안 됨). 전부 마지막 줄에 `ISSUES: 0`(또는
`TOTAL ISSUES: 0`)이어야 정상.

| 파일 | 확인하는 것 |
|---|---|
| `home_audit.js` | 여러 뷰포트에서 텍스트가 카드 밖으로 넘치는지 |
| `faq_audit.js` | FAQ 언어·기준 조합 레이아웃 |
| `audit_odds_compare.js` | 확률체감/비교 탭 |
| `wrap_audit.js` | 실제 렌더링 기준으로, 단어+괄호/%가 공백 없이 붙어있다 그 지점에서 줄이 갈리는지 Range.getClientRects()로 확인 |
| `i18n_coverage_audit.js` | `data-i18n*` 키가 `i18n-source/translations.json`에 없거나 일부 언어만 채워졌는지, 빌드 산출물(`i18n/*.json`)에도 반영됐는지 |
| `i18n_attr_lint.js` | plain `data-i18n`인데 한글 원문에 괄호/%가 붙어있어 `data-i18n-html` 전환이 필요할 수 있는 후보 사전경고(버그 확정 아님, 0건이 정상) |
| `broken_link_audit.js` | 로컬 href/src 참조 중 실제로 없는 파일 |
| `lang_leak_audit.js` | 다른 언어 선택했는데 한글 3자 이상 연속으로 남아있는 경우(언어 혼용) |
| `console_error_audit.js` | 탭×언어 로드 시 JS 콘솔 에러(외부 리소스 실패는 필터링됨) |
| `map_scroll_audit.js` | 국가 카드 탭 시 지도가 화면 밖에 남아있는 회귀 |
| `nav_slider_audit.js` | nav 원형 설정 버튼이 pill 3개와 같은 줄에 있는지, 금액 슬라이더(로그 스케일)가 단조 증가하는지·주요 배수에 도달 가능한지·타겟값 근처에서 흔들리지 않는지 |
| `draw_archive_integrity_check.js` | 파워볼·메가밀리언즈 당첨번호 아카이브의 날짜순 정렬·중복·형식 오류 (`minCount`/`maxFirstDate` 기준값은 백필 진행할 때마다 갱신할 것) |
| `fact_consistency_audit.js` | 일시불 비율(45~60%)·미국 연방세/한국 종합소득세 최고구간(37%/45%)처럼 여러 파일에 중복 박혀있는 사실이 서로 어긋나지 않는지. 세율이 실제로 바뀌면 스크립트 상단 주석의 "다른 사실" 목록도 같이 검토할 것 |
| `drift_consistency_test.js` | `script.js`의 `TAX_MODEL`(국가별 세율)과 각 랜딩페이지 본문 서술이 어긋나는지(script.js가 정정된 뒤 랜딩페이지 반영이 몇 주 늦었던 사례를 계기로 추가) — 계산기가 쓰는 핵심 세율 숫자가 페이지에 최소 한 번은 언급되는지만 보는 저비용 스모크 테스트 |
| `full_overflow_sweep.js` | 지원하는 전체 언어 × 대표 화면 폭 5개 × 주요 화면 전수 조합에서 실제 렌더링된 박스가 부모/화면 경계를 벗어나는 오버플로우가 있는지(`home_audit`/`wrap_audit`/`audit_odds_compare`는 ko/en 위주만 다뤄서 생긴 사각지대) |
| `link_navigation_audit.js` | `index.html?amount=...`류 쿼리스트링 딥링크가 기대한 화면/상태로 실제로 떨어지는지(href가 유효해도 런타임에 엉뚱한 탭으로 튕기는 버그 클래스는 `broken_link_audit.js`가 못 잡음) |

---

## 알려진 미해결 항목

- **⏰ index.html title/description의 "8/12 파워볼 $1.04B 당첨" 임시 SEO 문구(2026-08-14 추가,
  2026-08-20 기준 여전히 라이브) — 유효기간 지난 시의성 문구, 다음에 손대는 세션이 빼거나
  최신 사건으로 교체할 것.** `<title>` 바로 위 HTML 주석 참고. `odds-data.js`의
  `JACKPOT_HISTORY` 해당 항목(noteKo/noteEn 등)에 넣어둔 "미청구" 상태 메모도 같이 정리할 것
  (청구기한 1년짜리라 당첨자가 나타나거나 기한이 만료되면 사실이 바뀜). **2026-08-20 확인:
  이 항목 처리를 위해 예약해뒀다는 2026-09-04 셀프 리마인더가 `list_triggers`로 찾아지지
  않음**(트리거가 묶인 세션이 만료되면 조용히 사라지는 알려진 실패 모드 — 위 "Git push·자동
  Routine 현재 상태" 참고) — 리마인더가 올 거라고 믿지 말고, 이 항목을 보는 세션이 직접 처리할 것.
- **새 당첨 회차가 나올 때마다 반드시 확인할 3곳(자동 Routine이 이미 처리하지만, 수동으로
  다룰 때를 위한 체크리스트)**:
  1. `LATEST_DRAW.powerball`/`LATEST_DRAW.megamillions`(date/numbers/special) — 홈 화면
     "최근 당첨번호" 위젯이 읽는 값. `odds-data.js`의 `*_DRAW_ARCHIVE`/`*_JACKPOT_ARCHIVE`와
     **완전히 별개인 손 관리 값**이라, 아카이브를 갱신해도 이게 따로 누락될 수 있음(과거
     반복된 사고 패턴).
  2. `JACKPOT_DATA.powerball`/`JACKPOT_DATA.megamillions`의 `amountUsd`(다음 추첨 광고
     잭팟액)·`cashUsd`(현금가치, 공식 사이트가 발표 안 하면 비워두면 `CASH_VALUE_RATIO` 58%
     추정치로 자동 대체됨 — 추측해서 채우지 말 것). **`JACKPOT_DATA` 이 두 숫자만 고치면
     홈 카운트업 카드/퀵필 버튼/잭팟 카드/세후계산 드로어 4곳이 `getJackpotKRW()`/
     `getJackpotCashUsd()` 경유로 자동 반영됨** — 화면별로 따로 안 고쳐도 됨.
  3. `odds-data.js`의 `POWERBALL_DRAW_ARCHIVE`/`POWERBALL_JACKPOT_ARCHIVE`(메가밀리언즈도
     같은 패턴) — 이월 스트릭/실수령액 랭킹 위젯이 읽는 원본. `node tests/
     draw_archive_integrity_check.js`의 `last date` 출력으로 최신 회차 반영 여부 확인 가능.
     갱신했으면 `odds-data.js?v=...` 캐시버스팅 버전도 같이 올릴 것. 물가보정(CPI) 랭킹만
     큐레이션 목록(`JACKPOT_HISTORY`)을 써서 새 회차가 자동 반영 안 되는데, 이건 의도된
     설계(평범한 회차는 이 랭킹 대상이 아님).
- **`CPI_BASE_YEAR`(현재 2025, `script.js`)** — 2025년 10월 지수가 정부 셧다운으로 결측이라
  11개월 평균으로 확정한 값. **2026년 연평균이 확정되면 `CPI_BASE_YEAR=2026`으로 갱신할 것**
  (그 전까지는 되돌리지 말 것 — 2025년 10월 결측은 2025년 한정 이슈).
- **"문의하기" 기능은 삭제하지 않기로 최종 결정함(2026-07-23)** — 개인정보처리방침의 "개인정보
  보호책임자 연락처" 조항이 법적으로 연락 수단을 요구해서 지금 상태(Formspree 폼) 유지로
  결론남. 다시 꺼내지 말 것.
- **국가 지도는 실제 국경선 기반(Natural Earth)이지만 정밀 지도는 아님** — 참고용 시각화가
  의도, 정확한 지리 정보로 오인되지 않게 할 것.
- **SEO 프리렌더링(SSG) 리팩터는 검토했다가 실측 근거로 불필요 확정됨(2026-08-05~06) — 다시
  꺼내려면 재추측 말고 먼저 GSC "URL 검사"→"실시간 테스트"→"테스트된 페이지 보기"로
  `chamtax.com/?lang=en` 같은 URL이 실제로 그 언어로 렌더링되는지부터 확인할 것.** 배경:
  "index.html이 크롤러에게 항상 한국어로만 보인다"는 우려로 언어별 정적 프리렌더링 설계안까지
  검토했으나, 외부 AI 교차검수에서 "구글봇 WRS는 CSR 텍스트도 정상 색인하므로 이 진단 자체가
  검증 안 된 가정일 수 있다"는 반박을 받음 → 사용자가 GSC 실시간 테스트로 `?lang=en`이 실제로
  영어(nav/title/헤드라인)로 정상 렌더링되는 것을 스크린샷으로 확인 → 우려가 틀렸음이 실측으로
  확정됨. (주의: "크롤링된 페이지 보기"는 JS 실행 전 원본 소스라 이 판단에 안 맞음 — 반드시
  "테스트된 페이지 보기"를 쓸 것.) 프리렌더링은 89개 이상의 기존 랜딩페이지와 키워드
  자기잠식 위험·빌드 파이프라인 부채 증가라는 비용도 있어 실익보다 리스크가 크다고 결론남.
- **랜딩페이지 SEO 감사(2026-07-23~08-05에 여러 차례 진행) — 고아 페이지 0건, title/
  description/hreflang/JSON-LD 정상, 언어별 og:image 완비 상태로 종결됨.** 발견·수정됐던
  것들: 랜딩페이지 상당수가 다른 페이지에서 링크가 하나도 안 걸린 고아 페이지였던 문제(허브
  페이지에 hreflang 언어 목록을 화면 링크로 노출해 해결), 41개 랜딩페이지가 og:image를
  한국어 브랜드 이미지 하나만 공유하던 문제(크메르어·미얀마어 등 스크립트 폰트가 없어
  tofu(□)로 깨지는 문제는 `@fontsource/noto-*` 계열을 npm으로 직접 받아 `@font-face`로
  해결 — 폰트 CDN이 막힌 이 환경에서도 패키지 매니저 도메인은 열려있어서 가능했던 방법,
  비슷한 문제가 다시 생기면 참고), title이 너무 길던 페이지 축약, og/twitter description이
  meta description과 따로 놀던 80개 페이지 동기화. 전부 회귀 테스트(`broken_link_audit`/
  `fact_consistency_audit`) `ISSUES: 0` 확인 후 종결 — 다시 "og:image가 하나뿐이다"류로
  재조사하지 말 것.
- **잭팟 인덱스(이월 스트릭/물가보정 랭킹) — 우려됐던 미착수 항목 3개 전부 이미 해결/구현
  완료 상태였음이 확인됨**: 신규 회차 자동 반영은 위 "새 당첨 회차" 체크리스트의 아카이브가
  이미 담당(별도 GitHub Actions 불필요), 결과 캡처 공유카드는 `saveJackpotIndexShareCard()`로
  이미 배포돼있었음, CPI_BASE_YEAR는 위 항목에서 다룸.

---

## 홍보·마케팅 작업 전체 이력 (2026-08-06 정리, 재조사 금지 참고용)

**"홍보로 뭐 더 할 거 없나" 류 질문이 오면 이 섹션부터 먼저 확인하고 답할 것 — 채널
브레인스토밍은 아래에서 보듯 이미 여러 번(제미나이 5차례+) 공식 종료됐음. 진짜 새로
실행 가능한 채널을 실제로 찾았을 때만 이 섹션에 추가하고, 짐작이나 "혹시 이것도?"로
재조사부터 시작하지 말 것.** (이 섹션은 "지금 상태" 성격이라 날짜별로 계속 쌓지 말고,
상태가 바뀐 항목(완료/승인됨 등)이 있으면 아래 표시를 그때그때 덮어쓸 것 — 자세한 경위는
"작업 이력"/`HANDOFF-ARCHIVE.md`에 이미 있으므로 여기는 요약만 유지.)

- **2026-07-23 — 기반**: SEO 감사로 고아 페이지 발견 후 해결(완료). 도메인 `chamtax.com`
  전환(완료). 구글서치콘솔·네이버서치어드바이저·빙 웹마스터 등록(완료). 17개 언어
  title/og:title SERP 잘림 축약(완료).
- **2026-07-24 — 검색엔진 마무리 + 첫 외부AI 제안**: Daum(카카오) 등록(완료). `llms.txt`
  생성 + robots.txt AI 크롤러 허용(완료). 구글 애드센스 신청(**2026-08-10 주소 확인 PIN
  우편 수신, 사용자가 대시보드에 등록 완료**). IndexNow(당시 반려 → 2026-08-05 재검토해
  구현). 공유 딥링크(`?amount=&country=`), Breadcrumb/SoftwareApplication JSON-LD(완료).
  AlternativeTo/Product Hunt/LaunchingNext 초안 검토(보류 — 백링크 가치 낮음).
- **2026-07-29**: "미국 로또 초보 가이드" 다국어 랜딩페이지 신설, FAQ 연결(완료).
- **2026-07-30~31**: SEO 랜딩페이지 리브랜딩 색상 동기화(완료). OG 동적 공유카드 Worker
  구축(완료).
- **2026-08-01 — 글로벌 타겟 전환(대규모)**: 한국 중심 → 전세계 타겟 피벗, FAQ 다국가×2
  페르소나 확장, 국가별 거주자 랜딩페이지 신설(완료).
- **2026-08-03~04 — AEO(답변엔진최적화) 착수·확장**: 개체 정의 문장·HowTo/Organization/
  Speakable JSON-LD·최종수정일·질문형 H2를 전 페이지에 적용(완료). 다국어 "hook" OG
  이미지 롤아웃(완료).
- **2026-08-05 — 홍보 최대 격전일**: GA4 이벤트·인용 문구·IndexNow 연동(완료). 공개
  데이터셋 저장소 `us-lottery-tax-data`(완료). 기자용 `press-kit.html`(완료). SSG 다국어
  프리렌더링(보류 → 2026-08-06 실측으로 불필요 재확정, 위 "알려진 미해결 항목" 참고).
  랜딩페이지 SEO 감사·수정(완료). "자동봇 마케팅/콜드메일" 등 극단적 안(반려 — ToS 위반).
  임베드 위젯(`widget-embed.html`, 완료). 사이트맵 HTML 페이지 + 관련링크 블록(완료).
  Yandex·Daum 재등록(완료). Baidu 불가(중국 휴대폰 필요). Kaggle 데이터셋 공개(완료).
  Disquiet 제출(**승인 대기**). GeekNews(신규계정 7일 제한 **대기**). GitHub
  README/Topics 정비(완료). **채널 브레인스토밍 5차례+**(100개+ 아이디어) 검토 종료 —
  F5Bot/Google Alerts/AlternativeTo/Wayback/Awesome List PR/CodePen만 실행, 나머지(구식
  링크디렉토리·유료 PR·ORCID/Crossref·브라우저 확장·npm/PyPI·RSS 등)는 전부 반려. GitHub
  Awesome List PR 오픈(**승인 대기**). AlternativeTo 가입, 제출은 **대기**. SECURITY.md/
  CONTRIBUTING.md(완료). CODE_OF_CONDUCT/CITATION.cff/뱃지(반려).
- **2026-08-06 — 커뮤니티 정책 정비 + 바이럴 기능**: Wayback Machine 저장 완료. 6~8차
  브레인스토밍 재검토 → "실행 가능한 무료 채널 0개" 최종 결론. 동남아 로컬 포럼 상시활동
  (반려) → **커뮤니티 정책을 "상시활동 거절, 1회성은 케이스별 허용"으로 세분화**. Cốc Cốc는
  이미 완료 상태였음(robots.txt만으로 충족). Reddit 계정(`chamtax`) 생성, 게시는 대기.
  **향후 프로젝트 공용 계정명 `autumnbuilds` 확정**. Product Hunt 등록 완료(제출 후 방치
  방식). 낚시게임 "국가별 대결" 공유카드 신설(완료). 사용자 제안 홍보기능 7개 전수 확인 →
  딥링크/임베드위젯/이미지저장/Web Share/검색엔진등록 전부 이미 구현됨, 개발자용 JSON
  API(반려 — 타겟층 안 맞음), 잭팟뉴스 대응·도메인신뢰도는 코드로 될 일이 아님(설명만
  하고 종료).

**현재 남아있는 승인/시점 대기 항목**: **구글 애드센스 — 2026-08-20 재확인, 여전히
"정책 위반이 발견되었습니다"(카테고리명 없는 일반 문구)만 뜸.** 사용자가 스크린샷으로
공유한 화면에서 "애드센스 프로그램 정책" 링크를 눌러봐도 구체적 위반 항목 없이 일반
정책 도움말 문서로만 연결됨 — 실제 특정 위반이 아니라 신규 사이트 심사 초기 단계의
표준 placeholder로 판단됨. 이 세션에서 개인정보처리방침(`view-privacy`, AdSense 쿠키
명시적 언급 포함)·면책조항(`view-disclaimer`)·문의폼(`view-contact`)이 전부 이미
존재하고 홈 화면 하단 `site-footer`에 링크돼 있음을 코드로 재확인 — "필수 페이지
누락"이 원인일 가능성도 배제됨. 코드 쪽에서 더 확인할 게 없다고 결론 내리고, 사용자가
"검토 요청" 버튼을 눌러보기로 함(2026-08-20) — **다음 세션은 사용자가 결과를
알려주면 그에 따라 대응**(재반려 시 이번엔 구체적 사유가 나올 가능성 있음, 그 사유를
받으면 그때 코드 조사 재개). 그 전까지는 매번 재점검하지 말 것. **GitHub Awesome List PR**
(`stefanneculai/best-personal-finance-tools#4`) 승인 대기 — 2026-08-20 확인 결과 리뷰
코멘트 없이 오픈 상태, 그냥 대기. **Disquiet — 2026-08-20에 사용자가 "몇 개월 뒤로
미루자"고 결정**: 제출은 완료, 재확인은 사용자가 다시 물어볼 때까지 대기.
**AlternativeTo·GeekNews·Reddit 게시 — 2026-08-18에 사용자가 "몇 개월 뒤로 미루자"고
결정**: 계정/초안은 준비돼 있으니 실행 시점 자체는 지났지만 바로 안 올림. **Show HN은
날짜 고정 대기가 아님** — HN이 "최근 신규 유입이 많아 Show HN을 일시 전면 제한 중,
커뮤니티에 익숙해진 뒤 재시도하라"는 운영진 공지를 직접 띄운 것으로 확인됨(계정 나이·도메인
차단이 아니라 HN 전체의 임시 정책, URL/텍스트 포스트 형식을 바꿔도 우회 안 됨) — 실제로
다른 글에 댓글 등 커뮤니티 활동을 좀 한 뒤 재시도해야 함, "기한 없음"으로 취급할 것.

**정책 최종 상태**: 커뮤니티 상시활동은 항상 거절, 1회성 게시는 케이스별 허용(현재
r/SideProject·r/IndieHackers·Show HN 3곳 승인). 채널 브레인스토밍은 여러 차례 공식
종료 선언됨. 3곳 게시글 영문 초안(서로 다른 문장, 복붙 스팸처럼 안 보이게)은 이미 작성해
`SendUserFile`로 전달 완료 — 실행은 사용자 몫.

**"영문 AI/웹 생태계 1회성 인프라" 4개 검토(2026-08-06), 2개 실행·2개 보류**:
- **MCP 서버 구현·검증 완료**(`mcp-server/`, 의존성 0개): `calculate_lottery_takehome` 툴
  하나를 노출하는 stdio JSON-RPC 서버. `script.js`의 세금 계산 로직을 DOM 의존성 없는
  순수 숫자 계산으로 재구현(`tax-data.js`) — 실제 사이트와 동일한 결과인지 수동 검증함
  (검증 중 `pk`가 지원국가 목록에서 빠진 실제 버그 1건 발견·수정). 로컬 설치형이라 공개
  API 호스팅 불필요. **주의**: `tax-data.js`는 `script.js`를 손으로 베낀 것이라 자동
  동기화 안 됨 — `script.js` 세율이 바뀌면 같이 고칠 것(README.md에도 명시). glama.ai
  MCP 디렉토리 등록용 `glama.json`도 저장소 루트에 추가함(공식 검증 필드는
  `$schema`+`maintainers`뿐이라 그 둘만 채움) — 실제 색인 여부는 다음 세션이
  glama.ai/mcp/servers에서 검색해 확인.
- **Dev.to 기술 글 발행 완료**: Show HN과 다른 각도(세금 로직 엔지니어링 딥다이브)로 작성,
  `autumnbuilds` 계정으로 초안 생성 → 사용자가 외부 AI로 다듬은 상세 버전으로 교체(과장
  문장 1곳 정정 후 반영) → 실제 발행까지 완료. **최종 공개 URL**:
  `dev.to/autumnbuilds/how-i-modeled-us-lottery-tax-withholding-for-21-countries-
  without-a-backend-4m9`. 프로필 로고는 dev.to 공개 API에 업로드 엔드포인트가 없어
  (`GET /api/users/me` 확인, 읽기 전용) 사용자가 직접 업로드함. **이 웨이브 완전히 종료**.
- **OpenAI Custom GPT Action·Hugging Face Space는 보류**: 둘 다 세금 로직을 공개 API로
  한 벌 더 만들거나(Custom GPT) 별도 배포 계정 로그인이 필요해(HF Space) 사용자 확인
  없이 새 백엔드/표면을 만들지 않기로 함.

**4개 기술채널(ChatGPT Custom GPT 지식파일, npm/PyPI 패키지, 브라우저 확장, Cloudflare
Worker API 마켓플레이스 등재) 전수 반려**: 전부 이전에 반려했던 두 패턴(세율 유지보수
표면 중복 / 실제 타겟층인 복권 당첨자·이민자와 오디언스 불일치)으로 귀결됨을 확인 —
4개 다 반려 확정.

**"영문권 유입은 잭팟뉴스→구글/AI검색→ChamTax 랜딩페이지가 1순위 노출"이라는 낙관적
결론을 `WebSearch`로 실측 검증, 틀린 전제였음을 확인**: "Powerball tax for non-US
resident" 등 실제 검색 결과에 `chamtax.com`은 전혀 안 나옴(TheLotter·LotteryCalc 등만
노출). 다만 최종 행동 결론(새 채널 안 찾고 Reddit/Show HN 마무리 후 대기)은 유효 —
근거만 "기술적 조치(AEO/SEO)는 다 해놨고 지금 병목은 도메인 신뢰도·백링크 축적(시간이
지나야 쌓임)"으로 정정. **다음 세션 참고**: "이미 잘 되고 있다"류 낙관적 결론이 다시
나오면 결론만 보고 넘기지 말고 `WebSearch`로 실제 검색해 확인하는 습관을 유지할 것.

---

## 향후 아이디어 백로그 (미착수 — 검증 안 된 아이디어, 지금 진행 안 함)

**2026-07-30 세션에서 논의만 하고 착수는 안 하기로 확정한 것들** — 사용자가 "기록만
남겨달라"고 명시적으로 요청함, 다음 세션이 임의로 시작하지 말 것(아래 각 항목의
"보류 이유" 참고, 실제 사용자 요청/검증된 수요가 생기기 전엔 그대로 둘 것):

- **AI 안경(스마트글래스) 연동**: 제미나이가 "세계 최초 AI 안경 호환 복권 계산기" 같은
  마케팅 프레이밍을 제안했으나, 검토 결과 개인 개발자가 안경 전용 앱스토어/API에 입점할
  구조 자체가 아직 없음(메타 레이밴 등은 안경 내장 AI가 웹사이트를 읽어가는 방식뿐) —
  "안경 호환"이라는 이름으로 새로 만들 게 딱히 없다고 결론남.
- ~~**결과 음성 읽어주기(TTS)**~~ — **완료(2026-07-30 착수, 2026-08-03 이모지 읽힘 문제까지
  다듬음)**: 홈 결과 헤드라인·비교 카드 1위·FAQ 답변마다 "🔊 읽어주기" 버튼으로 배포돼있음
  (`toggleReadAloud()`/`stripEmojiForTts()`, script.js). **다음 세션이 또 "안 하기로 함"으로
  착각하지 말 것.**
- ~~**"3줄 AI 요약"**~~ — **완료(2026-08-05)**: 실제 LLM 호출 없이 이미 계산된 값을 문장
  템플릿에 끼워 넣는 방식으로 구현됨.
- **"영수증 이미지 공유"**: **이미 존재함**(`saveHomeResultAsImage()` 등) — 새로 만들 필요
  없고, 원한다면 기존 이미지 디자인을 "영수증 느낌"으로 다듬는 정도만 남음.
- ~~**재한 외국인 대상 송금/환전 가이드**~~ — **완료(2026-08-04)**: `faq.q22`/`faq.a22`로
  추가함(당첨금을 한국 계좌로 받을 때 은행 실무·외국환거래법 기준).
- **잭팟 금액 임계값 웹 푸시 알림**: "개인정보 수집 없이 익명 토큰으로 가능하다"는
  제안을 받았으나, **이 사이트는 GitHub Pages 정적 호스팅이라 서버 자체가 없음** —
  익명 토큰이라도 저장하고 스케줄에 맞춰 발송하려면 서버리스 함수+DB 같은 새 인프라를
  통째로 구축해야 해서 "무료"가 아님. 지금 규모에 비해 과한 투자로 판단, 보류.
- **2026-08-05 신규 — "극단적 자동화 마케팅"(AI 자율 홍보 파이프라인) 제안, 대부분 거절함**:
  외부 AI가 Reddit/X/Quora/Threads 자동 댓글 봇, 기자 이메일 자동 수집+대량 콜드메일 발송,
  AWS Lambda+Celery 24시간 무인 파이프라인 등을 제안했으나 **명시적으로 거절**함 —
  자동 댓글 봇은 대부분 플랫폼 이용약관이 금지하는 스팸/어스트로터핑이고, 콜드메일 자동
  수집·발송은 이 저장소가 이미 "개인정보 부담으로 안 하기로 확정"한 방침과 정면으로
  배치됨(위 "하지 않기로 확정한 것" 성격의 판단과 동일선상). 광고 자동집행·숏폼 영상
  자동생성·서버리스 인프라 등은 실제 유료 API 키/클라우드 계정/결제수단이 필요해 세션이
  임의로 진행할 수 없는 영역이라 보류. **유일하게 긍정 검토한 것**: 다른 사이트에 붙일 수
  있는 임베드 계산기 위젯(봇도 아니고 유료 계정도 불필요, 백링크 확보에 직접 도움) —
  **완료(2026-08-05)**: `widget-embed.html`(noindex, iframe 삽입용 미니 계산기) 제작,
  `press-kit.html`에 삽입 코드 안내 섹션도 추가함. 다시 이 종류 제안이 오면 위 판단
  기준(플랫폼 ToS 위반 여부, 이미 거절한 방침과의 충돌 여부, 실제 계정·비용 필요 여부)
  으로 먼저 걸러낼 것.
- **2026-08-18 신규 — Quora·Reddit "검색 기반 답변 마케팅"(사용자 제안, 미착수)**: 봇
  자동화가 아니라 **사용자가 직접 손으로** 기존 질문 스레드를 찾아 답변을 쓰는 방식이라
  위 "극단적 자동화 마케팅"(자동 댓글 봇) 반려 건과는 다른 건 — 커뮤니티 정책상
  "1회성/케이스별 허용"에 해당해 방침과 충돌 없음.
  - **Quora**: `site:quora.com "US lottery tax"` / `"foreigner powerball tax"` 등으로
    조회수 높은 기존 질문을 찾아, 원천징수 30%·국가별 조세조약(비거주자 과세 구조) 설명을
    충실히 작성한 뒤 답변 하단에 ChamTax 계산기 링크를 자연스럽게 덧붙임.
  - **Reddit**: `r/personalfinance`·`r/tax`·`r/expats`·`r/Lottery` 등에서 `lottery tax
    non resident` / `powerball foreign winner` 검색으로 기존 글을 찾아, 전문적인 답변을
    먼저 충실히 쓰고 참고용 링크를 가볍게 덧붙임(과도한 상업적 링크는 모더레이터가 삭제할
    수 있어 정보 제공이 최우선).
  - 코드 변경이 아니라 **사용자가 계정으로 직접 실행**해야 하는 일이라 이 세션이 대신
    못 함. 실행 시점은 사용자 판단(현재는 대기, 필요할 때 요청하면 재검토).
- **2026-08-18 신규 — 다국어 위키백과 '세금' 섹션에 링크 등재(사용자 제안, "링크만 남기는
  버전"과 "신뢰 쌓은 뒤 몰래 심는 버전" 둘 다 검토 후 미착수로 보류)**: 목적 자체(파워볼/
  메가밀리언즈 문서 세금 섹션에 원천징수 30%·조세조약 팩트 보완)는 유용하지만, 실행
  방법 두 가지 다 위키백과 이해상충(WP:COI)·링크스팸(WP:REFSPAM) 규정과 충돌해서
  **이 세션은 실행을 거절함**:
  - **1차 제안("팩트 보완 후 참고문헌란에 자연스럽게 링크")**: 본인 소유 사이트를
    출처/외부링크로 넣는 것 자체가 이해상충 — 세법/금융 문서는 특히 스팸 감시가
    빡빡한 영역(XLinkBot·COIBot 등)이라 반려.
  - **2차 제안("무관한 문서 5~10개 먼저 편집해 신뢰 쌓기 → 팩트만 채워 신뢰 확보 →
    나중에 외부링크 삽입, 편집자 적고 검토 느슨한 소언어권부터 시작")**: 이건 스팸
    탐지를 피하는 방법이라 **더 확실하게 거절** — 위키미디어 이용약관이 요구하는
    "이해관계 공개" 의무와 정반대(이해관계를 숨기고 일반 편집자처럼 보이게 만드는
    설계)이고, 편집자가 적어 감시가 약한 언어판을 의도적으로 골라 시작하는 것도
    해당 커뮤니티의 취약점을 이용하는 행위라 어느 언어판이든 규정 위반. **다음
    세션도 이 "신뢰 쌓기 후 링크 삽입" 변형이 다시 제안되면 같은 이유로 거절할 것
    — 재조사·재검토 불필요.**
  - **정책 준수 대안(미실행, 제안만 함)**: 사용자 페이지에 이해관계(chamtax.com
    운영자)를 공개하고, 문서를 직접 고치지 않고 해당 문서 토크(Talk) 페이지에
    "세금 섹션 보완 + 참고 링크 추가"를 편집 요청(Edit Request)으로 올려 제3의
    편집자 판단에 맡기는 방식. 사용자가 원하면 토크페이지에 올릴 팩트 요약+COI
    공개 문구 초안을 다음에 작성 가능(아직 작성 안 함).

## 작업 이력 (날짜순, 세션마다 맨 아래에 새 항목 추가)

이보다 오래된 세션 기록(~2026-08-20 국가별 세금 비교 페이지 6개 언어 다국어화까지)은
`HANDOFF-ARCHIVE.md` 참고(특정 과거 이슈의 배경이 필요할 때만 검색, 매 세션 필독
아님). 이 본문에는 최근 세션(국가별 세금 비교 페이지 나머지 30개 언어 완료, 홈페이지
언어 커버리지 최종 검증, 애드센스 정책위반 화면 재조사, 세션 시작 토큰 절감 방안 정리)
기록만 남겨둠 — 날짜별 항목이 3~4개를 다시 넘어가면 가장 오래된 날짜부터 또 이
방식으로 정리할 것(같은 패턴 반복,
`HANDOFF-ARCHIVE.md` 맨 뒤에 이어 붙이면 됨. **참고**: 세션 로그를 쓸 때는 반드시
`### YYYY-MM-DD ...` 헤더로 시작할 것 — 헤더 없이 이어붙인 기록은 "몇 개가 쌓였는지" 셀 수
없어서 이 정리 규칙 자체가 무력화됨, 2026-08-20 정리 세션에서 실제로 헤더 없는 기록이
방치돼있던 걸 발견해 뒤늦게 헤더를 붙여 아카이브함).

### 2026-08-20 이어서 — 국가별 세금 비교 페이지 나머지 30개 언어 완료 (36개 언어 전체 완성)

직전 세션에서 6개 언어(en/ko/zh/vi/hi/ja)만 다국어화했었는데, 사용자가 "언어 더
해줘" → "언어 다 해줘"로 두 번 더 요청 — 이 사이트가 지원하는 나머지 언어 전부
(fr/de/es/ru/th 5개, 이어서 ar/ur/tr/pl/pt/it/nl/id/bn/ne/si/my/km/lo/mn/tl/kk/ky/
uz/uk/sv/no/da/fi/tet 25개)를 순차적으로 병렬 서브에이전트 배치(1차 5개국 개별,
2차 25개국을 4~5개국씩 6개 그룹)로 진행 — 최종적으로 36개 언어(한국어 포함, 이
사이트가 지원하는 모든 언어) 전체가 이 비교 페이지를 갖게 됨.

**세션 한도 도달·복구**: 2차 배치(6개 서브에이전트 동시 실행) 도중 API 세션
한도(resets 2:40am UTC)에 걸려 그중 6개 중 6개 전부가 중단됨. 확인해보니 각
에이전트가 파일을 순차적으로 작성하고 있어서 중단 시점까지 이미 완성해둔 파일이
여럿 있었음(13개 언어 파일이 이미 완전한 상태로 디스크에 남아있었음) — 이걸
그냥 버리지 않고 JSON-LD 파싱+Playwright 숫자 정확도 검증을 직접 돌려 전부
정상임을 확인한 뒤 커밋. 남은 12개 언어(완전히 누락됐거나 부분적으로만 있던 것)는
한도 리셋 시각(UTC 기준)이 이미 지난 걸 `date -u`로 확인한 뒤 3개 그룹(4개국씩)
으로 다시 서브에이전트를 띄워 전부 완료.

**정직하게 기록해둘 실수**: 새 언어 파일들에 leftover Korean(번역 누락) 검사를
`grep -oP '[가-힣]'`로 하려다가 이 sandbox 셸의 `LC_CTYPE=POSIX` 로케일 때문에
`grep -P`가 아랍어·키릴 문자 등 비라틴 스크립트의 멀티바이트 UTF-8 시퀀스를
한글로 잘못 인식해서(아랍어 파일에서 13,167개, 카자흐어 파일에서 16,028개
"한글"이 나온 것처럼 보임) 실제로는 멀쩡한 파일들을 심각하게 오염된 것으로
오판할 뻔함 — Python(`re.findall(r'[가-힣]', ...)`, 제대로 된 유니코드 인식)으로
재확인하니 전부 정확히 7자(브랜드명 "참택스"+디자인시스템 코드 주석 "정산 티켓"만,
둘 다 의도적으로 번역 안 한 부분)로 정상이었음. 이후 서브에이전트들에게도 이
로케일 함정을 미리 경고해 같은 실수를 반복하지 않게 함.

**마무리 작업**: 노드 스크립트로 36개 파일 전체에 완전한 hreflang 상호링크(각자
나머지 35개를 전부 가리키고 x-default는 영어판)를 일괄 주입 — 기존 6개 언어짜리
부분 블록을 정규식으로 제거 후 새로 삽입(수작업 편집 대신 스크립트화, 36파일×37개
태그=1,332줄을 정확하고 일관되게 처리). `script.js`의
`COMPARE_COUNTRIES_PAGE_LANGS` 배열을 6→36개로 확장. `i18n-source/translations.json`
의 `home.compareCountriesLink` 키에서 남은 30개 언어의 "(EN)" 접미사 제거 —
처음에 `JSON.stringify(data, null, 2)`로 통째로 재직렬화했다가 무관한 부분까지
포맷이 틀어지는 대형 diff(9,747줄)가 생겨서 즉시 되돌리고, 정확히 그 30줄만
바꾸는 수술적 편집으로 재작업(diff 60줄로 축소) — "관련 없는 정리 금지" 원칙을
지킴. `sitemap.html`/`sitemap.xml`도 36개 언어 전체 반영.

**검증**: `fact_consistency_audit`(185개)·`drift_consistency_test`(29개국)·
`i18n_coverage_audit`(794 키)·`i18n_attr_lint`·`lang_leak_audit`(140개)·
`faq_audit`(18개)·`broken_link_audit`(180개)·`console_error_audit`(224개) 전부
`ISSUES: 0`. Playwright로 대표 언어 10개(우즈베크어·테툼어·타갈로그어·미얀마어·
크메르어·아랍어·핀란드어·폴란드어 등) href 스왑 로직 재검증.

**⚠️ 다음 세션 참고**:
- 이 페이지는 이제 사이트가 지원하는 36개 언어 전부를 커버 — 새 언어가 사이트
  전체에 추가되지 않는 한 이 페이지의 다국어화 작업은 완료된 상태.
- leftover-Korean/leftover-English 같은 텍스트 검사를 셸에서 할 때는
  `grep -oP '[가-힣]'` 대신 Python(`re.findall`)을 쓸 것 — 이 sandbox의
  `LC_CTYPE=POSIX` 로케일에서는 `grep -P`의 유니코드 문자 범위 매칭이 비신뢰
  가능(이번 세션에서 처음 발견된 함정, 앞으로 다른 다국어 작업에서도 재발할 수 있음).
- JSON 파일을 프로그래밍적으로 수정할 때 `JSON.parse` → 수정 → `JSON.stringify`
  전체 재직렬화 방식은 원본 포맷과 미묘하게 달라 무관한 부분까지 diff가 커질 수
  있음 — 특정 몇 줄만 바꿀 때는 문자열 치환(Edit 도구의 old_string/new_string
  또는 타겟 라인만 바꾸는 정규식)을 우선 사용할 것.
- 이 페이지처럼 JS 로직이 박힌 정적 페이지를 다국어화할 때는 반드시 Playwright로
  원본과의 숫자 정확도(비즈니스 로직 무결성)를 검증하고 텍스트만 바뀌었는지
  확인하는 습관을 다른 유사 페이지(`lottery-tax-data-hub.html` 등)에도 적용할 것.
- 병렬 서브에이전트를 6개 이상 동시에 띄우면 API 세션 한도에 걸릴 수 있음 —
  큰 배치가 필요하면 4~5개씩 나눠 진행하는 게 더 안전. 한도에 걸려도 이미
  작성된 파일은 버리지 말고 검증 후 재사용할 것(이번 세션에서 13개 파일을
  그렇게 구제함).

### 2026-08-21 이어서 — 홈페이지 언어 커버리지 100% 최종 검증

사용자가 "홈페이지 언어는 100% 다 커버된 거야?"라고 질문 — 국가별 세금 비교
페이지 36개 언어 작업(직전 세션)과 별개로, **홈페이지(`index.html`) 자체**의
번역 완전성을 다시 확인해달라는 요청. 새 기능 구현이 아니라 검증만 수행.

**확인 방법 및 결과 (전부 통과)**:
- 언어 선택 드롭다운(`index.html` `<select>`) 옵션 36개(ko + 35개) vs
  `i18n/*.json` 파일 목록(35개) + ko를 Python으로 집합 비교 — 양쪽 완전히 일치,
  누락/초과 없음.
- `script.js`의 `SUPPORTED_LANGS`(`['en','zh','vi','th','ru', ...ADDITIONAL_LANGS]`,
  `ADDITIONAL_LANGS` 30개 언어) + 'ko' 별도 처리 = 36개로 드롭다운과 정합.
- `tests/i18n_coverage_audit.js` 재실행 — 홈페이지의 `data-i18n`/`data-i18n-html`/
  `data-i18n-placeholder`/`data-i18n-aria-label` 키 794개 전부를 35개 언어 파일
  기준으로 대조, **`ISSUES: 0`**(누락 키·부분 번역 없음).
- `tests/lang_leak_audit.js` 재실행 — 140개 설정 전부 **`ISSUES: 0`**(다른 언어
  텍스트가 섞여 나오는 문제 없음).

**결론**: 홈페이지는 지원 36개 언어 전부에서 텍스트 커버리지 100% 확인됨. 코드
변경 없음(검증만 수행) — 이 항목은 HANDOFF.md 갱신 커밋 외에 별도 기능 커밋 없음.

**⚠️ 다음 세션 참고**: 이후 새 언어를 사이트에 추가하거나 홈페이지에 새
`data-i18n*` 요소를 추가할 때는 반드시 `i18n_coverage_audit.js`를 재실행해
전 언어 커버리지가 깨지지 않았는지 확인할 것 — 이 감사가 정적 분석(브라우저 불필요)
이라 비용이 매우 낮음.

### 2026-08-20 이어서 — 애드센스 "정책 위반" 화면 재조사 + 검토 요청 권고

사용자가 애드센스 앱에서 "정책 위반이 발견되었습니다"(카테고리명 없음) 화면 스크린샷을
여러 장 공유하며 원인을 물어봄. "애드센스 프로그램 정책" 링크를 눌러도 chamtax.com
전용 사유가 아니라 일반 정책 도움말 문서(연령제한 태그·COPPA·온라인 도박 정책 등)로만
연결됨을 스크린샷으로 여러 차례 확인 — 이 UI 자체가 구체적 사유를 안 보여주는 것으로
결론.

사용자가 다른 AI(정확히 어디서 온 건지는 명시 안 됨)로부터 받은 체크리스트(개인정보처리방침
/이용약관/소개/문의 페이지 누락, 텍스트 콘텐츠 부족, GSC 색인 부족을 원인으로 지목)를
붙여넣어서 검증 요청 → **코드로 직접 확인한 결과 전부 이미 존재함**:
- `view-privacy`(개인정보처리방침, AdSense 쿠키 사용을 4·5번 항목에서 명시적으로 언급)
- `view-disclaimer`(면책조항, 사실상 이용약관 역할, 7개 섹션)
- `view-contact`(Formspree 연동 실제 작동하는 문의폼)
- 위 3개 전부 홈 화면 `site-footer`(`class="footer-links"`)에 링크돼 있음
- GSC 색인 요청은 이전 여러 세션에 걸쳐 반복적으로 처리해온 이력이 이미 있음(HANDOFF
  상단 참고)

즉 그 체크리스트는 이 사이트에 해당 안 되는 일반론이었음 — "새로 만들어야 한다"는
결론이 틀렸다는 걸 코드 근거로 반박함. 사용자에게 "코드상 더 확인할 게 없다, 검토
요청을 눌러도 손해볼 게 없다"고 권고 → 사용자가 검토 요청 진행 예정.

**⚠️ 다음 세션 참고**: 사용자가 검토 요청 결과(승인/재반려)를 알려주면:
- 승인됐다면: 위 "현재 남아있는 승인/시점 대기 항목" 문단에서 애드센스 항목 제거.
- 재반려됐는데 이번엔 구체적 카테고리명이 나왔다면: 그 카테고리명을 갖고 코드 재조사
  시작(예: "가치 있는 인벤토리: 콘텐츠 부족"이면 실제 방문자 트래픽/체류시간 문제일
  수도 있어 코드만으론 못 고칠 가능성도 염두에 둘 것).
- 여전히 카테고리명 없이 똑같은 화면이라면: 재조사 없이 "그대로 대기" 상태 유지,
  PC 브라우저로 확인해보라고 이미 안내했었음(이번 세션에서 사용자가 PC로 확인 시도함,
  역시 구체적 사유 없이 일반 문서로 연결된 것까지 확인됨).

### 2026-08-21 이어서 — 세션 시작 시 토큰 소모가 큰 문제 조사 + 운영 방식 정리 (코드 변경 없음)

사용자가 "새 세션 시작할 때마다 토큰이 너무 많이 든다, 리스크가 크다"며 줄일 방법을
요청. 코드/설정 수정이 아니라 **원인 조사 + 운영 습관 합의**만 진행한 세션 — 커밋된
코드 변경 없음.

**조사 결과**: 이 저장소 쪽에는 손볼 게 이미 없었음 —
- `CLAUDE.md`(49줄)는 이미 "큰 출력 덤프 금지, 파일 offset/limit로 읽기" 원칙을
  스스로 지키고 있어 더 줄일 여지 없음.
- 레포에 `.mcp.json`(레포 레벨 MCP 서버 설정) 없음, `.claude/settings.json`에도
  `SessionStart` 훅 없음 — `HANDOFF.md`(860줄+)나 다른 대용량 파일이 세션 시작 시
  자동으로 컨텍스트에 덤프되는 지점 자체가 없음.
- `.claude/settings.json`의 permission 목록(20줄 안팎)은 이미 최소 상태라 정리해도
  절감되는 토큰이 사실상 0 — 손대지 않음.

**사용자가 공유한 claude.ai 계정 설정 스크린샷 4장(스킬/커넥터/플러그인/메모리) 확인
결과도 이미 최소 상태**:
- 스킬 탭: `morning`, `skill-creator` 2개뿐. (단, Claude Code 세션에 뜨는 20개 가까운
  내장 스킬 목록 — design/dataviz/docx/pdf/pptx/xlsx/code-review 등 —은 이 "사용자
  지정 스킬" 탭이 아니라 Claude Code에 기본 내장된 카탈로그라서 여기서 끄고 켤 수 있는
  대상이 아님. 다음 세션에서 이 목록을 더 줄이는 방법을 다시 찾을 필요 없음 — 확인
  결과 유저 쪽에서 통제 불가능한 고정 비용으로 결론남.)
- 커넥터 탭: GitHub 연동만 연결, 나머지(Gmail/Calendar/Drive)는 미연결.
- 플러그인 탭: 설치된 것 없음.
- 메모리 탭: "채팅 검색 및 참조", "채팅에서 메모리 생성" 둘 다 꺼져 있음.

**합의된 결론(운영 방식으로 해결, 설정 변경 아님)**: 세션 시작 토큰의 실제 원인은
Claude Code 세션에 자동으로 붙는 `github` MCP 도구 60여 개 + 안내 텍스트 블록이었고,
이건 레포나 계정 설정이 아니라 **"어떤 작업에 어떤 창을 쓰는지"로 해결하기로 함**:
- 코드 수정·GitHub(PR/이슈) 작업이 전혀 필요 없는 일(기획, 번역, 세법 검토, 아이디어
  브레인스토밍 등)은 터미널 Claude Code 대신 평소 웹 채팅(claude.ai)을 사용 —
  웹 채팅에는 애초에 GitHub MCP 도구가 안 붙어서 가장 확실한 절감법.
  실제로 코드를 고치거나 PR/이슈를 다뤄야 할 때만 이 레포가 연결된 Claude Code
  세션을 사용.

**추가로 정리한 토큰 절감 실전 노하우(환경 분리 외, 사용자가 직접 정리해 요청)**:
환경 분리(웹 vs 터미널)만으로 끝내지 않고, 앞으로 이 저장소에서 세션을 진행할 때
아래 습관도 함께 지킬 것 — 전부 이 문서 상단 "새 세션 시작 시 지켜야 할 것"이나
`CLAUDE.md`의 기존 원칙(큰 덤프 금지, offset/limit로 필요한 부분만 읽기 등)과 같은
방향이라 서로 충돌 없음, 여기서는 사용자에게 전달한 표현 그대로 기록:
1. **세션 수명 짧게 가져가기** — 대화가 길어질수록 이전 내용을 매번 다시 읽는 구조라
   기능 구현/버그 수정 하나가 끝나면 새 세션으로 넘어가는 걸 기본으로 할 것. 과거
   맥락이 필요하면 전체를 다시 보여주지 말고 핵심만 요약해서 넘길 것.
2. **전체 파일 대신 변경 부위 위주로 소통** — "전체 코드 보여줘" 대신 "바뀐 핵심
   로직만", "이 함수 안에서 바뀐 부분만" 식으로 범위를 좁혀서 요청/응답할 것(이미
   `CLAUDE.md`에 있는 "grep으로 구간 찾고 offset/limit로 읽기" 원칙과 동일선상).
3. **군더더기 없는 응답 유지** — 인사말·장황한 설명·불필요한 반복 없이 결론부터
   간결하게 답할 것.
4. **파일 탐색 범위를 직접 지정해주기** — "프로젝트 구조 분석해줘"처럼 광범위하게
   던지지 말고, 대상 파일 경로를 사용자가 직접 짚어줄 것(예: "`index.html`과
   `odds-data.js`의 특정 부분만 봐줘"). 세션 쪽에서도 광범위한 탐색이 필요하면
   먼저 Grep/Glob으로 범위를 좁히고 나서 읽을 것 — 이미 이 문서와 `CLAUDE.md`에
   있는 습관이지만 사용자가 요청 단계에서부터 범위를 좁혀주면 한 번에 더 정확해짐.

**⚠️ 다음 세션 참고**: 이 항목은 순수 상담/운영 합의 세션이라 다음 세션이 이어받을
후속 작업(코드/테스트/검증)이 없음. 혹시 사용자가 "그때 그 토큰 절감 얘기 다시
설명해줘"라고 물으면 이 항목을 참고해서 반복 조사 없이 바로 답할 것 — 레포 쪽
원인은 이미 다 확인했고 남은 건 없다는 결론이 이미 났음. 위 1~4번 실천법은 매
세션 시작할 때마다 스스로도 상기할 것(특히 2번·4번은 이미 `CLAUDE.md`에 있는
원칙이니 새 규칙처럼 취급해 중복 강조하지 말고, 이 습관을 실제로 지키고 있는지
점검하는 용도로만 참고).

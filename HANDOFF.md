# 참택스(ChamTax) 프로젝트 — 인수인계 문서 (통합본)

이 파일 하나가 **유일한 정식 인수인계 문서**입니다. `handoff-*.md`로 흩어져있던 예전 문서들
(2026-07-17 ~ 2026-07-23)을 이 파일 하나로 합쳤습니다. **새 세션을 시작하면 이 파일 전체를
Claude에게 읽게 하세요.**

**앞으로도 새 `handoff-...-partN.md` 파일을 만들지 말고, 이 파일 안의 "작업 이력" 섹션 맨 아래에
새 세션 항목을 이어서 추가할 것.** 다만 이 섹션이 계속 쌓이기만 하면 파일이 다시 비대해지므로
(2026-07-28에 2,300줄+ 상태에서 한 번 정리한 전례 있음, 아래 "작업 이력" 섹션 참고), **"작업
이력" 섹션에 남아있는 날짜별 세션 항목이 3~4개를 넘어가면, 가장 오래된 항목부터
`HANDOFF-ARCHIVE.md`(저장소 루트, 이 파일과 형제 파일)로 그대로(요약·의역 없이 원문 그대로)
옮기고 이 파일에는 최근 3개 세션 + "이보다 오래된 세션 기록은 `HANDOFF-ARCHIVE.md` 참고" 한
줄 포인터만 남길 것.** 옮기는 작업 자체도 "작업 이력"에 쌓아두지 말고 담담하게 그때그때
수행할 것(별도 세션 항목으로 기록할 필요 없음). "지금 상태" 관련 섹션(프로젝트 개요·아키텍처·
회귀 테스트·알려진 미해결 항목)은 바뀐 내용이 있으면 그때그때 덮어써서 항상 최신 상태를 유지할
것 — 날짜별로 쌓아두지 말고.

---

## 📌 새 세션 시작 시 지켜야 할 것

1. **항상 서브에이전트(Agent tool)로 시작할 것.** 메인 세션에서 탐색·점검·수정을 전부 직접
   하지 말고, 실질적인 작업은 서브에이전트에게 맡겨서 메인 세션의 컨텍스트를 아낄 것.
2. **이 저장소는 공개 저장소임(`semilee123456-ui/semilee123456-ui.github.io`).** 커밋되는
   모든 글(커밋 메시지, 코드 주석, 이 인수인계 문서 포함)은 GitHub에 업로드되면 누구나 볼 수
   있다고 가정하고 작성할 것.
3. **인수인계 문서(이 파일)에 쓰려는 내용 중 공개 저장소에 올리기 부적절한 게 있으면**(민감
   정보, 사용자 개인 신상, 내부용으로만 남기고 싶은 메모 등) — **이 파일에 넣지 말고, 대신
   사용자에게 채팅으로 직접 말할 것.** 조용히 빼놓기만 하지 말고, "이런 내용이 있는데
   공개 파일엔 못 적어서 여기 따로 말씀드려요"라고 알려줄 것.
4. **사이트 파일(HTML/CSS/JS)을 실제로 고쳤으면, 매번 Artifact 도구로 바뀐 페이지(또는 관련
   화면)를 렌더링해서 오른쪽 프리뷰 패널에 띄울 것.** 2026-07-24(열한 번째 세션)에 사용자가
   명시적으로 요청한 습관 — "커밋했다"는 텍스트 보고만으로 끝내지 말고, 매번 눈으로 바로 결과를
   확인할 수 있게 할 것. 로컬 HTML 파일을 그대로 Artifact로 publish하면 되고(외부 리소스 fetch가
   막혀있는 CSP 환경이라 `styles.css`/`script.js` 같은 외부 파일 링크는 인라인으로 합쳐 넣어야
   실제로 렌더링됨 — 원본 파일 자체를 고치지 말고 미리보기용 임시 파일을 스크래치패드에 따로
   만들 것), 텍스트 변경처럼 스크린샷/HTML 렌더링이 의미 없는 작업(예: HANDOFF.md 문서 수정,
   ads.txt 값 변경)까지 억지로 프리뷰를 만들 필요는 없음 — 사용자가 눈으로 확인할 시각적 결과물이
   있는 변경(레이아웃, 신규 UI 요소, 문구 노출 방식 등)에 한해 적용. **2026-07-28 세션에서 이
   습관을 지키지 못함**(Playwright 텍스트 검증만 하고 Artifact 프리뷰는 한 번도 안 띄움) —
   다음 세션은 이 항목을 다시 챙길 것.
5. **세션을 시작하자마자(코드를 고치기 전에) `git fetch origin main && git diff --stat
   origin/main HEAD`로 로컬과 실제 배포본 사이에 차이가 있는지부터 확인할 것.** 2026-07-24
   (일곱 번째 세션)와 2026-07-27(열세 번째 세션) 두 번 다, 이전 세션이 "다 전달했다"고
   기록해놓고 실제로는 사용자가 일부 파일(zip 여러 개 중 하나, 또는 특정 파일)을 GitHub에
   안 올렸거나 못 올린 채로 세션이 끝난 뒤, 다음 세션(또는 같은 세션 후반부)에서 사용자가
   "진짜 반영된 거 맞아?"라고 물어봐서야 뒤늦게 발견됨(연금 슬라이더 HTML이 몇 세션째 안
   올라가 있었던 사례 등). **이전 세션의 이 문서 기록을 "실제로 배포됨"과 동일시하지 말고,
   git으로 직접 검증할 것** — diff가 있으면 그 파일들부터 사용자에게 다시 전달.
6. **`git push`가 막혀있어서 파일을 zip/개별 SendUserFile로 전달 → 사용자가 GitHub 웹 UI로
   업로드하는 흐름인데, 이 업로드가 세션이 끝나기 전에(같은 대화 도중에) 실제로 일어날 수
   있음.** 2026-07-28 세션에서 사용자가 파일을 보내줄 때마다 몇 분 안에 바로 GitHub에
   업로드해서, `origin/main`이 세션 진행 중에 여러 번(최소 3번) 앞으로 나아갔음. 위 5번
   항목(세션 시작 시 확인)만으로는 부족함 — **매 작업 배치를 시작하기 전이나 새 zip을 묶기
   직전에도 다시 `git fetch origin main`으로 확인**하고, `origin/main`이 앞서 있으면
   `git merge origin/main`으로 합친 뒤(이 프로젝트는 지금까지 전부 자동 병합됐음 — 같은
   파일이라도 서로 다른 부분을 고쳤을 땐 충돌 안 남) 문법 검사(`node --check script.js`)와
   핵심 회귀 테스트를 한 번 더 돌리고 나서 다음 작업을 진행할 것. **절대 로컬 작업본을
   그대로 zip으로 묶어서 사용자에게 다시 보내면 안 됨** — `origin/main`에 이미 있는
   독립적인 변경사항(예: 사용자가 다른 경로로 추가한 기능)을 덮어써서 되돌려버릴 위험이
   있음(실제로 2026-07-28에 `index.html`의 금액 슬라이더 기능을 이렇게 잃어버릴 뻔했다가
   병합으로 막음).

---

## ⚠️ 이 파일 자체가 저장소에 실제로 반영이 안 될 수 있음 (2026-07-23에 실제로 발생한 문제)

2026-07-23 세션 초반에 사용자가 "인수인계 파일을 못 가져왔다"며 이 문서를 채팅에 직접
업로드해서 시작한 적이 있음 — 확인해보니 이 통합본(HANDOFF.md)이 실제로는 저장소에 커밋된 적이
없고, 대신 옛날 `handoff-2026-07-17.md` / `handoff-2026-07-21.md` / `handoff20260722.md`
(그리고 인코딩이 깨진 한글 파일명 잔재 몇 개)만 저장소 루트에 그대로 남아있었음. 즉 이 문서를
"합쳤다"는 서술 자체는 과거 세션의 의도였지만 실제 반영(커밋+업로드)까지는 안 됐던 것.

**세션을 마칠 때 이 파일을 갱신했으면, 반드시 GitHub 파일 목록에도 실제로 `HANDOFF.md`라는
이름으로 업로드되어 있는지, 그리고 옛 `handoff-*.md` 파일들이 삭제됐는지 사용자에게 확인을
부탁할 것 — "커밋에 남겼다"만으로 끝내지 말 것(이 세션은 GitHub에 직접 push할 권한이 없어서
사용자가 웹 UI로 수동 업로드/삭제해야 실제로 반영됨, 아래 섹션 참고).**

---

## ⚠️ GitHub push가 항상 막혀있음 (구조적 제약, 매 세션 재확인 불필요)

이 세션(Claude Code)은 **GitHub에 직접 push할 권한이 없습니다** — `git push` 시도하면 항상
`403`(Anthropic GitHub App이 Authorize만 되고 Install은 안 된 상태로 이미 진단 완료된 알려진
이슈). GitHub API(`push_files`, `create_pull_request`)도 마찬가지로 막혀있음을 확인했습니다.
재진단하지 말고 아래 방식을 그대로 따르세요:

1. Claude가 로컬에서 파일을 고치고 로컬 git에는 커밋함
2. `SendUserFile`로 바뀐 파일(들)을 사용자에게 직접 전달 — 여러 개면 zip 하나로 묶어서 전달
3. **사용자가 GitHub 웹 UI에서 "파일 업로드"로 직접 덮어씀 (파일을 지워야 할 때는 사용자가
   GitHub 웹 UI에서 직접 삭제 — zip 업로드만으로는 삭제가 안 됨, 위 섹션 참고)**

**이 규칙은 2026-07-24에 설정된 자동 루틴("ChamTax 로또 데이터 점검"(목/일), "ChamTax 주간
사이트 헬스체크"(수))에도 그대로 적용됨** — 루틴이 뭔가 실제로 코드를 고쳤으면 예외 없이
그 즉시 zip으로 묶어서 전달할 것("다음 세션 때 같이" 식으로 미루지 말 것, 사용자가 2026-07-24
세션에서 명시적으로 요청한 사항). 순수 점검만 하고 아무것도 안 고쳤으면 zip 없이 짧은 보고
메시지만 보내면 됨.

커밋 서명 관련 훅 경고("Unverified")는 매번 뜨는데, 실제로는 커밋에 SSH 서명(`gpgsig`)이
정상적으로 들어가 있고, 단지 로컬 검증에 쓰이는 서명 공개키 파일이 비어있어서 로컬에서만
"N"(미검증)으로 표시되는 환경 설정 문제입니다. 이 브랜치를 push하지 않는 워크플로우라 실제
GitHub 표시에는 영향 없음. 해결 불가능/불필요한 표시상의 문제이니 매번 재진단하지 마세요.

**2026-07-26: 두 자동 루틴 모두 "매번 새 세션 생성" 방식 → "이 세션(대화)에 이어서 실행" 방식으로
전환함.** 원래는 `create_new_session_on_fire`로 매번 새 세션을 만드는 방식이었는데, 예정된
시각(로또 점검 루틴 기준 KST 9시)에 실제로 실행 기록(`last_fired_at`)은 남았지만 세션 목록에
아무것도 안 뜨거나, 수동으로 `fire_trigger`했을 때 세션이 생겼다가 곧바로 사라지는 문제가
사용자에 의해 발견됨(세션 목록 스크린샷으로 확인). 원인은 이 세션의 도구로는 더 진단할 수
없는 플랫폼 쪽 문제로 판단, 우회책으로 두 루틴(`trig_01Yb8Ekx7avsFfbYGybQz1Fb` 로또 점검,
`trig_016qaJDm3fD1zddsRH6vmfWg` 헬스체크 — **둘 다 삭제 후 재생성돼서 트리거 ID가 바뀌었음**,
최신 ID는 `list_triggers`로 확인)를 삭제하고 `persistent_session_id`를 이 세션에 고정해서
재생성함. 앞으로 이 두 루틴이 실행되면 새 세션이 아니라 **이 대화(세션)에 새 사용자 메시지로
그대로 이어서 들어옴**. 다음 세션에서 이 사실을 몰라서 "왜 로또 체크가 안 왔지"를 또 재진단하지
말 것 — 이 세션이 계속 살아있는 한 이 방식이 맞음. 만약 사용자가 "또 안 왔다"고 하면, 이번엔
세션 자체(이 대화)가 만료/종료됐을 가능성을 먼저 의심할 것.

**파일명 하이픈 버그**: 사용자가 GitHub 웹 "Upload files" 화면에 하이픈(`-`)이 든 파일을
드래그해서 올리면, **업로드 결과물에서 하이픈이 사라진 채로 저장소에 반영됨**(예:
`odds-data.js`를 올렸는데 실제로는 `oddsdata.js`라는 별개 파일로 커밋됨). 이미 저장소에
같은 이름(하이픈 포함)의 파일이 있어도 그 파일을 덮어쓰는 게 아니라, 하이픈 없는 새 파일이
따로 생겨버림 — 즉 "덮어쓰기"를 의도했어도 실제로는 업데이트가 반영되지 않고 구버전 파일이
그대로 라이브에 남는다는 뜻. **2026-07-23 세션에서 실제 사고로 확인됨**: `odds-data.js`
갱신본이 `oddsdata.js`로 잘못 올라가 있었고, 정작 라이브의 `odds-data.js`는 구버전(172KB,
최신은 354KB)인 채로 몇 주간 방치되어 사이트가 옛날 확률/잭팟 데이터를 계속 보여주고 있었음.
**대응**: (1) 새 파일을 만들 때는 하이픈 대신 언더스코어(`_`)를 기본으로 쓸 것. (2) 하이픈이
든 기존 파일(`odds-data.js` 등)을 업데이트해야 할 때는, 업로드 후 반드시 저장소 파일 목록에서
하이픈 없는 이름의 파일이 새로 생기지 않았는지, 그리고 원래 파일의 커밋 시각/크기가 실제로
바뀌었는지 확인할 것 — "업로드했다"만으로 반영됐다고 가정하지 말 것. 이미 라이브에 있는
기존 파일명(하이픈 포함)은 검색엔진 인덱싱 때문에 이름 자체를 바꾸지 말 것.

---

## 프로젝트 개요

- **이름**: 참택스 (ChamTax)
- **내용**: 미국 복권(파워볼·메가밀리언즈) 당첨금의 세후 실수령액 계산기. 세금 계산 기준을
  `COUNTRY_TAX_PROFILES`(script.js) 기준 **21개국** 중 고를 수 있음(미국은 주별 세율까지). 예전
  문서에 "4개국(한국/미국/중국/인도)"로 적혀있었는데 이미 오래전에 21개국으로 확장된 상태였음
  — 국가 수를 언급할 땐 문서 서술 말고 `COUNTRY_TAX_PROFILES` 배열의 실제 `code:` 항목 개수를
  셀 것. **2026-07-28 추가**: 21개국 목록에 없는 나라 방문자를 위한 `기타 국가` 옵션이 국가
  토글 맨 아래에 추가됨(`country === 'other'` 분기, `calcTakeHome()`) — 자국 세법 조사 없이도
  확정적인 미국 IRS 비거주자 원천징수 30%만 계산하고, 자국 추가세는 "❓ 확인 필요"로 정직하게
  표시(0원이라 단정하지 않음). 이 옵션은 `COUNTRY_TAX_PROFILES` 배열 안에는 없음(국가별 비교
  탭·지도·잭팟 랭킹처럼 "진짜 나라 비교"가 전제인 기능에는 안 넣었음) — 국가 수를 셀 때 이
  옵션까지 22번째 "국가"로 착각하지 말 것. **2026-07-28 후속 세션에서 노출 범위 확장**: 홈
  화면 세금 기준 토글에만 있던 걸, (1) 국가별 비교 탭의 지도(`#countryMapWrap`) 바로 아래에
  "목록에 없는 나라이신가요? → 기타 국가로 계산해보세요" 안내 버튼(`compare.mapOtherNote`,
  누르면 홈으로 이동+토글 자동 선택) (2) 국가별 비교 탭 카드 그리드(`updateSideBySide()`)와
  잭팟 히스토리 국가별 금액 목록(`renderAmountBreakdownHtml()`) 양쪽에 "🌐 기타 국가" 카드/칩을
  **정렬·그룹핑 로직 밖에서 항상 맨 끝에 고정**으로 추가함(순위 경쟁 대상이 아니라서). 지도
  자체(핀·국경선)는 여전히 21개국 전용으로 안 건드림 — "기타 국가"는 특정 좌표가 없어서 지도에
  올라가지 않음. 자세한 구현 위치는 아래 "작업 이력"의 해당 세션 항목 참고.
- **2026-07-28 추가**: 홈 화면 금액 입력 탭(일시불/연금액/희망액) 3개가 공용 통화 선택기
  (`#homeCurrencySelect`, `sharedInputCurrency`)를 공유함 — KRW/USD 포함 21개 통화 중 골라서
  세 탭 모두 그 통화 단위로 타이핑/표시 가능. 기본값 **KRW**(과거엔 일시불/연금액이 항상
  "Million USD" 고정이었음). 자세한 설계 근거·데이터 소스는 아래 "코드 아키텍처 핵심 패턴"의
  `sharedInputCurrency` 항목과 "작업 이력"의 해당 세션 참고.
- **호스팅**: GitHub Pages, 저장소 `semilee123456-ui/semilee123456-ui.github.io`
- **커스텀 도메인**: `chamtax.com` (Cloudflare Registrar에서 2026-07-23 구매, 원가 등록비
  ~$10.46/yr, 마크업 없음). Cloudflare 네임서버 + A레코드(GitHub Pages IP 4개, "DNS only"/회색
  구름) + `www` CNAME → `semilee123456-ui.github.io`. 저장소 Settings→Pages→Custom domain에
  연결 완료, "DNS check successful" + "Enforce HTTPS" 모두 초록 체크 확인함(2026-07-23).
  기존 `semilee123456-ui.github.io` 주소도 계속 작동하며 새 도메인으로 자동 리다이렉트됨.
- **빌드 도구 없음** — 순수 HTML/CSS/JS (i18n JSON 빌드용 Node 스크립트 하나만 예외)
- **대상**: 한국에 거주하는 40~60대가 주 타겟 — 접근성(최소 폰트 크기, 명암비) 기준이
  명시적으로 있음. 한국 거주 외국인(재외국민이 아닌, 한국에 사는 외국인)도 별도 페르소나로
  지원함 — FAQ 등에서 두 페르소나를 혼동하지 않도록 주의(아래 "대상 배지" 참고).
- **26개 언어 지원**(2026-07-28, pt/es/uk/tet 4개 신규 추가로 22개→26개): ko(기본, HTML에
  직접 한글로 박혀있음), en/zh/vi/th/ru(이상 5개는 `pickLang`의 위치 인자), 나머지 21개
  (km/ne/id/my/si/uz/mn/kk/ky/ur/bn/lo/ja/ar/hi/fr/tl/pt/es/uk/tet — `pickLang`의 `more` 객체
  인자로 처리, `ADDITIONAL_LANGS`에서 확인 가능). `i18n-source/translations.json`(정적 텍스트,
  574개 키) + `script.js` 안의 `pickLang()` 동적 문자열(인라인 호출 189개 + 공유 상수/함수
  58개) 양쪽 다 26개 언어 100% 채워진 상태 — 회귀 테스트는 여전히 "22개 언어"라고 적힌 옛
  주석/변수명이 코드 여기저기 남아있을 수 있는데(예: 테스트 파일 로그 문구), 실제 지원
  언어 수는 `ADDITIONAL_LANGS.length + 5`(위치인자)로 확인할 것 — 문서·주석 서술보다 코드가
  진실.

### 파일/폴더 구조
```
index.html          메인 SPA (홈/비교/확률체감/FAQ/개인정보/면책/문의 화면이 전부 .view로 존재)
styles.css           전체 스타일 (:root 디자인 토큰, 다크모드는 :root[data-theme="dark"])
script.js            전체 로직 (세금 계산 + 렌더링 + i18n — 매우 큰 파일, 1MB+)
odds-data.js         확률체감 탭 전용 대용량 데이터(역대 당첨번호·잭팟 아카이브), 그 탭 열 때만 지연 로드
manifest.json        PWA 매니페스트
sw.js                PWA 서비스 워커(2026-07-24 신규, index.html에서만 등록) — 네트워크 우선,
                     앱 셸만 캐싱, 자세한 설계 이유는 파일 상단 주석 + 아래 "작업 이력" 참고
sitemap.xml          SEO 사이트맵 (41개 랜딩 페이지 + index.html, 2026-07-23 감사 결과 정합성 문제 없음)
robots.txt           크롤링 허용 + sitemap 위치 명시, 정상

i18n/                언어별 번역 JSON 22개 — 빌드 산출물, 직접 고치지 말 것
i18n-source/translations.json   번역 원본(source of truth) — 여기를 수정
scripts/build-i18n.js           translations.json → i18n/*.json 생성 스크립트

tests/               회귀 테스트 스크립트 9개 (아래 "회귀 테스트" 섹션 참고, 2026-07-23에 6개
                     유실 확인·전부 재작성 완료)

*.html (그 외 41개)   언어별/국가별 SEO 랜딩 페이지 — 메인 계산기와 별개, 각자 자체 <style> 안에
                     자체 :root 색상/폰트 변수를 중복 정의(반복적 버그 원인이었음 — 새
                     디자인 토큰을 바꿀 때 이 페이지들도 같이 확인할 것). title/description/
                     hreflang/JSON-LD는 41개 전부 정상(2026-07-23 SEO 감사 확인) — 문제는
                     "알려진 미해결 항목"의 SEO 섹션 참고.
```

### 다국어(i18n) 작업 방법
1. `i18n-source/translations.json`에서 해당 키의 번역을 수정 (**`i18n/*.json`을 직접 고치지
   말 것** — 다음 빌드 때 덮어써짐). 큰 JSON 파일 전체를 `json.dump()`로 재직렬화하면 기존의
   수작업 포맷팅이 깨져서 거대한 diff가 남으므로, 바뀌는 부분만 타겟팅한 문자열 치환으로 수정할 것.
2. `node scripts/build-i18n.js` 실행 → `i18n/*.json` 22개 재생성
3. HTML 정적 텍스트: `data-i18n`/`data-i18n-html`/`data-i18n-placeholder`/`data-i18n-aria-label`
   속성 + `resolveI18n(key)`가 `i18n/{lang}.json`을 fetch해서 치환. **주의**: plain `data-i18n`은
   언어 전환마다 `el.textContent`로 통째로 덮어써지므로, 원문 안에 `<span>` 같은 마크업을 넣어도
   다음 전환 때 사라짐 — 마크업이 필요하면 `data-i18n-html`을 쓸 것(`tests/i18n_attr_lint.js`가
   이 오사용을 사전 경고함).
4. JS 동적 문자열: `pickLang(ko, en, zh, vi, th, ru, more)` — 6개는 위치 인자, 나머지 17개는
   `more` 객체. **`more`를 빠뜨리면 그 17개 언어에서 영어로 조용히 폴백됨** — 과거 세션에서
   발견된 버그의 상당수가 이 실수였음.
5. **새 `data-i18n*` 키를 추가했으면 반드시 `tests/i18n_coverage_audit.js`를 돌려서 22개 언어
   전부에 번역이 채워졌는지 확인할 것.** 2026-07-23에 FTC 체크리스트 관련 키 10개가 번역
   누락 상태로 몇 세션째 라이브에 떠 있던 걸 이 테스트를 처음 만들면서 발견함(21개 외국어에서
   그 부분만 한국어 원문이 그대로 노출되고 있었음) — 새 기능 추가 시 "22개 언어 번역함"이라고
   적어놓고 실제로는 빠뜨리는 실수가 반복되니, 서술이 아니라 이 테스트 통과 여부로 확인할 것.

---

## 코드 아키텍처 핵심 패턴

- **`sharedCountry`**: 'kr'/'us'/'cn'/'in'/... — 사용자가 고른 "세금 계산 기준". 홈 화면과
  국가비교 화면이 이 값을 공유함. `data-basis="kr"` 같은 속성으로 FAQ 항목을 세금 기준별로도
  필터링함(`filterFaq()`).
- **`currentLang`**: 'ko'/'en'/... — 표시 언어. localStorage에 저장 안 되고 새로고침하면
  'ko'로 리셋됨(`?lang=` URL 파라미터로 시작은 가능, 적용 후 URL에서 자동 제거).
- **`sharedInputCurrency`**(2026-07-28 신규): 'KRW'/'USD'/'CNY'/... — 홈 화면 금액 입력 탭
  (일시불/연금액/희망액) 3개가 공유하는 "표시 통화". `#homeCurrencySelect`(공용 선택기 하나,
  탭 버튼 줄 바로 위) 하나로 세 탭 전부 동시에 바뀜. 기본값 **KRW**(과거엔 일시불/연금액이
  항상 "Million USD"였음 — 이 기본값 변경이 이 세션의 핵심 판단, 아래 작업 이력 참고). 내부
  계산·슬라이더 값(`sharedAmountUsd`, `homeAmountSlider`의 `data-usd-min/max`·로그스케일 위치)은
  **항상 USD 기준으로 그대로 유지**되고, `sharedInputCurrency`는 순수히 "입력칸에 타이핑/표시
  되는 단위"만 바꾸는 표시 레이어임 — `usdMillionsToInputUnits()`/`inputUnitsToUsdMillions()`가
  이 변환을 전담(KRW는 "억원" 단위, 그 외는 "Million <코드>" 단위). 통화 전환 시 재표시는
  슬라이더의 반올림된 위치가 아니라 `sharedAmountUsd`/`sharedAnnouncedUsdMillions`(정확한 원본)
  에서 다시 계산해서, KRW↔다른 통화를 여러 번 왔다갔다 해도 드리프트가 안 생기게 함. 통화별
  symbol/flagEmoji/locale/실시간환율 메타데이터는 `CURRENCY_DISPLAY_META`(script.js,
  `CURRENCY_RATE_CONFIG` 근처)에 모아둠.
- **FAQ 대상 배지**: `data-basis`가 "어느 나라 세법을 적용받는지"만 구분하고 "당신이
  누구인지"(재외국민 vs 한국 거주 외국인)는 구분 못 함 — 특정 페르소나 전용 질문에는
  `faq-audience-badge` 클래스로 별도 배지를 붙임(예: "🌏 해외 거주 한국인 전용").
- **다크모드**: `:root[data-theme="dark"]`, `localStorage.getItem('theme')`,
  `toggleTheme()`/`syncThemeToggleIcon()`(script.js). `--teal`/`--status-red`/`--gold`/`--navy`는
  "다크모드에서 글자색으로 쓰일 때" 기준으로 튜닝되어 있어서, 이 색을 **배경**으로 쓰고 흰
  글자를 얹는 배지/버튼에는 하드코딩된 `color:#fff` 대신 `color:var(--on-accent)`를 쓸 것
  (라이트=흰색, 다크=잉크색으로 자동 전환됨).
- **접근성 폰트 floor**: `--fs-small:16px`가 명시적 최소값(시니어 가독성 연구 근거 주석 있음).
  랜딩페이지들은 자체 CSS 변수를 따로 가지고 있어 메인 앱과 별도로 확인 필요.
- **`COUNTRY_TAX_PROFILES`**: 국가별 세금 프로필. 새 국가 추가 시 여기 + `calcTakeHome()` 세율
  로직 + 국가 지도 좌표(`COUNTRY_MAP_COORDS`/SVG path, flagCode로 매칭) 3곳을 같이 갱신.
  국가 지도(`#countryMapWrap`)는 **"국가별 비교" 탭(view-compare)에만 있음**(홈 탭 아님 —
  테스트 짤 때 실수하기 쉬움, 아래 "테스트 짤 때 흔한 함정" 참고).
- **`formatWon(n)`**: 원화 금액을 언어별로 다르게 표시(억/조 vs million/billion). 비슷한 요구가
  생기면 이 함수의 분기 패턴을 참고.
- **`highlightCountryOnMap()`**(script.js): 국가별 비교 탭에서 카드를 탭하면 지도가 스크롤 밖에
  있을 때만 `scrollIntoView({block:'nearest'})`로 보이게 함. 이 스크롤 로직을 건드릴 일이 있으면
  `tests/map_scroll_audit.js`로 회귀 확인할 것.
- **비파괴적 non-breaking space 수정 패턴**: 한국어는 "단어(괄호)"처럼 공백 없이 붙여 쓰는
  경우가 많은데, 좁은 화면에서 이 지점이 그대로 줄바꿈되면 문장이 잘린 것처럼 보임. 고치는
  방법 2가지:
  - 원래 공백이 있어야 자연스러운 자리(예: 라벨과 괄호 사이)는 일반 공백을 NBSP(U+00A0,
    파이썬에서 `'\xa0'`로 명시적으로 써야 함 — 터미널에 직접 타이핑하면 전송 과정에서 일반
    공백으로 뭉개지는 경우가 있었음)로 바꿔서 그 지점에서 줄바꿈이 안 일어나게 함.
  - 원래도 공백이 없어야 자연스러운 자리(예: "주(State)에서"의 ")"와 "에서" 사이)는 NBSP로도
    막히지 않음(CJK 스크립트는 거의 모든 글자 경계에서 줄바꿈이 허용되기 때문) —
    `<span style="white-space:nowrap">...</span>`로 통째로 감싸야 함. 이 경우 `.textContent`가
    아니라 `.innerHTML`로 대입해야 마크업이 살아남음.
  - **주의(2026-07-23 발견)**: `display:flex; justify-content:space-between`인 컨테이너
    (예: `.calc-detail-summary`) 안에 이 nowrap `<span>`을 넣으면, 원래 텍스트가 "텍스트노드 +
    span + 텍스트노드" 3개의 flex item으로 쪼개져서 space-between이 그 사이 간격을 화면
    전체로 벌려버리는 별개의 버그가 생김(줄바꿈 문제가 아니라 flex 레이아웃 문제). 이런
    컨테이너에서는 nowrap span을 추가할 때 전체 텍스트를 감싸는 바깥 `<span>` 하나를 더 씌워서
    flex item 개수를 1개로 유지할 것.

---

## 테스트 짤 때 흔한 함정 (Playwright로 감사 스크립트 짤 때 참고)

1. **`getComputedStyle(el).display === 'none'`만으로는 조상 요소의 `display:none`을 못 잡음.**
   `display`는 상속되는 속성이 아니라서, 부모가 `display:none`이어도 자식 요소 자신의 계산된
   `display` 값은 그대로 `block` 등으로 나옴. **실제 렌더링 여부는
   `el.getClientRects().length === 0`로 확인할 것** — 조상 체인 어디서 `display:none`이 걸려도
   정확히 잡힘.
2. **어느 탭(view)에 있는지 먼저 확인할 것.** 예: 국가 지도는 `view-compare`에 있지 홈
   (`view-home`)에 없음. 엉뚱한 탭에서 요소를 찾으면 애초에 화면에 없어서
   `getBoundingClientRect()`가 전부 0을 반환하고, "이슈 없음"이 사실은 "아무것도 안 본" 거짓
   통과가 됨.
3. **0건이 나왔다고 끝내지 말고, 일부러 버그를 재현시켜서 탐지되는지 네거티브 컨트롤로
   확인할 것.** (코드를 임시로 한 줄 주석 처리 → 테스트 재실행 → 실패하는지 확인 → 원복). 이
   원칙 자체로 2026-07-23에 진짜 버그(FTC 번역 누락)를 하나 찾아냈음 — 테스트를 새로 만드는
   행위 자체가 버그 탐지로 이어진 사례.
4. **NBSP(U+00A0) 같은 non-ASCII 문자를 Python/JS 소스에 직접 타이핑하지 말 것.** 도구 호출을
   거치는 과정에서 일반 공백으로 뭉개지는 게 간헐적으로 관찰됨 — `'\xa0'`처럼 ASCII escape
   시퀀스로 명시할 것(파이썬은 소스 코드 안에서 이 시퀀스를 안정적으로 해석함).
5. **파일에 쓰는 문자열 치환은 `expected_count`를 지정해서 검증할 것.** 같은 문구가 눈에
   보이는 UI 텍스트뿐 아니라 `<script type="application/ld+json">` SEO 구조화 데이터 블록에도
   중복되어 있는 경우가 많아서(FAQ 스키마 등), 무심코 전체 치환하면 JSON-LD를 깨뜨림. 개수가
   안 맞으면 더 구체적인 앞뒤 문맥으로 앵커를 좁힐 것.
6. **Playwright 브라우저 컨텍스트의 기본 언어는 'ko'가 아닐 수 있음.** locale 설정 없이
   그냥 `page.goto()`만 하면 사이트가 영어로 뜨는 경우가 있었음 — 한국어 기준으로 테스트하려면
   `newPage({ locale: 'ko-KR' })` + `?lang=ko` 둘 다 명시할 것.
7. **이 실행 환경은 외부 인터넷이 막혀있어서(폰트 CDN·광고·환율 API 등) "Failed to load
   resource" 콘솔 에러가 항상 남.** `console_error_audit.js`는 이런 외부 리소스 실패를
   필터링하고 로컬(127.0.0.1) 리소스 실패나 진짜 JS 예외만 잡도록 만들어져 있음 — 비슷한
   테스트를 새로 만들 때 이 필터링을 빠뜨리면 매번 40개 안팎의 가짜 이슈가 뜬다.

---

## 회귀 테스트 (tests/, 11개 — 2026-07-23에 6개 유실 확인 후 전부 재작성, 이후 2개 추가)

```bash
python3 -m http.server 9000 &   # 저장소 루트에서
NODE_PATH=/opt/node22/lib/node_modules node tests/home_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/faq_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/audit_odds_compare.js
NODE_PATH=/opt/node22/lib/node_modules node tests/wrap_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/lang_leak_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/console_error_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/map_scroll_audit.js
NODE_PATH=/opt/node22/lib/node_modules node tests/nav_slider_audit.js
node tests/i18n_coverage_audit.js         # 브라우저 불필요
node tests/i18n_attr_lint.js              # 브라우저 불필요
node tests/broken_link_audit.js           # 브라우저 불필요
node tests/draw_archive_integrity_check.js   # 브라우저 불필요, 로또 아카이브 날짜순·중복·형식 검증
```
Playwright 크로미움 경로: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`
(이미 설치되어 있음, `playwright install` 하면 안 됨). 전부 마지막 줄에 `ISSUES: 0`(또는
`TOTAL ISSUES: 0`)이어야 정상. `i18n_attr_lint.js`는 한때 "8개 검토 후보가 정상"이라고
적혀있었으나 2026-07-23에 재확인해보니 이미 0건이었음(다른 세션이 고쳐놓고 이 표만 안 고친
사례) — 지금은 이 파일도 다른 테스트와 똑같이 `ISSUES: 0`이 정상임.

| 파일 | 확인하는 것 |
|---|---|
| `home_audit.js` | 여러 뷰포트에서 텍스트가 카드 밖으로 넘치는지 |
| `faq_audit.js` | FAQ 언어·기준 조합 레이아웃 |
| `audit_odds_compare.js` | 확률체감/비교 탭 |
| `wrap_audit.js` | 실제 렌더링 기준으로, 단어+괄호/%가 공백 없이 붙어있다 그 지점에서 줄이 갈리는지 Range.getClientRects()로 확인 |
| `i18n_coverage_audit.js` | `data-i18n*` 키가 `i18n-source/translations.json`에 없거나 일부 언어만 채워졌는지, 빌드 산출물(`i18n/*.json`)에도 반영됐는지 |
| `i18n_attr_lint.js` | plain `data-i18n`인데 한글 원문에 괄호/%가 붙어있어 `data-i18n-html` 전환이 필요할 수 있는 후보 사전경고(버그 확정 아님, 지금은 0건이 정상) |
| `broken_link_audit.js` | 로컬 href/src 참조 중 실제로 없는 파일 |
| `lang_leak_audit.js` | 다른 언어 선택했는데 한글 3자 이상 연속으로 남아있는 경우(언어 혼용) |
| `console_error_audit.js` | 탭×언어 로드 시 JS 콘솔 에러(외부 리소스 실패는 필터링됨) |
| `map_scroll_audit.js` | 국가 카드 탭 시 지도가 화면 밖에 남아있는 회귀 |
| `nav_slider_audit.js` | (신규, 2026-07-23) nav 원형 설정 버튼이 pill 3개와 같은 줄에 있는지, 금액 슬라이더(로그 스케일)가 단조 증가하는지·주요 배수에 도달 가능한지·타겟값 근처에서 흔들리지 않는지 |
| `draw_archive_integrity_check.js` | 파워볼·메가밀리언즈 당첨번호 아카이브의 날짜순 정렬·중복·형식 오류 (`minCount`/`maxFirstDate` 기준값은 백필 진행할 때마다 갱신할 것) |

---

## 알려진 미해결 항목

- **`formatWon()`이 "억원" 단위로만 반올림해서, 0.5억원(5,000만원) 미만 금액은 화면 헤드라인에
  항상 "0억원"으로 보임 (2026-07-28 열아홉 번째 세션에서 발견, 손대지 않고 문서화만 함)**:
  홈 화면 금액 입력의 "만원~수천만원" 소액 지원(같은 세션에서 수정)을 검증하다가 발견 —
  입력칸·슬라이더·`home-final-amt`의 `dataset.eokVal`(실수령액 원본 숫자)은 전부 정확한데도,
  화면에 실제로 찍히는 "입력하신 금액"/"실수령액" 헤드라인 텍스트는 `formatWon()`이
  `Math.round(n)`(억원 단위 정수 반올림)만 하도록 짜여있어서 이 구간 대부분에서 "0억원"으로
  보임. `formatWon()`은 홈/비교/공유카드/PNG 저장 등 여러 화면이 공유하는 핵심 함수라 이번
  세션 범위 밖으로 남김 — 다음에 사용자가 "왜 아직도 0으로 나오냐"고 물으면, 이건 방금 고친
  음성/슬라이더 버그의 재발이 아니라 `formatWon()` 자체의 "억 단위 미만은 표시 못 함"이라는
  기존 설계 한계임을 먼저 확인할 것(예: 억 미만도 "만원" 단위까지 보여주는 분기를 추가하는
  방향으로 고칠 수 있으나, 여러 화면 회귀 위험이 있어 신중하게 접근 필요).
- ~~**다음 세션 시작하면 제일 먼저 확인할 것 — zip 업로드가 실제로 반영됐는지**~~ — **2026-07-23
  일곱 번째 세션(`claude/new-session-folfs5`)에서 확인 완료: zip의 모든 파일이 `origin/main`에
  바이트 단위로 이미 정확히 반영돼있었음**(`diff -rq` 비교, "differ" 0건). `tests/` 10개 전부,
  `i18n-source/translations.json`, `scripts/build-i18n.js`, `odds-data.js`(정상본), `CNAME`
  모두 확인됨. 사용자가 GitHub 웹 UI 업로드를 실제로 완료한 상태였음 — 더 이상 재확인 불필요.
- **SEO 감사 후속 조치 — 2026-07-23 일곱 번째 세션에서 고아 페이지 문제 해결함**:
  - ~~랜딩 페이지 41개 중 26개가 고아 페이지~~ **해결됨**: `korea-resident-us-lottery-tax.html`
    허브 페이지의 hreflang 26개 언어 전체를 화면에 보이는 `.lang-links` 링크 목록으로 변환(기존엔
    베트남어 1개만 노출). 별도로 발견된 `lottery-jackpot-amount.html`(en/zh 미노출)도 같은 패턴이라
    같이 수정. 수정 후 전체 41개 랜딩 페이지를 "다른 페이지에서 최소 1개 이상 링크로 연결되는지"
    스크립트로 재검사 → 구글 인증용 `google45a28cb010bb349d.html`(원래 링크 없는 게 정상) 제외
    전부 통과. `tests/broken_link_audit.js`로 새 링크 44개 파일 기준 이슈 0건 확인.
  - index.html(메인 계산기 SPA)은 크롤러에게 항상 한국어 메타데이터만 보임(언어 전환이 JS로만
    일어나서) — 구조적 문제라 완전히 고치려면 서버사이드 렌더링이 필요함. 지금 전략(랜딩
    페이지가 다국어 SEO를 전담)은 나쁘지 않으니 급하지 않음, 다만 위 고아 페이지 문제를 먼저
    풀어야 그 전략이 실제로 작동함.
  - ~~일부 랜딩 페이지 `<title>`이 너무 김(스리랑카 222자, 미얀마 191자 등)~~ — **2026-07-23
    일곱 번째 세션에서 해결**: 17개 언어(러시아·우크라이나·아랍·일본·힌디·태국·베트남은 이
    세션이 초안 작성 후 검수 요청, 나머지 신할라·미얀마·벵골·라오·크메르·몽골·우르두·키르기스·
    카자흐·우즈베크는 저자원 언어라 초안 없이 제미나이에게 통째로 맡김)를 단축·검수받아 반영함.
    네팔어는 이미 충분히 짧다는 검수 의견에 따라 그대로 둠. `<title>`/`og:title`/
    `twitter:title`만 바꿨고 `<h1>` 등 본문은 그대로임. (참고: 유니코드 코드포인트 기준 글자수는
    태국어·미얀마어 등 결합문자 많은 스크립트에서 실제 화면 길이보다 부풀려져 보이므로, 이
    수치만으로 "안 줄었다"고 오판하지 말 것 — 실제 검수는 원어민 자연스러움 기준으로 했음.)
  - 41개 랜딩 페이지가 og:image를 하나만 공유(한국어 브랜드 이미지) — 언어별로 다르게 만들면
    더 좋겠지만 사소함.
  - sitemap.xml/robots.txt/hreflang/JSON-LD 구조화 데이터/중복 콘텐츠는 감사 결과 문제 없음.
- **로또 번호 백필 — 2026-07-23 여섯 번째 세션에서 사실상 완결**: 공식 출처(data.ny.gov 공식
  CSV + 교차검증된 GitHub 데이터셋 1개)만 써서 파워볼을 최초 추첨일(1992-04-22)까지, 메가밀리언즈를
  전신 "The Big Game" 출범 시점(2002-05-17)까지 완전히 채움. 최종: 파워볼 3,827건, 메가밀리언즈
  2,520건, `tests/draw_archive_integrity_check.js`(신규, Node 전용)로 날짜순·중복·형식 검증
  통과분만 반영. 새 회차는 자동으로 갱신되지 않으니, 다음에 세션이 열리면 그동안 나온 새
  회차를 이 스크립트로 검증 후 반영할 것. **2026-07-24 열 번째 세션에서 갭 재발견**: 아카이브
  마지막 회차가 파워볼 2026-07-20/메가밀리언즈 2026-07-21로 멈춰있어서, 최소 수요일(07-22)
  파워볼 1회차가 비어있을 가능성이 높음(추첨 요일 기준). 이 세션은 `data.ny.gov`가 네트워크
  정책으로 막혀있는 걸 확인했고, 대체 출처를 찾던 중 API 사용 한도로 중단됨(위 "API 월간
  사용 한도" 항목 참고). **메인 세션이 한도 회복 후 직접 재시도함**: `data.ny.gov`/뉴스 기사
  원문(abc10·Yahoo)/`powerball.com` API 전부 403으로 재확인. `WebSearch`는 됐지만 반환된
  요약 답변에 구체적 당첨번호가 담겨있었는데, 검색 결과 목록 자체에 2024년·2026년 기사가
  섞여 있어서(이 항목 위쪽에 기록된 "다른 연도 데이터 혼입" 사고와 같은 위험 신호) 원문
  대조 없이는 신뢰 안 하기로 하고 폐기함 — 원문 조회 자체도 403이라 대조 불가. GitHub raw
  콘텐츠는 막혀있지 않다는 것만 확인(임의로 찍은 경로라 404, 실제 막힌 건 아님)했지만
  검증된 데이터셋 URL을 몰라서 더 진행 못 함. **결론: 이번 세션은 신뢰 가능한 출처로 검증
  못 해서 `odds-data.js` 그대로 유지 — 다음 세션에 이월**(다른 네트워크 정책의 세션이거나,
  사용자가 공식 CSV를 직접 구해서 전달하는 방법이 가장 안전함).
- **2026-07-24 이어서**: 사용자가 스크린샷으로 라이브 사이트 결과 페이지(`파워볼 과거 결과`,
  usamega류 집계 사이트로 추정)를 전달함 — 7/18·7/20 항목이 이미 검증된 아카이브 데이터와
  숫자·잭팟액까지 정확히 일치해서(2중 교차확인) 신뢰하고, **7/22 회차(4,5,22,50,58 / 파워볼
  1 / $569.8M)를 `POWERBALL_DRAW_ARCHIVE`+`POWERBALL_JACKPOT_ARCHIVE` 양쪽에 추가·
  `draw_archive_integrity_check.js` 통과·`odds-data.js` 캐시버스팅 버전(`?v=20260724`)
  올림**. 커밋 `dfa6fed`.
  - **잭팟 인덱스 위젯 3개의 자동 반영 여부를 사용자가 물어봐서 코드로 확인**: 🏆 실수령액
    랭킹(`getJackpotRealTakeHomeRanking`→`getCombinedJackpotHistory`)은 큐레이션 목록
    (`JACKPOT_HISTORY`)+전체 아카이브를 합쳐서 쓰므로 새 회차 자동 반영됨(단, TOP10 안에
    들어올 만큼 큰 금액이어야 실제로 보임). 🚀 이월 스트릭(`getTopRolloverStreaks`)은 전체
    아카이브만 순수 계산하므로 완전 자동 반영. 📈 물가보정 랭킹(`getJackpotRealValueRanking`)은
    **큐레이션 목록만 사용**(전체 아카이브 안 봄) — 이건 설계 의도(평범한 회차는 애초에
    이 랭킹 대상이 아님)라 새 회차가 자동 반영 안 되는 게 정상. 앞으로 백필할 때마다 이
    표를 참고해서 "왜 저 위젯엔 안 뜨지?" 재검증 반복하지 말 것.
  - 이 백필 데이터는 22개 언어가 전부 공유하는 단일 배열(`odds-data.js`)이라 언어별 추가
    작업 불필요 — 화면 라벨만 언어별로 씌워지는 구조.
  - **"사이트 전반에 다 반영되는지" 사용자가 재확인 요청 → 실제로 안 되는 곳 하나 발견**:
    홈 화면 "🎱 최근 당첨번호" 위젯은 `POWERBALL_DRAW_ARCHIVE`/`MEGAMILLIONS_DRAW_ARCHIVE`와
    **전혀 무관한, 별도로 손으로 관리하는 하드코딩 값**(`LATEST_DRAW`/`JACKPOT_DATA`, script.js
    2638~2660줄 근처, "재미 요소 + 공유 유도용" 장식 위젯이라 계산기 본질 기능과는 무관하다고
    코드 주석에 명시돼있음)이라 아카이브 백필과 자동 연동 안 됨 — Playwright로 라이브 렌더링
    직접 확인해서 발견함(코드만 읽었으면 놓쳤을 것). `LATEST_DRAW`가 파워볼 7/18·메가밀리언즈
    7/17 값으로 멈춰있던 걸 발견해서, 파워볼은 오늘 백필한 7/22 검증값으로, 메가밀리언즈는
    이미 아카이브에 있던(이번 세션 이전부터 있던) 7/21 값으로 동기화함(둘 다 이미 검증된
    내부 데이터를 그대로 복사한 것이라 새 외부 조회 불필요). **`JACKPOT_DATA`(다음 추첨
    광고 잭팟액, "🎟️ 오늘 잭팟 수동 업데이트 존")는 손 안 댐** — 지금 파워볼 5억 4,400만
    달러로 하드코딩돼있는데, 이게 7/25(토) 추첨 기준 현재 광고 잭팟액이 맞는지 확인할
    근거가 없어서(7/22가 당첨자 나온 회차인지 이월인지도 모름) 추측 안 함. **다음 세션/
    사용자가 공식 사이트 보고 30초 안에 고칠 수 있는 부분**(코드 주석에 이미 그렇게 안내돼
    있음) — 이 위젯 자체가 원래 "갱신 깜빡해도 계산기 기능엔 영향 없음"이라고 설계돼있어서
    급하지 않음.
  - **`JACKPOT_DATA`를 실제로 쓰는 화면 전부를 grep으로 전수 조사함**(사용자가 "당첨금을
    직접 입력해보세요" 퀵필 버튼 화면을 스크린샷으로 보여주며 "여기도 있다"고 지적 →
    확인해보니 같은 원인의 다른 화면이었음, 그래서 나머지도 전부 찾음). **좋은 소식: 딱
    한 군데(`JACKPOT_DATA`, script.js 2641~2644줄)만 고치면 아래 전부 자동으로 같이
    갱신되는 구조**임 — 화면별로 따로따로 고칠 필요 없음. `JACKPOT_DATA`를 참조하는
    화면 4곳:
    1. **홈 화면 상단 파워볼/메가밀리언즈 카운트업 카드**(`#jp-powerball`/`#jp-mega`,
       `applyJackpotData()`가 `data-target` 속성에 주입) — 사이트 접속 시 가장 먼저 보이는
       큰 숫자 애니메이션.
    2. **"당첨금을 직접 입력해보세요" 퀵필 버튼**(`#quickfill-pb-amt`/`#quickfill-mm-amt`,
       사용자가 스크린샷으로 보여준 화면) — `JACKPOT_DATA.amountUsd × CASH_VALUE_RATIO(58%)`.
    3. **`.prize-amt`("일시불 기준" 금액) 카드**(`#jackpot-card-amt`) — `getJackpotKRW()`
       경유(단, 이 함수는 파워볼 값만 읽음 — 메가밀리언즈 전용 표시는 없는 것으로 보임,
       구조 확인만 하고 손은 안 댐).
    4. **"눌러서 세후 실수령액 보기" 드로어**(`#jackpot-calc-box` 안의 `#jc-jackpot`
       발표잭팟/`#jc-cash` 일시불세전/`#jc-final` 세후실수령 3단계) — 역시 `getJackpotKRW()`
       경유, 파워볼 기준.
    - **결론**: `JACKPOT_DATA.powerball.amountUsd`/`JACKPOT_DATA.megamillions.amountUsd`
      두 숫자만 공식 사이트(powerball.com/megamillions.com) 보고 갱신하면 위 4곳 전부
      자동으로 맞게 반영됨. 이번 세션은 검증된 최신 광고 잭팟액을 몰라서 손 안 댐 —
      다음에 확인되는 대로 이 두 숫자만 고치면 됨.
- ~~`i18n_attr_lint.js`가 찾은 8개 후보~~ — **2026-07-23 일곱 번째 세션에서 재실행해보니 이미
  0건으로 해결돼있었음**(여섯 번째 세션이 `data-i18n-html`+nowrap span 전환으로 이미 고쳤는데,
  이 "미해결 항목" 섹션 갱신을 빠뜨렸던 것 — 문서 서술 말고 테스트 결과를 믿으라는 원칙 자체를
  이 섹션에서 어긴 사례. 앞으로 이 실수를 반복하지 않도록, 세션 종료 전에 이 섹션의 각 항목이
  최신 테스트 결과와 여전히 일치하는지 한 번씩 재확인할 것).
- ~~`wrap_audit.js`가 찾은 실제 렌더링 이슈 1건 (영어 "(FTC applied, approximate)" 줄바꿈)~~ —
  **2026-07-23 일곱 번째 세션에서 재현 안 됨을 확인**: Playwright로 영어 비교 탭 360~375px
  폭에서 직접 재검증했으나 wrap 안 됨. 애초에 이 텍스트는 `.side-group-label`이 아니라
  `.side-breakdown-label`에서 렌더링됨(문서 서술이 클래스명부터 부정확했음) — 다른 세션에서
  이미 고쳐지고 이 항목만 미갱신 상태로 남아있었던 것으로 보임. 실제 존재 여부보다 문서 서술을
  믿었으면 계속 헛다리 짚을 뻔한 사례.
- **`odds.howtoText`** 등 일부 콘텐츠가 6개 핵심 언어(ko/en/zh/vi/th/ru)만 지원한다고 예전
  문서에 적혀있었으나, 2026-07-23에 확인해보니 이미 22개 언어 전부 채워져 있었음(과거 세션에서
  해결된 듯) — 혹시 비슷하게 "일부 언어만 지원"이라고 알려진 다른 키가 있으면
  `i18n_coverage_audit.js`로 실제 상태부터 확인할 것(문서 서술보다 테스트 결과를 믿을 것).
- ~~**랜딩페이지 aria-label**(`LANGUAGE_CONTENT_PAGES`) 약 23개 항목이 영어/한국어 폴백 상태~~
  — **2026-07-28 해결**: 26개 항목 중 19개는 `buildCountryMore()`를 재사용하고 있어서 그 함수에
  pt/es/uk/tet를 채우자 자동으로 같이 해결됐고, "~언어권이라면" 커스텀 문구를 쓰는 나머지 7개
  (아랍어/프랑스어/스페인어/포르투갈어/대만·홍콩/동티모르/우크라이나)는 직접 pt/es/uk/tet를
  추가함. 26개 항목 전부 26개 언어로 aria-label 확인 완료(Playwright로 각 언어별 실제 렌더링
  텍스트 검증함, 빈 문자열이나 3자 미만 없음).
- ~~**미국 주(State) 이름은 모든 언어에서 영어 그대로**~~ — **문서 서술이 실제 코드와 어긋나
  있었음(2026-07-28 발견)**: `STATE_DISPLAY_NAMES_MORE`를 실제로 열어보니 이미 17개 언어
  (km/ne/id/my/si/uz/mn/kk/ky/ur/bn/lo/ja/ar/hi/fr/tl)가 주 이름을 자기 문자로 옮겨서(예:
  크메르어로 "아칸소" 표기) 다 채워져 있었고 실제로 `buildStateNameMore()`를 통해 3곳
  (`stateSuffix`)에서 쓰이고 있었음 — "영어 그대로 두기로 결정함"이 실제로 지켜진 적이
  없었던 것으로 보임. 이 세션에서 나머지 pt/es/uk/tet 51개(DC 포함) 주 이름도 채워서
  21개 언어 전부 완비함(Nueva York/Nova York 같은 언어별 관용 표기 사용, 나머지 4개
  핵심 위치인자 언어(en/zh/vi/th/ru)와 ko는 원래부터 영어/한자 표기 유지 — 이건 의도된
  설계). 다시 "영어 그대로가 맞다"고 되돌리지 말 것.
- **국가 지도는 실제 국경선 기반**(Natural Earth)이지만 정밀 지도는 아님 — 참고용 시각화
  목적, 정확한 지리 정보로 오인되지 않게 하는 게 의도.
- **"문의하기" 기능은 삭제하지 않기로 최종 결정함(2026-07-23)**. 사용자가 관리 부담 때문에
  삭제/게시판 전환/이메일만 남기기를 각각 검토했으나, 개인정보처리방침의 "개인정보 보호책임자
  연락처" 조항이 법적으로 연락 수단을 요구하는 점 등을 확인한 뒤 결국 지금 상태(Formspree 폼)
  유지로 결론남. 다시 꺼내지 말 것 — 이미 논의 끝난 사안임.
- ~~**`chamtax.com` 도메인 → Google/Naver/Daum/Bing 검색엔진 등록**~~ — **완료(2026-07-24,
  사용자 확인)**: Google Search Console(도메인 속성 + sitemap 제출), 네이버 서치어드바이저,
  Bing Webmaster Tools, 다음(Daum) 웹마스터도구 **4곳 전부 사용자가 직접 소유확인·등록
  완료함**(대시보드 작업이라 Claude는 코드 쪽 준비만 했음 — Google Search Console 도메인
  속성/sitemap 제출, `index.html`의 네이버 인증 메타태그, `robots.txt`의 다음 PIN 인증 태그,
  `sitemap.xml`/랜딩페이지 43개 `canonical`/`og:url`/JSON-LD `item`의 `chamtax.com` 치환은
  전부 2026-07-23에 이미 반영돼있었음). **더 이상 확인·재점검 불필요, 이 항목 마무리.**
- ~~**구글 애드센스에 `chamtax.com` 도메인 추가 여부 미확인**~~ — **완료(2026-07-24, 사용자
  확인)**: 애드센스 대시보드에 새 도메인을 사용자가 직접 추가함(대시보드 작업, Claude 권한
  밖). 이 항목도 마무리.
- **🏆 ChamTax 잭팟 인덱스 (2026-07-24 신규, MVP 단계)**: 이월(Rollover) 스트릭 TOP 10 + 물가보정
  (CPI-U) 실질가치 랭킹, 확률체감 탭의 실수령액 랭킹 위젯 바로 아래에 있음. 자세한 기획 배경/
  코드 위치는 아래 "작업 이력"의 해당 세션 항목 참고. **남은 것(의도적으로 미룬 것, 다음 반응
  보고 진행)**: (1) GitHub Actions로 신규 회차 자동 반영 — 지금은 수동 백필 방식 그대로라 새
  회차가 나와도 이 인덱스에 자동으로 안 잡힘. (2) 결과 캡처 공유카드 — 지금은 텍스트 랭킹만
  있고 SNS 공유용 이미지 카드는 없음. (3) CPI_BASE_YEAR(현재 2024)를 최신 연도로 갱신 —
  2025년 연평균 CPI-U가 공식 확정되면 `US_CPI_ANNUAL`에 추가하고 갱신할 것(근거 없이 갱신
  금지, 확정 전까지는 지금처럼 자동 제외되는 게 맞음).
- **Cloudflare Auto Minify + Brotli 켜기 (2026-07-24 제안, 사용자가 "지금은 못 하겠다"며
  보류 — 사용자가 Cloudflare 대시보드에서 직접 해야 함, Claude가 대신 실행할 권한 없음)**:
  사이트 무게를 더 줄이고 싶다는 요청에 대한 답으로 제안함. `script.js`(1.2MB)/`styles.css`
  (125KB)를 실제로 minify해서 배포하는 방식은 "빌드 도구 없음" 원칙과 충돌하고, 이
  프로젝트가 반복해온 "재빌드/재배포 누락" 실패 패턴을 하나 더 만들 위험이 있어서
  일부러 안 함(위 "이미지 압축" 항목 근처의 판단 이유와 동일) — 대신 **소스는 하나도
  안 건드리고 엣지에서 자동 압축**해주는 이 방법을 대안으로 제안한 것.
  - **할 일**(Cloudflare 대시보드, `chamtax.com` 도메인):
    1. DNS 레코드의 구름 아이콘을 회색(DNS only) → **주황색(Proxied)**으로 전환
    2. SSL/TLS 모드를 **"Full"**로 설정(GitHub Pages와 궁합 확인됨 — "Flexible"로 하면
       리다이렉트 루프 위험 있으니 피할 것)
    3. Speed → Optimization에서 **Auto Minify**(HTML/CSS/JS 체크) + **Brotli** 켜기
  - **효과**: gzip보다 15~20% 추가 압축(Brotli) + 엣지 캐싱, 소스 코드/배포 워크플로우
    변경 전혀 없음.
  - **주의**: DNS/SSL 쪽 변경이라 되돌리기는 쉽지만(주황 구름 → 다시 회색으로), 적용 직후
    사이트가 이상해 보이면 즉시 되돌릴 것. 다음 세션은 이 항목이 아직 안 됐는지부터
    사용자에게 물어볼 것 — 이미 했다면 이 항목은 지울 것.
- **"외국인 검색 노출, Claude가 할 수 있는 건 다 해달라"는 요청 처리 (2026-07-24)**: 네이버/
  다음/Bing 소유확인 통과 여부는 대시보드 로그인이 필요해서 제외하고, 코드로 할 수 있는 것만
  점검함.
  - **`index.html`(메인 계산기) hreflang이 3개 언어(ko/en/zh)+x-default만 있던 걸 발견·수정**:
    사이트는 22개 언어를 지원하는데(`ADDITIONAL_LANGS` 17개 + 위치인자 6개, `ko` 제외),
    메인 페이지 hreflang은 그중 2개만 크롤러에게 알려주고 있었음 — 나머지 19개 언어는
    "이 페이지의 다른 언어판이 있다"는 신호 자체가 안 나가던 상태. 22개 언어 전부(`?lang=xx`)
    hreflang alternate 링크 추가, 추가 전에 각 언어 코드가 `script.js`의 URL 파라미터
    처리 로직(`SUPPORTED_LANGS.includes(urlLang)`)에서 실제로 유효한 진입점인지 코드로
    확인 후 반영. `og:locale:alternate` 태그도 주요 언어 9개 추가(페이스북 등 소셜 공유용
    부가 신호).
  - **점검했지만 문제 없어서 그대로 둔 것들**: 랜딩페이지 41개 `<img>` alt 속성(전부 있음,
    0건 누락), sitemap.xml 구조(hreflang 어노테이션 없이도 이미 페이지 자체 hreflang으로
    커버되므로 추가 불필요).
  - **검토는 했지만 실행 안 한 것**: 언어별 og:image(지금은 21개 언어 전부 한국어 브랜드
    이미지 하나만 공유) — 새로 만들려면 이 실행 환경에 크메르어·미얀마어 등 일부 스크립트를
    제대로 그릴 폰트가 없다는 걸 이전 세션에서 이미 확인한 적 있어서(2026-07-24 여덟 번째
    세션 기록 참고), 잘못 렌더링된 이미지가 라이브에 올라갈 위험이 검색 노출 이득보다 커
    보여 보류함. 원하면 핵심 몇 개 언어(영어/중국어/베트남어 등 라틴/한자 계열, 폰트 위험
    낮은 언어)만 시범적으로 만들어볼 수 있음 — 필요하면 요청할 것.

---

## 작업 이력 (날짜순, 세션마다 맨 아래에 새 항목 추가)

이보다 오래된 세션 기록은 `HANDOFF-ARCHIVE.md` 참고(특정 과거 이슈의 배경이 필요할 때만 검색, 매 세션 필독 아님).

### 2026-07-28 (열일곱 번째 세션, `claude/progress-checkpoint-vd08p1` — 서브에이전트로 진행)

**요청 배경**: 사용자가 홈 결과 카드 "이미지로 저장"(`saveHomeResultAsImage()`) 스크린샷을 보여
주며, 지금은 그냥 밋밋한 데이터 카드인데 진짜 복권 당첨 티켓처럼 "들고다니고 싶은" 느낌으로
만들 수 있는지 물어봄. 이 사이트엔 이미 같은 톤의 선례(`saveMyNumbersAsTicketImage()`, 내가
고른 번호를 "가상 복권 티켓"으로 저장하는 기능 — 번호 공, 장식용 바코드, 빨간 테두리 큰
"가상 티켓·실제 구매 아님" 배지)가 있어서, 그 시각 언어를 결과 카드에도 입히는 방향으로 진행.

**리디자인 핵심**: "데이터는 그대로, 배치만 축제 티켓처럼" — 기존에 보여주던 값(로고, 기준 한
줄, 결과 라벨, 큰 금액, 실수령/세금 비율 막대, before→after 줄, 참고용 배지, 날짜) 중 뺀 건
없음, 순서와 장식만 바꿈.
- **금색 바깥 테두리 + 절취선**: 캔버스 전체를 은은한 금색 그라데이션(`--grad-amber` 계열)으로
  깔고, 안쪽에 기존 크림색(`#FFFEF9`) 카드를 얹은 뒤, 본문(로고~before/after 줄)과 하단
  "스텁"(참고용 배지+발행일+바코드) 사이에 진짜 티켓처럼 좌우 펀칭 노치 2개 + 점선을 그림
  (`drawTicketPerforation()` 신규 헬퍼) — 노치는 카드 바깥 배경색으로 채워 "베어 문" 자국처럼
  보이게 함(실제 알파 투명 구멍은 안 씀 — 다운로드된 PNG가 어떤 배경 위에서 열리든 항상 똑같이
  보이게 하려고 일부러 불투명 트릭으로 처리).
- **결과 라벨을 금색 리본 배지로**: 사이트 기존 `--gold-chip-bg`/`--gold-chip-border` 톤을 그대로
  재사용한 알약 모양 배지 안에 기존 결과 라벨 텍스트 + "🎉" 이모지(언어 무관 장식, 새 번역
  불필요)를 넣음. 글자색은 원래 잉크색(#262420) 그대로라 명암비 훼손 없음.
- **축하 별 장식**: 로고 반대쪽 모서리에 작은 5각 별 3개(`drawTicketStar()` 신규 헬퍼, 순수
  도형·텍스트 없음).
- **바코드 헬퍼 공유화**: `saveMyNumbersAsTicketImage()`에 있던 "seed 기반 의사난수 세로줄
  바코드" 로직을 `drawDecorativeBarcode(ctx, x0, x1, y, h, seed, color)`로 분리해서 두 함수
  모두 이 헬퍼를 호출하게 함(요청사항의 "중복 코드 헬퍼로 분리" 반영). 결과 카드 쪽 seed는
  기준+금액 표시 문자열에서 파생시켜, 같은 결과를 다시 저장해도 같은 패턴이 나오게 함.

**안전장치(요청의 "실제 당첨/공식 서류로 오인되면 안 됨" 원칙 그대로 적용)**:
- **새 텍스트를 하나도 안 늘림**(의도적 선택) — "WINNER"/"당첨" 류의 새 문구나 정부·복권사·
  국세청을 연상시키는 문양은 아예 안 넣고, 장식은 전부 도형(별·리본 배경·절취선)과 이모지
  (🎉, 언어 무관)로만 구성함. 그래서 이번 리디자인엔 새 `pickLang()`/`_MORE` 상수가 하나도
  없음 — 26개 언어 번역 작업 자체가 필요 없었음(스코프를 줄이는 의도적 설계 판단이었지, 빠뜨린
  게 아님, 아래 검증 항목 참고).
- **참고용 배지(`RESULT_IMG_DISCLAIMER_MORE`, 기존 그대로 재사용)는 절취선 아래 스텁 구역 맨
  위**에 두되 카드 바닥까지 약 100px 여유를 둬서 어떤 레이아웃 변화에도 안 잘리게 함. 크기·
  굵기·색(경고 의미의 빨강) 전부 기존 그대로 — 장식 목적으로도 줄이거나 흐리게 하지 않음.
- 실수령액 큰 숫자(58px/800) 등 실제 숫자류 폰트 크기·굵기는 전부 기존 그대로 유지(시니어
  가독성 튜닝값 보존).

**실제로 잡은 버그(구현 중 Playwright 스크린샷으로 발견)**: 아랍어(RTL)에서 "🎉 " + 결과
라벨(아랍어)을 합친 새 리본 문자열이, 캔버스 `ctx.direction` 기본값이 'ltr'이라 이모지가
왼쪽 끝에 고정되고 아랍어 문장은 그 오른쪽에 따라붙는 어색한 순서로 나옴(문장이 실제로
시작하는 오른쪽과 이모지 위치가 안 맞음) — 순수 아랍어만 있던 기존 disclaimer 배지는 이
문제가 없었는데, "이모지+아랍어"를 섞은 새 문자열에서만 발생. `ctx.direction = isRtl ?
'rtl' : 'ltr'`을 리본 텍스트 그리기 직전에 명시하고 그리기 직후 `'ltr'`로 복원해서 해결 —
고치기 전/후 아랍어 스크린샷을 직접 비교해서 확인함.

**검증**: `node --check script.js` 통과. 회귀 테스트 11개 전부 재실행 — 전부 `ISSUES: 0`
(`TOTAL ISSUES: 0`), `i18n_coverage_audit.js`(565개 키, 새 pickLang 문구가 없어서 키 개수
변화 없음)도 포함. Playwright로 saveHomeResultAsImage()를 직접 호출해서(annotate 오버레이로
넘기기 직전 `openAnnotateOverlay`를 가로채 캔버스를 PNG로 export) 한국어/영어/베트남어/
아랍어(RTL) 4개 언어로 실제 렌더링 결과를 확인 — 참고용 배지가 잘리지 않고 카드 중앙에
크고 선명하게 보이는지, 절취선·바코드·별 장식이 텍스트와 안 겹치는지, RTL에서 리본 이모지
순서가 자연스러운지 전부 눈으로 직접 확인(PNG 4장을 스크래치패드에 저장해서 확인). 리팩터한
`saveMyNumbersAsTicketImage()`(바코드 공유 헬퍼로 교체)도 실제 호출해서 기존과 동일하게
정상 렌더링되는지 별도로 확인(레이아웃 변경 없음, 헬퍼 추출만 했으므로). **네거티브 컨트롤**:
`drawDecorativeBarcode(...)` 호출 줄을 임시로 주석 처리 → 캔버스의 바코드 영역 픽셀을
직접 검사하는 스크립트로 "진짜 바코드가 사라짐"을 확인한 뒤 원복.

**의도적으로 안 한 것**: 캔버스 크기를 900×500→900×620으로 키움(절취선+스텁 공간 확보) — 다운
로드되는 이미지의 가로세로 비율이 달라지지만, `openAnnotateOverlay()`는 어떤 크기의 캔버스든
그대로 받아 처리하도록 이미 설계돼있어서(코드 확인 완료) 오버레이 쪽은 손댈 필요 없었음.
"WINNER" 텍스트, 실제 복권사/국세청 문양, 사용자별 커스터마이징(색 선택 등)은 위 안전장치
이유로 의도적으로 안 넣음.

변경 파일: `script.js`(바코드/별/절취선 헬퍼 3개 신규 + `saveHomeResultAsImage()` 리디자인 +
`saveMyNumbersAsTicketImage()`는 바코드 호출부만 헬퍼로 교체), `HANDOFF.md`(이 항목 + 오래된
"열세 번째 세션" 항목을 `HANDOFF-ARCHIVE.md`로 이동). 새 i18n 키 없음(위 참고). **`git push`
권한 여전히 없음**(구조적 제약, 재진단 불필요) — 로컬 커밋 후 zip으로 `SendUserFile` 전달,
사용자가 GitHub 웹 UI로 업로드해야 실제 반영됨. 다음 세션은 위 5번 항목대로
`git fetch origin main`부터 확인할 것.

### 2026-07-28 (열여덟 번째 세션, `claude/progress-checkpoint-vd08p1` — 서브에이전트로 진행, 같은 날 바로 앞 세션을 대체)

**이 세션은 바로 앞(열일곱 번째) 세션의 결과물을 대체함 — "복권 티켓" 스타일이 아니라 "대형
기념 수표" 스타일이 사용자가 실제로 원했던 것이었음.** 열일곱 번째 세션이 `saveHomeResultAsImage()`
를 번호 공·장식용 바코드·절취선·금색 리본 배지가 있는 "복권 티켓" 느낌(`saveMyNumbersAsTicketImage()`
와 같은 톤)으로 만들어 사용자에게 전달했는데, 사용자가 실제 확인해보고 "실제 당첨된 사람 복권
이미지처럼"이 의미했던 건 복권 티켓이 아니라 미국 주 복권공사가 실제 당첨자에게 사진 촬영용으로
건네는 **포스터보드 크기의 가짜 수표("giant novelty check")**였다고 레퍼런스 사진과 함께
명확히 함(넓은 직사각형 수표 모양, 좌상단 작은 발행처 로고, 우상단 게임 로고+날짜, 큰 세리프체
당첨자 이름, 거대한 금액, "Powerball Jackpot Winner" 문구, 하단에 당첨자가 수표를 들고 있는
사진). 이 요청에 맞춰 `saveHomeResultAsImage()`를 처음부터 다시 리디자인함 — 앞선 세션의 티켓
장식(번호 공·절취선·바코드·별)은 전부 제거.

**핵심 안전 판단(사용자 요청 원문의 하드 제약을 코드로 그대로 구현) — 다음 세션이 이 판단을
임의로 되돌리지 말 것**:
1. **실제 기관 이름/로고 절대 금지**: "파워볼"/"메가밀리언즈"/특정 주 복권공사명/은행명/정부
   문양을 전부 배제. 발행처 자리엔 사이트 자체 브랜드(참택스/chamtax.com, 기존
   `--teal`(#155445)/`--status-red`(#C0392B)/`--gold`(#D9B65A) 계열 색)만 씀 —
   `saveMyNumbersAsTicketImage()`가 이미 지켜온 "재미 이미지가 진짜 공식 서류로 오인되면
   안 된다" 원칙과 같은 선.
2. **실제 인물 사진/이름 절대 금지**: 레퍼런스 사진에 있던 실존 당첨자 이름은 이 코드베이스
   어디에도(커밋 메시지·주석·문서 포함) 적지 않음 — 요청에서도 이 이름을 하드코딩하지 말라고
   명시했음. 대신 "PAY TO THE ORDER OF"(=`CHECK_PAYTO_MORE`, 신규 26개 언어) 라벨 옆에 진짜
   빈 수표처럼 **밑줄만 그어진 빈칸**을 그림 — 사용자가 기존 "꾸며서 저장하기" 펜/텍스트
   오버레이(`openAnnotateOverlay()`, 이 함수가 캔버스를 다 그린 뒤 자동으로 넘겨주는 그 화면)로
   이 빈칸에 자기 이름을 직접 써넣는 흐름. 요청에서 "선택사항"이라고 명시한 얼굴 실루엣 장식은
   레이아웃 복잡도·26개 언어 여백 확보 리스크 대비 얻는 게 작다고 판단해 이번엔 넣지 않음(다음
   세션이 원하면 추가 가능 — 새 입력 필드나 사진 업로드 기능은 필요 없음, 순수 장식 요소만
   추가하면 됨).
3. **스캔·발행 가능한 진짜 수표로 오인될 요소(계좌번호·라우팅번호·MICR·서명란) 절대 없음**:
   "받는 사람" 빈칸 + 큰 금액만 장르 관습으로 넣고, 그 외 기능적 요소는 일절 안 그림.
4. **참고용 배지(`RESULT_IMG_DISCLAIMER_MORE`, 기존 그대로) 크기·비중 오히려 키움**: 이전 버전은
   작은 알약 배지(폭 14px 굵기)였는데, 이번엔 카드 폭 전체에 가까운 배너(폭 18px 굵기, 카드
   바닥까지 항상 16px 이상 여유)로 바꿈 — 수표 모양이 실제 금융 서류를 연상시키는 정도가
   티켓보다 크다고 판단해서 "더 작아지지 않게"가 아니라 "더 눈에 띄게" 강화함.
5. **"당첨/WINNER" 같은 새 미검증 문구를 추가로 만들지 않음**: `resultLabelText`(예: "💰 일시불
   예상 실수령액")는 이미 화면에서 "예상"이라는 프레이밍을 쓰고 있는 기존 문구라 그대로
   재사용 — 실제 당첨 발표처럼 읽힐 새 캡션은 아예 만들지 않음.

**유지한 것(요청의 "데이터는 그대로" 원칙)**: 로고, 계산 기준 한 줄(`basisMini`, 예: "100M USD
당첨 · 한국 거주자"), 결과 라벨, 큰 실수령액(오히려 58px→66px로 더 키움 — 캔버스가 넓어져서
시각적 비중이 줄지 않도록), 실수령/세금 비율 막대, before→after 금액 줄, 참고용 배지, 날짜 —
전부 이전과 같은 DOM 값을 그대로 읽어와서 캔버스에 옮김.

**레이아웃/치수 변경**: 캔버스를 900×620(거의 정사각형에 가까운 티켓 비율)에서 **1180×560
(약 2.1:1, 실제 대형 기념 수표의 가로로 긴 포스터보드 비율에 맞춤)** 로 다시 잡음. 이중 테두리
(바깥 굵은 브랜드 그린 + 안쪽 얇은 금색) + 네 모서리 코너 브래킷(순수 선 장식, 특정 기관 문양
아님)으로 "증서" 느낌을 냄 — 위변조 방지 무늬(길로셰) 같은 과한 금융서류 흉내는 안전장치 3번과
충돌 위험이 있어 의도적으로 안 함.

**신규 헬퍼**: `fitFontSize(ctx, text, weight, maxSize, minSize, maxWidth)` — 텍스트가 폭을
넘으면 폰트 크기를 1px씩 줄여서 26개 언어 어디서도 카드 밖으로 안 넘치게 하는 방어 헬퍼. 계산
기준 한 줄·"받는 사람" 라벨·실수령액·결과 라벨·퍼센트 줄·before→after 줄·참고용 배지까지
전부 이 헬퍼로 감쌈(이전 버전은 일부만 `Math.min()`으로 폭을 클램프하고 폰트는 고정이었음 —
이번엔 언어별 실제 렌더링 폭 차이가 더 클 수 있는 넓은 레이아웃이라 더 방어적으로 감).

**신규 i18n**: `CHECK_PAYTO_MORE`(pickLang 6개 위치 인자 + `more` 객체 21개, "받는 사람"/
"PAY TO THE ORDER OF" 라벨) — 실제 수표 법률 용어를 그대로 옮기는 대신("배서" 등은 나라마다
법적 의미가 다를 위험), 어느 언어에서나 안전하게 통하는 "받는 사람/수취인" 개념으로 26개 언어
전부 직접 번역함(en만 장르 관습상 바로 알아보는 고전 문구를 그대로 씀 — 요청 원문에도 이
표현이 예시로 나와있었음). 열여섯 번째 세션이 정한 선례(`input.currencyLabel`)와 같은 이유로,
짧은 상용구 하나라 Gemini 배치 번역 워크플로 없이 이 세션이 직접 번역함.

**삭제한 코드**: `drawTicketStar()`/`drawTicketPerforation()`(열일곱 번째 세션이 추가한 별·
절취선 헬퍼) — grep으로 `saveHomeResultAsImage()` 밖에서 참조가 전혀 없음을 확인한 뒤 삭제.
`drawDecorativeBarcode()`는 `saveMyNumbersAsTicketImage()`가 계속 쓰므로 그대로 유지(수표
디자인엔 바코드를 넣지 않음 — 장르가 달라져서 이제 어울리지 않는다고 판단).

**검증**: `node --check script.js` 통과. 새 `CHECK_PAYTO_MORE`가 21개 `more` 언어(km/ne/id/my/
si/uz/mn/kk/ky/ur/bn/lo/ja/ar/hi/fr/tl/pt/es/uk/tet) 전부 채워졌는지 Node 스크립트로 직접
`eval`해서 누락 0건 확인. 회귀 테스트 11개 전부 재실행 — 전부 `ISSUES: 0`(`TOTAL ISSUES: 0`),
`i18n_coverage_audit.js`(565개 키, data-i18n 키 수 불변 — `CHECK_PAYTO_MORE`는 `pickLang()`
동적 문자열이라 이 감사 대상이 아님, 위 "다국어 작업 방법" 4번 참고)도 포함. Playwright로
`saveHomeResultAsImage()`를 직접 호출해서(annotate 오버레이로 넘기기 직전을 가로채 캔버스를
PNG export) 한국어/영어/베트남어/아랍어(RTL) 4개 언어 실제 렌더링을 눈으로 확인 — (a) 실제
기관 이름/로고 없음 (b) 실제 인물 이름 없음 (c) "받는 사람" 빈칸이 밑줄로 뚜렷이 보임 (d)
참고용 배지가 카드 폭 배너로 잘리지 않고 선명하게 보임 (e) 아랍어에서 이모지+텍스트 방향이
자연스러움(전 세션이 고쳐둔 `ctx.direction` 로직 그대로 재사용해서 재발 안 함) 전부 확인.
`saveMyNumbersAsTicketImage()`(공유 헬퍼 `drawDecorativeBarcode()`를 그대로 쓰는 별개 기능)도
실제 호출해서 기존과 동일하게 정상 렌더링되는지 별도로 확인(번호 공·바코드·배지 전부 정상).
**네거티브 컨트롤**: "받는 사람" 빈칸을 그리는 `ctx.stroke()` 줄을 임시로 주석 처리 →
캔버스의 해당 행 픽셀을 직접 스캔하는 스크립트로 "빈칸 선이 사라짐"(비배경 픽셀 3,870개 →
0개)을 확인한 뒤 원복(원복 후 다시 3,870개로 복귀 확인) — 이어서 회귀 테스트 핵심 3개도
재실행해서 원복이 완전한지 재확인함.

**의도적으로 안 한 것**: 얼굴 실루엣 장식(요청에서 "선택사항"이라 명시, 위 안전장치 2번 참고),
"AMOUNT" 같은 부가 라벨(금액 자체가 이미 통화 기호를 포함한 완성된 표시값이라 불필요),
글로 풀어쓴 금액("이백삼십사만... 원정" 같은 수표의 "금액 문자 표기" 줄) — 임의 통화·26개
언어 조합에 대해 정확한 숫자→글자 변환을 구현하는 건 실수 위험이 크고, 참고한 레퍼런스
자체("포토용 대형 기념 수표")에도 이런 법률 문서 요소는 없었음.

변경 파일: `script.js`(`saveHomeResultAsImage()` 전면 재작성 + 신규 `CHECK_PAYTO_MORE`/
`fitFontSize()` + `drawTicketStar()`/`drawTicketPerforation()` 삭제, `drawDecorativeBarcode()`는
그대로), `HANDOFF.md`(이 항목). **`git push` 권한 여전히 없음**(구조적 제약, 재진단 불필요) —
로컬 커밋 후 zip으로 `SendUserFile` 전달, 사용자가 GitHub 웹 UI로 업로드해야 실제 반영됨.
다음 세션은 위 5번 항목대로 `git fetch origin main`부터 확인할 것.

### 2026-07-28 (열아홉 번째 세션, `claude/progress-checkpoint-vd08p1` — 서브에이전트로 진행)

**요청 배경**: 사용자가 홈 화면 금액 입력(일시불 탭 `#homeAmountInput`/`#homeAmountSlider`,
연금액 탭도 같은 공용 로직 공유)에서 음성으로 "천원"/"백원"/"천만원"처럼 작은 금액을 말했을 때
잘 안 먹힌다고 제보. 조사 결과 원인 2가지를 확인(둘 다 통화·언어 무관한 공용 숫자 파이프라인에
있어서 한국어/원화만의 문제가 아니었음):
1. **음성 반올림이 소수점 4자리 고정**(`Math.round(millions * 10000) / 10000`, Million USD
   단위) — 이 격자 자체가 약 $100 단위라, "백원"(~$0.067)·"천원"(~$0.67)이 정확히 0으로
   반올림돼 입력이 조용히 아무 반응도 안 함.
2. **슬라이더 로그축 하한(`SLIDER_LOG_FLOOR_M`)이 1(=$100만)에 고정** — 그보다 작은 값(예:
   "천만원"≈$6,700)은 `setSliderMillions()`의 `Math.round(safeMillions)`가 소수를 0으로
   뭉개버린 뒤 이 하한으로 다시 끌어올려져서, 서로 다른 소액이 전부 같은(왼쪽 끝) 위치로
   보임 — 계산 자체는 되는데 슬라이더 핸들만 실제 값과 무관하게 고정돼 보이는 문제.

**목표 스케일 확정(사용자 채팅으로 확인, 이 공개 문서엔 대화 원문 대신 결론만)**: "만원~수천만원"
(대략 ₩10,000~수천만원, USD로 약 $6.7~$수만) 구간이 실제로 잘 동작하면 됨 — 복권 잭팟
계산기라는 정체성상 리터럴 ₩1 단위까지 억지로 늘리면 슬라이더의 주 사용 구간(파워볼/메가밀리언즈
실제 잭팟, $20M~$2B 스케일)의 상대적 정밀도가 희석되므로 그렇게까지는 안 함.

**수정 1 — 유효숫자 기준 반올림 도입**: `roundToSignificantFigures(n, sigFigs)` 신규 함수
(`roundAmountForInput()` 바로 위, ~6900줄대) — 고정 소수점 자릿수 대신 "유효숫자 개수"로
반올림해서, 값의 크기에 비례해 반올림 격자 자체가 같이 줄어들게 함(작은 값은 안 뭉개지고,
큰 값은 반대로 magnitude가 작아져서 자연히 깔끔한 정수/한두 자리 소수로 남아 "지저분한 소수점"도
안 생김). 적용한 곳 3군데:
- `startAmountVoiceInput()`의 `onresult`(음성 인식 결과 반올림) — 4자리 유효숫자로 교체.
- `applyHomeAnnouncedMillions()`의 `lumpMillions`(연금 발표액→일시불 환산값) — 예전엔
  `Math.max(1, Math.round(...))`로 "정수 + 최소 1(Million USD)"를 강제해서, 연금액 탭에
  소액을 넣으면 실제 환산값(예: $3,900)이 아니라 $1,000,000으로 부풀려지는 별개의 버그가
  있었음(조사하며 추가로 발견 — 슬라이더 하한 버그와 같은 뿌리). 강제 정수/최소값 없이
  유효숫자만 보존하도록 교체.
- `roundAmountForInput()`(입력칸 재표시 반올림) — 1 미만 구간만 고정 소수점 2자리 대신 유효숫자
  방식으로 교체(1 이상 구간은 완전히 그대로 유지, 회귀 없음). KRW "억원" 단위에서 0.0001억원
  (=만원)처럼 1 미만인 값이 예전엔 `round(n*100)/100`에 걸려 그대로 0으로 사라졌었음.

**수정 2 — 슬라이더 로그축 하한/usdMin 계산 재설계**: `SLIDER_LOG_FLOOR_M`을 `1`→`0.00000001`
(=$0.01, 순수 안전장치용 — 로그가 0 이하를 못 다루는 것에 대한 최후 방어선일 뿐, 실제 usdMin은
아래 규칙이 결정) 로 낮춤. `setSliderMillions()`의 usdMin 계산을 `Math.max(SLIDER_LOG_FLOOR_M,
Math.min(10, Math.round(safeMillions)))`(반올림이 소수를 0으로 죽이는 게 근본 원인)에서
`safeMillions <= 10 ? Math.max(SLIDER_LOG_FLOOR_M, safeMillions * 0.8) : 10`으로 교체 — usdMax
쪽이 큰 값이 들어오면 그 값 자체를 상한으로 쓰는 것과 대칭으로, 작은 값이 들어오면 그 값의 80%를
하한으로 써서(핸들이 로그축 맨 왼쪽에 완전히 달라붙지 않게 약간 여유) 서로 다른 소액이 서로
다른(값에 비례한) 위치에 보이게 함. **기본 페이지 로드 시 범위(10~2,000M USD, `index.html`의
정적 `data-usd-min/max`)는 이 상수와 무관하게 그대로**라 주 사용 사례(잭팟 스케일)엔 영향 없음
— 10 초과 구간은 여전히 `usdMin=10`.

**수정 3 — `sliderPosToMillions()`의 `roundTo` 단계 세분화**: 예전엔 `raw < 500 ? 10 : 50`
2단계뿐이라, 슬라이더가 다루는 범위가 작아지면(예: raw=0.0067) 10 단위 반올림에 걸려 통째로
0이 돼버림. `SLIDER_ROUND_TIERS`(십진 등비, 0.00000001~500 사이 10단계) + `sliderRoundStep()`로
세분화 — **10 이상 구간(500 미만은 10, 500 이상은 50)은 완전히 기존 그대로**라 주 사용 사례엔
회귀 없음, 그 아래로 8단계를 추가해서 작은 스케일에서도 핸들을 드래그했을 때 값이 0으로
뭉개지지 않게 함.

**손대지 않은 것(범위 밖 확인)**: `renderSliderTicks()`/`SLIDER_TICK_CANDIDATES`(20~5000M USD
고정 후보)는 작은 스케일에선 그냥 눈금이 하나도 안 뜨는 것으로 그침(에러·이상 렌더링 없음,
Playwright로 확인) — 요청에서도 이 정도는 허용 범위라고 명시해서 새 소규모 눈금 티어는 추가
안 함. `MAX_INPUT_MILLIONS`(1,000,000)도 그대로, 상단 스케일은 이번 작업과 무관.

**놓칠 뻔했다가 발견한 별개 이슈(고치지 않고 문서화만 함, 범위 밖)**: `formatWon()`은 항상
"억원" 단위로 `Math.round()` 반올림하는 구조라, 이번에 고친 "만원~수천만원" 구간의 상당수
(0.5억원=5,000만원 미만)는 내부 계산·입력칸·슬라이더는 전부 정확한데도 화면 헤드라인
("입력하신 금액"/"실수령액")엔 여전히 "0억원"으로 보임(Playwright로 실제 렌더링 확인 —
예: 천만원 입력 시 실수령액 700만원까지는 정확히 계산되지만 헤드라인은 "0억원"). 이건 이번
버그(반올림이 값 자체를 0으로 죽이는 것)와는 다른, `formatWon()`의 "억 단위보다 작은 값은
표시 못 함"이라는 기존 설계상 한계 — 여러 화면에서 공유하는 함수라 이번 세션 범위 밖으로 남김.
다음에 이 부분을 사용자가 다시 지적하면, 음성/슬라이더 버그가 아니라 이 표시 함수 자체의
문제임을 먼저 확인할 것.

**검증**: `node --check script.js` 통과. 회귀 테스트 11개 전부 재실행 — 전부 `ISSUES: 0`
(`TOTAL ISSUES: 0`). `tests/nav_slider_audit.js`에 소액 스케일 회귀 테스트 신규 추가(음성
반올림이 0/null이 안 되는지 한국어 4개 케이스 + 베트남어 1개 케이스, 슬라이더 하한이 값에
비례해서 서로 달라지는지, `SLIDER_LOG_FLOOR_M` 자체가 다시 커지지 않았는지). **네거티브
컨트롤 2건**: (1) `SLIDER_LOG_FLOOR_M`을 임시로 `1`로 되돌림 → 새 슬라이더 하한 테스트가
이슈 6건을 실제로 잡아내는지 확인 후 원복(원복 후 재실행 0건 확인). (2) 테스트 안의 반올림
호출을 임시로 옛날 방식(`Math.round(millions*10000)/10000`)으로 바꿔치기 → "천원"/"백원"/
"만원" 3건이 0으로 사라지는 걸 실제로 잡아내는지 확인 후 테스트 파일 원복. 원복 후 `git status`로
두 파일(`script.js`, `tests/nav_slider_audit.js`) 외에 다른 변경이 안 남았는지 확인함.
Playwright로 한국어("천원"/"백원"/"만원"/"천만원") + 베트남어("10000000 đồng") 양쪽에서 실제
`parseSpokenAmountToMillions()`→`onHomeAmountTyped()` 전체 파이프라인을 호출해 (a) 입력칸에
정확한 값이 남는지 (b) 슬라이더 위치가 서로 다른 소액마다 다르게(비례해서) 찍히는지 (c)
`home-final-amt`의 `dataset.eokVal`(실수령액)이 손계산(30% 미국 비거주자 원천징수 → 한국
누진세는 FTC로 전액 상계돼 결과적으로 70% 실수령, 만원→7,000원/천만원→7,000,000원 등)과
정확히 일치하는지 확인. 연금액 탭(`applyHomeAnnouncedMillions`)도 소액(0.1억원 발표)으로
별도 확인해서 더 이상 $1M로 부풀려지지 않고 정확한 환산값이 나오는지 확인. 1500억원으로
되돌려서 주 사용 사례(잭팟 스케일)에 회귀 없음도 확인(`sliderUsdMin=10`/`sliderUsdMax=2000`
그대로).

변경 파일: `script.js`(`roundToSignificantFigures()` 신규 + `roundAmountForInput()`/음성
`onresult`/`applyHomeAnnouncedMillions()` 3곳 반올림 방식 교체 + `SLIDER_LOG_FLOOR_M`/
`setSliderMillions()`/`sliderPosToMillions()`의 `roundTo` 로직), `tests/nav_slider_audit.js`
(소액 스케일 회귀 테스트 신규), `HANDOFF.md`(이 항목 + 오래된 "열다섯/열여섯 번째 세션" 항목을
`HANDOFF-ARCHIVE.md`로 이동 + "알려진 미해결 항목"에 `formatWon()` 관련 새 항목 추가).
**`git push` 권한 여전히 없음**(구조적 제약, 재진단 불필요) — 로컬 커밋 후 zip으로
`SendUserFile` 전달, 사용자가 GitHub 웹 UI로 업로드해야 실제 반영됨. 다음 세션은 위 5번
항목대로 `git fetch origin main`부터 확인할 것.

### 2026-07-28 (스무 번째 세션, `claude/progress-checkpoint-vd08p1` — 서브에이전트로 진행)

**요청 배경**: 열여덟 번째 세션이 만든 "대형 기념 수표" 스타일 결과 카드(`saveHomeResultAsImage()`)에
대해 사용자가 채팅으로 두 가지 후속 수정을 요청함(공개 문서엔 대화 원문 대신 결론만): (1) 실수령/
세금 비율 막대 + 퍼센트 텍스트 줄 제거(카드가 너무 빽빽해 보인다는 판단), (2) 캔버스에 찍히는
날짜를 항상 실제 오늘 날짜로 고정하지 말고 사용자가 직접 정할 수 있게 편집 가능한 입력칸 추가.

**수정 1 — 비율 막대 + 퍼센트 줄 제거**: `saveHomeResultAsImage()`에서 빨강 배경/초록 채움 막대
(`barW`/`barH`/`barX0`/`barY0` 관련 `ctx.roundRect`+`ctx.clip`+두 번의 `ctx.fillRect`)와 그 아래
`pctLine`(`${takePct}% ${takeLabel} · ${taxPct}% ${taxLabel}`) 텍스트를 통째로 삭제. 이 두 값만
쓰던 DOM 읽기 4개(`result-visual-take-pct`/`result-visual-tax-pct`/`[data-i18n="result.takeHomeLabel"]`/
`[data-i18n="result.taxLabel"]`)도 함수 안에서 grep으로 다른 용도가 전혀 없음을 재확인한 뒤 같이
삭제 — 화면(온스크린)의 `result-visual-bar`/`result-visual-take-pct` 자체(`updateResult()`가 채우는
실제 위젯)는 이 카드와 무관한 별개 요소라 손대지 않음. 막대가 있던 자리를 빈 공백으로 남기지
않도록, before→after 세전/세후 줄(`beforeAfterLine`)을 원래 위치(y=406)에서 결과 라벨(y=300)과
아래 참고용 배지 상단(bannerY0≈470) 사이 남은 공간의 정중앙(y=386)으로 끌어올려 자연스럽게 다시
채움 — 참고용 배지의 위치·크기(폭 전체 배너, 굵기 18px, 카드 바닥까지 16px 이상 여유)는 전혀
안 건드림(요청의 "명료함 강화, 절대 축소 금지" 원칙 재확인).

**수정 2 — 날짜를 사용자가 직접 편집 가능하게**: 기존엔 함수 안에서 `new Date()`로 항상 실제
오늘 날짜를 계산해서 그대로 그렸음 — 이제 index.html에 새 텍스트 입력칸(`#home-check-date-input`,
"이미지로 저장" 버튼 바로 위, 라벨 `home.checkDateLabel`)을 추가하고, 캔버스를 그릴 때 이 입력칸의
값을 읽어서 사용함. **네이티브 `<input type="date">` 대신 텍스트 입력을 택함** — 이 코드베이스에
이미 있는 정확한 선례(확률체감 탭 "날짜로 당첨번호 찾아보기" 섹션, 2026-07-24)가 iOS Safari에서
네이티브 date input의 내부 UI가 CSS 지정 폭을 넘어 튀어나오는 문제 때문에 select 3개로 바꾼
전례라, 여기서도 같은 이유로 피하고 `#compare-rate-input`과 같은 인라인 텍스트 입력 스타일(밑줄
포커스 등)을 재사용함. 값 검증은 신규 `sanitizeCheckDateForCanvas(rawValue)`(script.js,
`saveHomeResultAsImage()` 바로 위)가 전담 — `/^\d{4}-\d{2}-\d{2}$/` 정규식 검사 후, `Date`
생성자로 만든 날짜가 입력값과 정확히 같은지(연/월/일 각각) 되짚어 확인해서 "2026-02-30"처럼
존재하지 않는 달력 날짜까지 걸러냄; 비어있거나 형식이 안 맞거나 존재하지 않는 날짜면 전부
기존과 동일하게 오늘 날짜로 대체함. 입력칸 자체는 페이지 로드 시(`DOMContentLoaded`) 빈 칸이
아니라 오늘 날짜로 미리 채워둠 — "날짜로 당첨번호 찾아보기" select가 이미 확인한 교훈(빈 date
입력칸은 아이폰 사파리 등에서 완전히 텅 빈 것처럼 보여 사용자가 뭘 넣어야 할지 헷갈려함,
2026-07-22)과 같은 이유.

**신규 i18n**: `home.checkDateLabel`(data-i18n, "카드에 표시할 날짜"/"Date on card") — 라벨
텍스트와 `aria-label` 양쪽에 같은 키 재사용(`input.currencyLabel`이 이미 쓰던 패턴과 동일).
26개 언어(ko 포함, ko는 HTML 원문) 전부 직접 번역해서 `i18n-source/translations.json`에 추가 —
기존 `input.currencyLabel`(통화)·`privacy.effectiveDate`(날짜) 두 키가 이미 26개 언어 전부
확보해둔 "통화"/"카드"/"날짜" 개별 단어 번역을 참고해 일관된 어휘로 맞춤(예: 미얀마어 "ကတ်"=카드,
크메르어 "កាលបរិច្ឆេទ"=날짜). `node scripts/build-i18n.js` 실행 후 `i18n/*.json` 26개 재생성,
`tests/i18n_coverage_audit.js`로 567개 키(기존 566+1) 전부 0건 확인. placeholder("YYYY-MM-DD")는
포맷 예시일 뿐이라 언어 무관으로 판단해 번역 키를 따로 안 만듦.

**검증**: `node --check script.js` 통과. 회귀 테스트 11개 전부 재실행 — 전부 `ISSUES: 0`
(`TOTAL ISSUES: 0`), `i18n_coverage_audit.js`(567개 키) 포함. Playwright로 `saveHomeResultAsImage()`를
직접 호출해서(annotate 오버레이로 넘기기 직전을 가로채 캔버스를 PNG export) 한국어(기본 날짜)/
한국어(커스텀 날짜 "2030-12-25" 직접 입력)/영어/아랍어(RTL) 4장을 실제로 눈으로 확인 — (a) 막대·
퍼센트 텍스트가 완전히 사라지고 빈 공백 없이 before→after 줄이 자연스럽게 그 자리를 채움 (b) 입력칸에
타이핑한 커스텀 날짜가 실제 오늘 날짜(2026-07-28) 대신 캔버스에 정확히 그려짐 (c) 참고용 배지가
여전히 카드 폭 전체 배너로 선명하게 보임(안 잘림, 안 작아짐) (d) 그 외 요소(로고, 받는 사람 빈칸,
큰 금액, 결과 라벨)는 전과 동일. 온스크린 UI 자체도 스크린샷으로 확인 — 날짜 입력칸이 저장 버튼
바로 위에 오늘 날짜로 미리 채워진 채 보임. **네거티브 컨트롤**: 날짜 읽기 로직을 임시로 원래
`new Date()` 고정 코드로 되돌린 뒤, 커스텀 날짜("2030-12-25")를 입력하고 저장해도 여전히 실제
오늘 날짜(2026-07-28)만 그려지는 것을 확인(=이 검증 방법이 실제로 회귀를 잡아낼 수 있음을 증명)한
후 원복, 원복 후 다시 커스텀 날짜가 정확히 반영되는지와 핵심 회귀 테스트(`home_audit.js`,
`i18n_coverage_audit.js`)로 원복이 완전한지 재확인함.

**세션 시작 시 확인**: `git fetch origin main` 결과 `origin/main`이 이전 세션 종료 후 사용자가
zip을 GitHub 웹 UI로 업로드하면서 여러 "Add files via upload" 커밋으로 전진해있었으나, 트리
내용은 이 세션 시작 시점의 로컬 `HEAD`(`134bf65`, 열아홉 번째 세션 커밋)와 완전히 동일함
(`git diff HEAD origin/main` 0줄) — 즉 이전 세션분은 이미 전부 정상 반영됐고 이번 세션은 그 위에
새로 커밋만 하면 되는 깨끗한 상태였음(병합 불필요).

변경 파일: `script.js`(`saveHomeResultAsImage()`의 막대/퍼센트 줄 삭제 + before→after 줄 위치
재조정 + 신규 `sanitizeCheckDateForCanvas()` + 날짜 입력칸 기본값 채우는 `DOMContentLoaded` 리스너
신규), `index.html`(`#home-check-date-input` 텍스트 입력칸 신규, "이미지로 저장" 버튼 바로 위),
`styles.css`(`.check-date-row`/`.check-date-label`/`.check-date-input` 신규), `i18n-source/translations.json`
+ `i18n/*.json` 26개(`home.checkDateLabel` 신규 키), `HANDOFF.md`(이 항목). **`git push` 권한
여전히 없음**(구조적 제약, 재진단 불필요) — 로컬 커밋 후 zip으로 `SendUserFile` 전달, 사용자가
GitHub 웹 UI로 업로드해야 실제 반영됨. 다음 세션은 위 5번 항목대로 `git fetch origin main`부터
확인할 것.
